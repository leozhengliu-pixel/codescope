# Parity gap report

_This document is the canonical repo-local parity gap report for `sourcebot-rewrite`._

This report tracks parity gaps against the 2026-04-18 full-parity roadmap. Task `03` is too broad for a single slice, so the completed slices now cover **task03a** (**backend/API**), **task03b** (**worker**), **task03c** (**integrations**), **task03d** (**frontend**), **task03e** (**auth/admin**), and **task03f** (**ops**). Later roadmap slices should extend this same document with implementation evidence instead of creating competing gap reports.

## Status legend

- **Missing** — no meaningful rewrite implementation evidence exists yet for the parity-facing capability.
- **Partial** — some rewrite surface or behavior exists, but major parity requirements, acceptance coverage, durability, or operational behavior are still open.
- **Complete** — parity looks meaningfully closed based on current repo evidence and acceptance coverage.

## Backend/API domain

Grounding for this slice comes from the live API router in `crates/api/src/main.rs`, the acceptance inventory in `specs/acceptance/index.md`, the current acceptance specs under `specs/acceptance/`, the parity matrix in `specs/FEATURE_PARITY.md`, and the roadmap phases/tasks in `docs/plans/2026-04-18-sourcebot-full-parity-roadmap.md`.

| Capability area | Current rewrite evidence | Status | Highest-value next gap(s) |
| --- | --- | --- | --- |
| Service health, readiness, and public config | `crates/api/src/main.rs` exposes `/healthz`, `/readyz`, and `/api/v1/config`; acceptance index already treats these as the current public/operator-facing baseline. `/readyz` is bounded to file-backed readiness plus PostgreSQL SQLx-migration-inventory reachability when `DATABASE_URL` is configured. | Partial | Add broader operator-focused acceptance coverage and production-grade config/observability/readiness parity called out by Phase 0 and later ops tasks. |
| Local bootstrap and session auth API baseline | Router exposes `/api/v1/auth/bootstrap`, `/api/v1/auth/login`, `/api/v1/auth/me`, `/api/v1/auth/logout`, and `/api/v1/auth/revoke`; `specs/acceptance/auth.md` now also grounds the narrower PostgreSQL-backed bootstrap-admin, durable local-session, and local-account/membership/invite auth baseline when `DATABASE_URL` is configured, including invited-account login, auth-me identity restoration, admin-visible member/invite rosters, linked-account memberships, invite acceptance persistence, and the fail-closed credential-verification burn path for missing/corrupted persisted secrets, without claiming broader production session hardening. | Partial | Keep the shipped PostgreSQL bootstrap/session/local-account baseline grounded, then finish the still-deferred API-key/OAuth-client durability, broader organization aggregates, richer onboarding/admin management flows, and acceptance detail from roadmap Phases A/C. |
| API key and auth-adjacent admin endpoints | Router exposes `/api/v1/auth/api-keys` and revoke, plus `/api/v1/auth/search-contexts`; acceptance auth coverage now also locks the narrower fail-closed credential-verification burn baseline for missing/corrupted persisted API-key secrets, and `specs/acceptance/search.md` grounds a backend saved-context lifecycle where authenticated users can create/list/delete contexts and apply them to `/api/v1/search?context_id=...` without leaking hidden repos. | Partial | Finish full API key lifecycle semantics, SQL-backed search-context durability, frontend context management UI, richer search-context editing behavior, and broader endpoint-level acceptance evidence. |
| Repository catalog, tree/blob, commit, and code-navigation APIs | Router exposes `/api/v1/repos`, repo detail, tree, blob, definitions, references, commits, commit detail, and commit diff endpoints; the current browse/commit surface now accepts repo-scoped revision queries for tree and commit history, preserves embedded-newline Git paths in revision tree output through NUL-delimited parsing, skips obvious build-artifact directories during recursive local browse `glob`/`grep` traversal, returns binary blob metadata with `is_binary: true` plus empty text content for non-UTF-8 local or revisioned blobs instead of failing UTF-8 decoding, loads per-file commit-diff patches with literal Git pathspec semantics so metacharacter-bearing filenames cannot include sibling patches, caps patch text at 64 KiB with `patch_truncated` status, and anchors no-revision local-sync commit fallback list/detail/diff reads to the successful sync job's recorded revision instead of exposing later unsynced local commits. | Partial | Expand beyond the current repo-scoped revision, binary-metadata, and backend diff-patch cap baseline into fuller branch/ref discovery UX, multi-language code-nav correctness, pagination/diff UX polish, richer repo metadata/status behavior from Phases 2–4, frontend binary/download affordances, and real indexing/performance parity beyond the current browse guardrail. |
| Search API and UI | Router exposes `/api/v1/search`; `specs/acceptance/search.md` defines cross-repo, cross-branch, filter, snippet, saved-context, bounded pagination, and fail-closed explicit repository-filter expectations; the API now accepts bounded `limit`/`offset` parameters and returns visibility-scoped `pagination.limit`, `pagination.offset`, `pagination.total_count`, and `pagination.has_more` metadata alongside the current result page, rejects hidden/unknown/blank present `repo_id` filters instead of broadening search, exposes explicit `mode=boolean|literal|regex` matching across startup and local-sync artifact-backed search paths, surfaces malformed/non-regular local-sync search-index artifacts and artifacts with mismatched `index_status.indexed_line_count` through `/api/v1/repos/{repo_id}/index-status` as `error` status with a stable redacted `malformed local-sync search index artifact` parse/load reason instead of silently trusting mutable snapshot contents, rejects symlinked artifact parents before creating writer directories through them, treats primary startup index `error` status as unavailable so requested-repo search can still use the latest authorized local-sync artifact, the startup/local-sync search store now matches boolean query text case-insensitively, treats unquoted space-separated query words as all-term line matches within boolean groups, preserves quoted phrases as literal line substrings, applies a bounded unquoted `OR` separator between line-term groups, applies minimal unquoted `lang:` filters plus substring and glob-style `path:` result-path filters, returns deterministic repo/path/line ordering for identical indexed content and merged primary/local-sync fallback API results before pagination, and `#/search` renders a result-count summary, boolean/literal/regex mode selector, and previous/next controls that preserve the selected mode while requesting the next bounded API page. | Partial | Replace the current bounded search path with real indexing, richer filter grammar beyond bounded literal/regex/phrase/path-glob/language/OR/order components, cross-branch semantics, stronger stable-pagination/relevance guarantees across changing indexes, and streaming search API parity from Phase 3. |
| Ask/chat completion API | Router exposes `/api/v1/ask/completions`, `/api/v1/ask/threads`, and `GET`/`PATCH`/`DELETE /api/v1/ask/threads/{thread_id}`; `specs/acceptance/ask.md` now truthfully locks the shipped baseline around repo-scoped ask submission, authenticated thread list/detail/reopen, frontend-backed title/private-shared visibility updates and selected-thread deletion on `#/chat`, citations, hash-restored active-thread continuity, and the dedicated `#/agents` review-agent visibility route while explicitly deferring archive controls, richer conversation/source-preview UX, and richer agent management/retry/orchestration. | Partial | Expand beyond the current ask/chat plus dedicated agents-route baseline into archive/richer conversation lifecycle controls, richer source-preview presentation, and deeper agent-management semantics from Phase 5 without over-claiming those deferred surfaces. |
| Review webhook and review-agent visibility/intake APIs | Router exposes authenticated review-webhook, delivery-attempt, and review-agent-run listing/detail endpoints plus public webhook event intake; the frontend now uses those existing authenticated visibility APIs both in the shared `#/settings/review-automation` inventory shell and in the dedicated `#/agents` route baseline that restores a selected run and loads its related webhook plus delivery-attempt detail views, while acceptance coverage now also locks the narrower fail-closed credential-verification burn baseline for wrong/missing/corrupted persisted webhook secrets. | Partial | Finish durable worker orchestration, retries, scheduling, failure recovery, and dedicated worker/operator acceptance specs before claiming automation parity. |
| Audit, analytics, and OAuth client admin APIs | Router exposes `/api/v1/auth/audit-events`, `/api/v1/auth/analytics`, and `/api/v1/auth/oauth-clients`; `/api/v1/auth/audit-events` now reads PostgreSQL audit rows scoped to organizations visible to the current user when `DATABASE_URL` is configured, and local member invite create/cancel, member-role update, and member-removal mutations write durable PostgreSQL audit events while preserving file-backed audit visibility as fallback/compatibility mode. Parity matrix still lists broader audit logs, analytics, and OAuth client/token flows as later-phase advanced features. | Partial | Implement full OAuth authorization/token/revocation flows, broaden durable audit/analytics/admin record coverage beyond the local member lifecycle slice, and add richer filtering/export/retention acceptance evidence. |
| Connection management and repository-status APIs | `crates/api/src/main.rs` already exposes authenticated `GET/POST /api/v1/auth/connections`, `PUT/DELETE /api/v1/auth/connections/{connection_id}`, `GET/POST /api/v1/auth/repository-sync-jobs`, and `POST /api/v1/auth/repository-sync-jobs/{job_id}/retry`; `specs/acceptance/repository-operations.md` and `specs/acceptance/generic-local-git.md` now lock the current read-only sync/history, bounded admin-only sync enqueue/retry, and generic/local connection-management baseline. | Partial | Move beyond settings-shell CRUD, read-only sync history, and backend-only queued-job creation into durable catalog-backed repository-status parity, coherent settings/admin navigation, real provider enumeration/import/runtime behavior, worker execution, retries, and operator progress controls from roadmap Tasks 18–20 and 55–59. |

### Evidence notes

- The backend/API surface is broader than a pure MVP: the router already exposes public config, local auth, repo browse/search/commit/code-nav, ask completions, review-webhook visibility, audit, analytics, OAuth-client, and API-key endpoints.
- Even so, the evidence stays mostly at the **route-family** level today. The acceptance docs intentionally describe broad behavior, and `specs/FEATURE_PARITY.md` is only partially audited so far rather than serving as a fully evidence-backed parity ledger yet.
- The roadmap explicitly schedules major backend/API follow-up work after this slice: durable metadata foundations (Phase 1), connection CRUD and sync visibility (Phase 2), real search/indexing parity (Phase 3), browse/commit/code-nav hardening (Phase 4), ask/chat contract completion (Phase 5), and OAuth/auth/admin expansion (Phases 6–7). Catalog persistence, analytics/audit aggregates, and remaining organization-state durability still remain follow-up work after the PostgreSQL API-key/OAuth-client slice.
- Because those downstream roadmap items remain open, this slice treats current backend/API parity as **Partial** unless the repo shows a clearly closed gap, which it does not yet.

## Worker domain

Grounding for this slice comes from the live worker entrypoint in `crates/worker/src/main.rs`, the worker library and focused smoke/unit coverage in `crates/worker/src/lib.rs` plus `crates/worker/tests/*.rs`, the acceptance inventory and journey map in `specs/acceptance/index.md` and `specs/acceptance/journeys.md`, the parity matrix in `specs/FEATURE_PARITY.md`, and the roadmap follow-up tasks in `docs/plans/2026-04-18-sourcebot-full-parity-roadmap.md`.

| Capability area | Current rewrite evidence | Status | Highest-value next gap(s) |
| --- | --- | --- | --- |
| One-shot/bounded worker entrypoint and state-path wiring | `crates/worker/src/main.rs` loads `AppConfig::from_env()`, builds the organization store from `config.organization_state_path`, converts the configured stub outcomes, logs the resolved runtime baseline, and runs one tick by default or a bounded multi-tick loop when `SOURCEBOT_WORKER_MAX_TICKS` is configured; `specs/acceptance/worker-runtime.md` records that black-box baseline. | Partial | Extend beyond the current explicitly bounded logged baseline into durable orchestration and richer operator-visible runtime status. |
| Single-run claim/complete-fail execution path | `crates/worker/src/lib.rs` claims the next queued review-agent run, routes it through `execute_claimed_review_agent_run_stub`, persists either Completed or Failed through the organization store, recovers stale `running` repository-sync jobs after a one-hour lease before claiming the next queued repository-sync job, and can queue exactly one automatic stale-lease retry after an additional one-hour backoff when no queued/running or prior auto-retry replacement exists for the same organization/repository/connection target. | Partial | Replace the stub-only execution boundary with real review-agent execution, durable queue orchestration, richer failure handling, and broad retry/backoff behavior from roadmap Phase 8. |
| Idle/no-work runtime behavior | `crates/worker/src/main.rs` logs `no queued review-agent run or repository sync job available`; `crates/worker/tests/no_queued_review_agent_run_idle_smoke.rs` proves successful exit for default-empty, missing-file, and existing-no-queued state without unintended persisted-state mutation; `crates/worker/tests/worker_runtime_logging_smoke.rs` now proves the no-work log still appears alongside the startup runtime baseline. | Partial | Extend the current idle-path evidence with richer operator-facing diagnostics, scheduling semantics, and runtime visibility instead of only one-shot smoke guarantees. |
| Config-driven stub outcome contract | `crates/worker/src/main.rs` reads `SOURCEBOT_STUB_REVIEW_AGENT_RUN_EXECUTION_OUTCOME` plus `SOURCEBOT_STUB_REPOSITORY_SYNC_JOB_EXECUTION_OUTCOME`; `crates/worker/tests/explicit_completed_stub_outcome_smoke.rs`, `invalid_stub_outcome_smoke.rs`, `repository_sync_claim_smoke.rs`, and `worker_runtime_logging_smoke.rs` prove explicit configured outcomes, fail-closed validation, startup runtime-baseline logging, and repository-sync failed terminal-status logging. | Partial | Replace stub-outcome-only control with the real execution contract while preserving fail-closed config validation and truthful runtime-baseline logging. |
| Ordered bounded runtime over queued runs | `crates/worker/tests/explicit_completed_stub_outcome_smoke.rs` proves repeated real-binary invocations process only one oldest queued run at a time, then advance to the next-oldest remaining queued run on later invocations; `crates/worker/tests/repository_sync_claim_smoke.rs` now also proves `SOURCEBOT_WORKER_MAX_TICKS=2 SOURCEBOT_WORKER_IDLE_SLEEP_MS=0` lets one explicitly bounded invocation process two queued repository-sync jobs and log bounded runtime completion. | Partial | Add real queue scheduling, retry windows, and continuous/background orchestration so progress does not depend on manual one-shot or bounded-loop invocations. |
| Retry, scheduling, and resume-loop orchestration | `specs/acceptance/worker-runtime.md`, `specs/acceptance/index.md`, and `specs/acceptance/journeys.md` now ground the current one-tick/default plus bounded multi-tick worker baseline truthfully while also naming the bounded automatic stale-lease retry/replay surface and still marking broad retry/scheduling/resume loops as not implemented. | Missing | Implement roadmap Tasks 63–65: real queue orchestration, real execution, durable retries/backoff beyond bounded stale-running lease recovery plus one stale-lease replay, and the later worker/operator acceptance expansions needed to prove this area. |
| Worker/operator observability | The current worker surface includes logs from `crates/worker/src/main.rs`, optional supervisor-readable worker status snapshots that include last work-item id/status plus bounded/redacted terminal error summaries, authenticated `GET /api/v1/auth/worker-status` access to the bounded status snapshot, and authenticated review-webhook/run inspection endpoints owned by the API layer; `specs/acceptance/worker-runtime.md` and `specs/acceptance/operator-runtime.md` now ground the one-tick default plus explicit bounded multi-tick startup/runtime-baseline log, no-work, stub terminal-outcome, bounded completion visibility, redacted failed repository-sync error visibility, and authenticated local status visibility. | Partial | Extend the current worker/runtime acceptance baseline with later operator observability surfaces for queue depth, richer retry/failure state, heartbeat/supervision, metrics/tracing, and health parity from roadmap Phases 8–9. |

### Evidence notes

- The worker surface is no longer purely hypothetical: the repo has a real `sourcebot-worker` binary, a one-tick `run_worker_tick` path, explicit Completed/Failed stub outcomes, oldest-queued claim semantics, repeated-invocation smoke coverage, explicit bounded multi-tick coverage, optional supervisor-readable status snapshots, and no-work idle-path coverage.
- However, the current evidence is still intentionally **stub-oriented**. The worker runs one tick by default or an explicitly configured bounded multi-tick loop, depends on a file-backed organization store path, and treats actual review execution, broad automated retries/backoff, production scheduling/supervision, and background daemonization as explicitly deferred follow-up work, with only bounded stale-running repository-sync lease recovery, one automatic stale-lease retry, and an optional local last-run status snapshot now covered before the next claim.
- The acceptance corpus now has a dedicated `specs/acceptance/worker-runtime.md` home for the current worker baseline, but it still keeps the runtime intentionally narrow: a one-tick default, explicit bounded multi-tick looping, stub outcomes, startup/runtime logging, optional local status snapshots, and no-work exits rather than full worker automation parity.
- Because roadmap Phase 8 still reserves real orchestration, execution, and retry behavior for later tasks, this slice treats most current worker capability areas as **Partial** and the missing orchestration/observability layers as **Missing**.

## Integrations domain

Grounding for this slice comes from the integrations acceptance spec in `specs/acceptance/integrations.md`, the feature inventory rows in `specs/FEATURE_PARITY.md`, the live API router and authenticated/public integration-adjacent endpoints in `crates/api/src/main.rs`, the shared connection/OAuth/review-webhook models in `crates/models/src/lib.rs`, the repo/detail assembly in `crates/core/src/lib.rs`, the sample catalog/auth fixtures in `crates/api/src/storage.rs` and `crates/api/src/auth.rs`, the MCP crate in `crates/mcp/src/lib.rs`, the current frontend repo/detail connection display in `web/src/App.tsx`, and the roadmap follow-up tasks in `docs/plans/2026-04-18-sourcebot-full-parity-roadmap.md`.

| Capability area | Current rewrite evidence | Status | Highest-value next gap(s) |
| --- | --- | --- | --- |
| GitHub-oriented connection and review-webhook baseline | `crates/models/src/lib.rs` defines `ConnectionKind::GitHub`; repo/detail responses carry `connection` metadata; `crates/api/src/main.rs` exposes authenticated review-webhook CRUD/listing plus the public `/api/v1/review-webhooks/{webhook_id}/events` intake route; fixtures in `crates/api/src/auth.rs` exercise a `conn_github`-backed webhook/review-agent flow. | Partial | Finish real GitHub connection auth/configuration and repository enumeration/sync from roadmap Task 56, then complete provider webhook validation, event normalization, idempotency, and operator-visible failure handling from Task 62. |
| GitLab and generic/local Git connection modeling | `crates/models/src/lib.rs` already defines `ConnectionKind::{GitLab, GenericGit, Local}` plus provider-scoped configs; authenticated `/api/v1/auth/connections` CRUD exists in `crates/api/src/main.rs`; the rewrite now also exposes authenticated `POST /api/v1/auth/repositories/import/local` plus catalog import paths that validate one real local Git working tree, surface it through `/api/v1/repos` and `/api/v1/repos/:repoId`, bind it to the importing admin organization, and queue one repository-sync job for the worker baseline; repo/detail and `web/src/App.tsx` surface connection metadata; and `specs/acceptance/generic-local-git.md` records the limited settings-shell local import baseline plus read-only sync-history visibility without over-claiming broader parity. | Partial | Extend the current local-only settings-driven import baseline into generic-host/GitLab repository discovery, durable catalog-backed list/detail parity, and richer sync/index runtime behavior instead of stopping at one authenticated local import plus read-only sync-history visibility. |
| Gitea, Gerrit, Bitbucket, and Azure DevOps providers | `crates/models/src/lib.rs` already defines `ConnectionKind::{Gitea, Gerrit, Bitbucket, AzureDevOps}` plus matching config variants, but the current rewrite still lacks provider-specific auth flows, repository enumeration/import behavior, and sync/runtime evidence for those hosts. | Missing | Implement provider-specific auth, enumeration/import, and sync/runtime flows for roadmap Tasks 58–59 before claiming multi-host parity from shared models alone. |
| OIDC / SSO provider login and external account mapping | `specs/acceptance/integrations.md` expects OIDC/SSO login, but the live auth surface in `crates/api/src/main.rs` is still local bootstrap/login/session management plus OAuth client admin endpoints and a bounded linked-account inventory model; repo-wide code search in this run found no `oidc`, `sso`, or `openid` login/callback implementation paths. | Missing | Land roadmap Task 60/Task 95 follow-ups for identity-provider metadata, login callbacks, external account creation/linking, and account-merge policy before claiming provider-login parity. |
| MCP server retrieval surface | `crates/mcp/src/lib.rs` publishes an MCP manifest, retrieval tool definitions, and `execute_tool_call(...)` support for `list_repos`, `list_tree`, `read_file`, `glob`, and `grep`; `crates/api/src/main.rs` now exposes authenticated HTTP/API MCP manifest, tool-list, and tool-call routes under `/api/v1/mcp/...`, with focused tests proving unauthenticated tool calls fail closed, `list_repos` is constrained to the caller-visible repository set, and retrieval tool-list input schemas advertise closed JSON objects with `additionalProperties: false` to match runtime unknown-field rejection. | Partial | Expand beyond the bounded authenticated HTTP/API bridge into full MCP protocol/session transport interoperability, client integration, richer tool coverage, and broader end-to-end replacement evidence before claiming MCP parity. |
| Public REST API contract and machine-readable responses | `crates/api/src/main.rs` serves versioned `/api/v1/...` routes across config, repo browse/search, auth, OAuth-client, and review-webhook surfaces, while `web/src/App.tsx` consumes typed JSON repo/detail responses that include connection and sync metadata. | Partial | Expand endpoint completeness, integration-specific acceptance evidence, and durable connection/repository operations so the public API satisfies the broader connector/admin/operator contracts promised by the integrations acceptance spec. |

### Evidence notes

- The rewrite already has meaningful integration-adjacent scaffolding: shared connection models, repo/detail responses that surface connection metadata and `sync_state`, GitHub-backed review-webhook ingestion/inspection endpoints, a versioned public REST API, and a standalone MCP crate.
- Even so, the current evidence is still mostly **shared-contract**, **settings-shell**, or **fixture-backed** rather than end-to-end provider parity. The rewrite now has an authenticated connection-management surface plus read-only sync-history visibility, but it still lacks real provider auth/credential exchange, repo-enumeration/import workflows, and broad sync/recovery behavior tied to specific external hosts.
- The gap is especially clear for identity and provider breadth: the current codebase has local auth plus OAuth-client admin records, but no OIDC/SSO login path, and no concrete implementation evidence for Gitea, Gerrit, Bitbucket, or Azure DevOps.
- Because roadmap Phase 7 and Phase 8 still reserve the real provider, identity, and webhook-hardening work for later tasks, this slice keeps all current integrations capability areas at **Partial** or **Missing** rather than over-claiming parity from shared models alone.

## Frontend domain

Grounding for this slice comes from the live frontend implementation in `web/src/App.tsx`, the focused UI tests in `web/src/App.test.tsx`, the browse/ask/auth acceptance specs in `specs/acceptance/browse.md`, `specs/acceptance/ask.md`, and `specs/acceptance/auth.md`, the frontend surface inventory plus journey map in `specs/acceptance/index.md` and `specs/acceptance/journeys.md`, the feature rows in `specs/FEATURE_PARITY.md`, and the later frontend/auth/admin follow-up work scheduled in `docs/plans/2026-04-18-sourcebot-full-parity-roadmap.md`.

| Capability area | Current rewrite evidence | Status | Highest-value next gap(s) |
| --- | --- | --- | --- |
| Repository home/list page and top-level navigation shell | `web/src/App.tsx` now uses a minimal hash router with `#/`, `#/search`, `#/agents`, `#/auth`, and `#/repos/:repoId`; `RepoListPage` stays focused on repository cards plus a dedicated-search call-to-action, while `SearchPage` reuses the current `/api/v1/search` flow with query input, repo filter, result rendering, and direct deep links into repository detail/source from search matches; `AgentsPage` adds a dedicated operator-visible review-agent route baseline on top of the existing authenticated visibility APIs; `AuthPage` now adds first-run onboarding, local login, invite-redemption, session-restoration, and logout baseline behavior; `web/src/App.test.tsx` covers the dedicated search route shell, the focused repository home inventory shell, the agents-route baseline, and the auth-route baseline. | Partial | Add a richer app shell with broader route persistence, shared session-aware navigation states, and frontend acceptance evidence for multi-page parity beyond the current search/repo/agents/auth/settings baseline. |
| Repository detail metadata shell | `RepoDetailPage` renders repo name/id, default branch, sync state, connection name, and connection kind before mounting the browse and commit panels, keeps a contextual back link to `#/search` when a user opens repo detail from a dedicated-search result, preserves that route context across detail-load failures with an in-place retry affordance, and now exposes a branch/tag/revision control that keeps the repo route stable while the nested panels reload around the selected revision. | Partial | Add richer repo metadata/status surfaces, branch/ref discovery affordances, and broader frontend parity coverage beyond the current metadata-plus-recovery-and-revision baseline. |
| Browse/source and code-navigation panels | `BrowsePanel` loads tree/blob data, supports nested directory traversal, opens files, accepts an optional revision from navigation results, preselects a requested file path when repo detail is opened from a search result, renders definitions/references with stale-response suppression and clears stale symbol results after opening a navigation target, keeps repository detail visible while browse failures offer an in-place retry affordance plus an explicit empty-directory state, and now reloads tree/blob state for the selected repo-route revision while preserving canonical file-path deep links. The backend blob/code-navigation contract also reports binary local/revisioned blobs with path/size metadata and `is_binary: true` without attempting text rendering, can resolve no-revision definitions/references from the latest caller-authorized local sync snapshot using the terminal sync revision when the primary browse/code-navigation store has no blob for that visible repository, and now supports bounded top-level TypeScript/JavaScript symbol extraction in addition to Rust. `web/src/App.test.tsx` covers browse success/errors, retry recovery, definitions, references, unsupported capability notices, empty-result fallbacks, revision-route reloads, and search-to-source deep-linking; `crates/search/src/lib.rs` and `crates/api/src/main.rs` cover the new TypeScript/JavaScript extraction and definitions path. | Partial | Add syntax-highlighted rendering, frontend binary/download affordances, richer branch/ref discovery UX, deeper code-navigation UX beyond top-level symbol extraction, and broader acceptance coverage for the browse/code-nav interactions that today remain mostly API-shaped. |
| Commit list/detail/diff UX | `CommitsPanel` renders the latest 20 commits, loads commit detail and diff payloads on selection, shows rename/addition/deletion metadata, falls back when patch text is absent, reloads the visible history when the repo route applies a selected revision, and renders `patch_truncated` diff entries as explicit truncation notices instead of pretending the backend marker is a complete patch; the backend no-revision local-sync commit fallback is anchored to the successful sync job's recorded revision so later unsynced local commits are not exposed by fallback list/detail/diff reads; `web/src/App.test.tsx` covers commit loading, selection, diff rendering, revision-route reloads, no-patch fallback behavior, and truncated patch rendering. | Partial | Add deeper history/navigation affordances and richer large-change handling beyond the current capped-per-file notice. |
| Ask/chat frontend | `web/src/App.tsx` now exposes dedicated `#/ask` and `#/chat` routes: `#/ask` keeps repo-scope submission, provider/model metadata, inline rendered citation links, and hash-restored active-thread continuity, while `#/chat` lists authenticated repo-scoped thread summaries, restores a selected thread from the hash, renders prior messages, continues the selected thread without leaving the chat route, and lets the user rename the selected thread plus switch private/shared visibility through the authenticated lifecycle API; `web/src/App.test.tsx` covers the dedicated route, reopen, stale-thread reset, in-flight invalidation, continuation, and metadata update paths. | Partial | Expand this baseline into delete/archive controls, richer conversation/source-preview presentation, streaming/progress states, and agents-management parity from roadmap Phase 5 without over-claiming those deferred surfaces today. |

### Evidence notes

- The frontend is now more than a placeholder: one React file plus focused Vitest coverage already proves a working repo list, dedicated search route, repo detail shell, file browsing, source viewing, code navigation, and commit inspection flow.
- Even so, the shipped frontend remains **single-shell and API-shaped**. Routing now covers `#/`, `#/ask`, `#/search`, `#/auth`, `#/settings...`, and `#/repos/:repoId`, but the current UI is still assembled in one `App.tsx`, and broader multi-page application-shell/state-restoration parity remains follow-up work.
- The acceptance inventory and journey map explicitly position richer route/page coverage as follow-up work rather than shipped parity, so the current shell should be read as a narrow but real frontend slice rather than a full UI rewrite.
- Because the roadmap still reserves richer browse UX, ask/chat UI, and broader route/page coverage for later tasks, this slice keeps the current frontend capability areas at **Partial** or **Missing** instead of treating panel-level evidence as full product parity.

## Auth/admin domain

Grounding for this slice comes from the auth acceptance spec in `specs/acceptance/auth.md`, the auth/admin and operator/admin inventory plus journeys in `specs/acceptance/index.md` and `specs/acceptance/journeys.md`, the feature rows in `specs/FEATURE_PARITY.md`, the live auth/admin router and handlers in `crates/api/src/main.rs`, the shared auth/org models in `crates/models/src/lib.rs`, the file-backed auth/org state fixtures in `crates/api/src/auth.rs`, the permission helper in `crates/core/src/lib.rs`, the current auth route plus shared auth/admin settings-shell implementation in `web/src/App.tsx`, and the later auth/admin follow-up work scheduled in `docs/plans/2026-04-18-sourcebot-full-parity-roadmap.md`.

| Capability area | Current rewrite evidence | Status | Highest-value next gap(s) |
| --- | --- | --- | --- |
| First-run onboarding, invite redemption, and local session baseline | `crates/api/src/main.rs` exposes `/api/v1/auth/bootstrap`, `/api/v1/auth/invite-redeem`, `/api/v1/auth/login`, `/api/v1/auth/me`, `/api/v1/auth/logout`, and `/api/v1/auth/revoke`; `create_bootstrap_admin(...)` closes bootstrap with `409` after initialization; `redeem_local_invite(...)` consumes a pending non-expired invite id plus invited email, creates or reuses the invited local account, rejects already-credentialed existing accounts, adds any missing membership, marks the invite accepted, and issues a local session; the current Task 87a/87b1 slices now prove that when `DATABASE_URL` is configured the bootstrap-admin record and local sessions persist durably in PostgreSQL, so fresh router processes can keep bootstrap closed, re-authenticate the bootstrap admin, and restore login -> `/api/v1/auth/me` without depending on the bootstrap-state or local-session JSON files; `web/src/App.tsx` still provides the dedicated `#/auth` route that checks bootstrap status, renders either first-run onboarding, invite redemption, or local login, persists the returned local session token client-side, restores `/api/v1/auth/me`, exposes logout plus restored-identity UI, and shows truthful provider-aware OAuth callback-status notices when `provider`/`error`/`error_description`/`code`/`state` land on `#/auth`; `specs/acceptance/auth.md`, `web/src/App.test.tsx`, and `crates/api/src/main.rs` together ground bootstrap, invite redemption, local session auth, and narrow callback-status handling as the current baseline journey. | Partial | Keep the newly durable PostgreSQL-backed bootstrap-admin and local-session slices bounded, then move the remaining bootstrap/account/org aggregates off the current file stores and extend this baseline into broader org/invite/account-management UX plus the real external-provider login/callback exchange from roadmap Tasks 47–50, 51–52, 60, and Phase 1 Task 9. |
| Organization, membership, invite, and role state | `crates/models/src/lib.rs` already defines `Organization`, `OrganizationMembership`, `OrganizationInvite`, and `OrganizationRole`; `OrganizationState` persists `organizations`, `memberships`, `accounts`, and `invites`; `crates/api/src/main.rs` now exposes authenticated `GET /api/v1/auth/members`, which returns only organizations the local-session user administers plus member/invite inventory joined to visible local-account details, a bounded `POST /api/v1/auth/members/invites`, which lets an admin create a pending invite for an administered organization in file state or PostgreSQL auth metadata while preserving file-backed audit visibility and writing a durable PostgreSQL audit event when configured, `DELETE /api/v1/auth/members/invites/{invite_id}`, which cancels pending invites for administered organizations while rejecting accepted/hidden invites without deleting rows, `PATCH /api/v1/auth/members/{user_id}/role`, which updates visible member roles for administered organizations while rejecting hidden member targets, `DELETE /api/v1/auth/members/{user_id}`, which removes a visible active membership for an administered organization without deleting the local account while rejecting self-removal plus last-admin removal/demotion without mutation and preserving file-backed `auth.member.removed` audit visibility, and `POST /api/v1/auth/invite-redeem`, which consumes a pending non-expired invite plus invited email, reuses or creates only the invited passwordless local account, adds the missing membership, marks the invite accepted, and returns a local session; `web/src/App.tsx` renders the current baseline as admin-visible members inventory plus minimal invite creation, pending-invite cancellation, bounded member-role updates, bounded member removal, and a dedicated `#/auth` invite-redemption flow; `crates/api/src/auth.rs` and `crates/api/src/main.rs` include fixture/state coverage for those records. | Partial | Implement broader org/member CRUD, email delivery/resend workflow, richer invite-management UX, durable audit-event storage beyond the shipped file-backed visibility aggregate, broader role lifecycle policy management, and fuller role lifecycle parity from roadmap Tasks 49–50, then expand acceptance coverage beyond the current admin-visible inventory/create/cancel/role-update/member-removal/redeem baseline into richer duplicate-email and role-management evidence. |
| API key lifecycle and adjacent authenticated admin surfaces | `crates/api/src/main.rs` exposes `/api/v1/auth/api-keys` plus `/api/v1/auth/api-keys/{api_key_id}/revoke`; `crates/models/src/lib.rs` persists `ApiKey` records with `secret_hash`, optional `revoked_at`, and optional `repo_scope`; Task 87c adds SQL migration `0009_api_key_oauth_client_metadata`, PostgreSQL-backed API-key inventory/create/revoke storage plus bearer-auth lookup in `crates/api/src/auth.rs` and `crates/api/src/main.rs`, focused route coverage that proves those routes ignore stale organization-state fixtures when `DATABASE_URL` is configured, and the `#/settings/api-keys` panel now fetches current-user keys, shows truthful scope/status details, creates a minimal key with optional newline-delimited repository scope, reveals the returned plaintext secret only in the immediate success area, and can revoke active keys in place. | Partial | Finish richer API-key rotation/bulk-management/advanced-scope UX, add deeper admin acceptance coverage beyond the current minimal inventory/create/revoke flow, and extend the still-file-backed repo-permission/whole-aggregate org durability work that sits behind broader authz parity. |
| Access / permission sync and fail-closed authz baseline | `crates/core/src/lib.rs` computes visible repos from `memberships` + `repo_permissions`; `crates/api/src/main.rs` intersects API-key scopes with live visible repos for search/browse/ask and returns `404` when a repo is outside the caller's visible set; `web/src/App.tsx` now also exposes a read-only `#/settings/access` panel backed by `/api/v1/repos` so authenticated users can inspect the repositories currently visible to their account without claiming shipped permission-sync management UX. | Partial | Implement the durable permission-sync behavior and broader fail-closed enforcement promised by roadmap Task 54 so access changes propagate across every search, browse, ask, review-agent, and admin surface with dedicated acceptance evidence. |
| Connection management and sync/index admin controls | `crates/api/src/main.rs` already exposes authenticated `/api/v1/auth/connections` CRUD and `/api/v1/auth/repository-sync-jobs` history plus bounded admin-only POSTs that queue a repository-sync job for an already visible repository using the catalog-derived connection id and requeue visible failed jobs without overwriting terminal history, plus visible-repository artifact-backed search/index-status, unscoped/search-context search artifact fallback that runs only when the primary startup search response is empty, and snapshot fallback for no-revision `/api/v1/repos/{repo_id}/tree`/`blob` when a latest authorized local sync snapshot exists but the startup browse/search stores have no tree/blob/status; `web/src/App.tsx` includes the limited `#/settings/connections` shell and a repository-detail read-only index-status panel; and `specs/acceptance/generic-local-git.md` plus `specs/acceptance/repository-operations.md` now ground the current baseline. | Partial | Move beyond the limited settings shell, read-only sync/index visibility, backend-only queued-job creation/retry, and one-shot stub worker progress into broader admin/settings navigation, durable per-connection status/import controls, real worker execution, automated retries/backoff/progress, and real provider/runtime parity from roadmap Phase 2 and Task 55. |
| Linked external accounts and SSO/OIDC identity mapping | `crates/models/src/lib.rs` now persists bounded `ExternalAccount` records in `OrganizationState`; migration `0013_external_accounts` adds a PostgreSQL-backed `external_accounts` table; and authenticated `GET /api/v1/auth/linked-accounts` returns the current local account identity, any persisted external provider identities linked to that same local account, and organization memberships visible to that identity while excluding external identities for other local users and failing closed when the local session lacks a real `LocalAccount` record. `web/src/App.tsx` renders that read-only baseline at `#/settings/linked-accounts`; `web/src/App.test.tsx`, `crates/api/src/main.rs`, and the migration inventory test include focused coverage while keeping external provider linking/callback exchange, SSO login, and account-merge workflows explicitly out of scope. | Partial | Implement roadmap Task 95 follow-ups for provider login/callback handling, external-account creation/linking, account-merge policy, and richer settings UX for managing external identities beyond the current same-user read-only inventory baseline. |
| Auth/admin/settings frontend shells | `web/src/App.tsx` now exposes a broader shared settings route family at `#/settings`, `#/settings/connections`, `#/settings/api-keys`, `#/settings/members`, `#/settings/access`, `#/settings/linked-accounts`, `#/settings/oauth-clients`, `#/settings/observability`, and `#/settings/review-automation`; the API-keys subsection has advanced beyond a placeholder into a minimal authenticated inventory/create/revoke panel that appends newly created keys, reveals the returned plaintext secret only in the immediate success area, and hides management controls when the initial inventory load fails; the members subsection now loads an admin-visible organization member/invite inventory from `/api/v1/auth/members` with minimal invite creation, pending-invite cancellation, member-role update, and member-removal controls, the access subsection now loads a read-only visible-repository inventory from `/api/v1/repos`, the linked-accounts subsection now loads the current local identity, same-user external identities, and visible organization memberships from `/api/v1/auth/linked-accounts`, the OAuth-clients subsection now loads admin-visible inventory from `GET /api/v1/auth/oauth-clients` plus a minimal authenticated create form backed by `POST /api/v1/auth/oauth-clients`, reveals the returned plaintext client secret exactly once in a focused success area, and still keeps secret hashes plus persisted plaintext secret material out of the inventory, the observability subsection now loads visible audit-event and analytics inventories from `/api/v1/auth/audit-events` and `/api/v1/auth/analytics` with truthful loading/failure states, and the review-automation subsection now loads visible review-webhook, delivery-attempt, and review-agent-run inventories from `/api/v1/auth/review-webhooks`, `/api/v1/auth/review-webhook-delivery-attempts`, and `/api/v1/auth/review-agent-runs` with truthful endpoint-scoped loading/failure states while keeping invite management, permission-sync management, external provider linking, richer OAuth authorization/token/revocation flows, retry/manage/run-control UX, and broader admin CRUD explicitly out of scope; `web/src/App.test.tsx` covers the landing page, API-key inventory/create/revoke/failure behavior, members inventory behavior, access inventory behavior, linked-accounts behavior, OAuth-client inventory/create behavior, observability behavior, review-automation behavior, and the existing connections route. | Partial | Keep expanding the route family into real user/admin journeys while preserving truthful boundaries; highest-value remaining gaps are invite/member management, richer permission-sync controls, external identity linking, richer API-key/OAuth-client management, audit/analytics filtering/export, and review-automation management/retry controls. |

### Evidence notes

- The rewrite already has real auth/admin scaffolding: bootstrap, invite-redemption, and local-session routes; API key list/create/revoke surfaces; a minimal API-key inventory/create/revoke settings panel; audit and analytics listing endpoints; a minimal review-automation visibility panel; OAuth-client records; shared org/invite/account models; a linked-accounts visibility route; and a permission helper that ties repo visibility to memberships plus repo-permission bindings.
- Even so, the current auth/admin evidence is still only **partial product coverage** rather than end-to-end parity. Org/invite management endpoints remain bounded to admin-visible members inventory, minimal invite creation, pending-invite cancellation, and the invited-user redeem path; there is still no real external-provider login/callback exchange beyond truthful `#/auth` callback-status messaging and read-only same-user external-account inventory, and although bootstrap-admin, local sessions, local accounts/memberships/invites, API keys, OAuth-client metadata, and external-account inventory now have bounded PostgreSQL durability, broader repo-permission and whole-aggregate organization/auth state remain follow-up work; the frontend settings surfaces still stop well short of full creation/scoping/admin workflows.
- The acceptance corpus itself confirms that gap: `specs/acceptance/auth.md` and `specs/acceptance/settings-navigation.md` now anchor the current auth-route and shared-settings baseline, while `specs/acceptance/journeys.md` still calls for richer admin-observability, OAuth-client, and review-automation acceptance docs.
- Because roadmap Phase 6 still reserves broader onboarding UX, org/invite creation and management, linked-account work, API key completion, permission-sync hardening, and settings navigation for later tasks, this slice keeps current auth/admin capability areas at **Partial** or **Missing** instead of over-claiming parity from route presence and shared models alone.

## Ops domain

Grounding for this slice comes from the project README in `README.md`, the local Postgres bootstrap file in `docker-compose.yml`, the live runtime/env contract in `crates/config/src/lib.rs`, the API startup/config/health surface in `crates/api/src/main.rs` plus `crates/api/src/storage.rs`, the worker entrypoint in `crates/worker/src/main.rs`, the acceptance inventory and journey map in `specs/acceptance/index.md` and `specs/acceptance/journeys.md`, the feature rows in `specs/FEATURE_PARITY.md`, and the later operator/runtime work scheduled in `docs/plans/2026-04-18-sourcebot-full-parity-roadmap.md`.

| Capability area | Current rewrite evidence | Status | Highest-value next gap(s) |
| --- | --- | --- | --- |
| Local self-host bootstrap and runtime config baseline | `README.md` and `.env.example` now document a repo-local local-runtime contract built around `SOURCEBOT_DATA_DIR`, while `Makefile` exposes `make api` and `make worker`; `scripts/check_end_to_end_smoke_matrix.sh` now adds one bounded repo-local smoke command that starts the real API against an isolated temp runtime, bootstraps/login-checks a local admin, exercises authenticated connections/search/ask plus public review-webhook intake, runs the real default one-tick worker with a stubbed completed outcome, and verifies the queued run reaches `completed`; `docker-compose.yml` still provisions a local Postgres 16 service with a healthcheck; `crates/config/src/lib.rs` defines env-driven runtime config for bind address, `DATABASE_URL`, shared file-backed state paths, explicit per-file overrides, and optional LLM settings. | Partial | Extend the local runbook beyond API + default one-tick/bounded-loop worker + stub-backed smoke bring-up into full local stack bring-up and later durable metadata/runtime workflows instead of treating the current shared data-dir baseline as production deployment parity. |
| Service liveness, readiness, and public config visibility | `crates/api/src/main.rs` exposes `/healthz`, `/readyz`, and `/api/v1/config`; `/readyz` now reports file-backed metadata readiness when `DATABASE_URL` is unset and fails closed with `503` if configured PostgreSQL metadata cannot expose the SQLx `_sqlx_migrations` inventory; `AppConfig::public_view()` deliberately redacts secret values to `has_database_url` / `has_llm_api_key`; config tests in `crates/config/src/lib.rs` and `crates/api/src/main.rs` prove secret-hiding behavior; `specs/acceptance/operator-runtime.md` now records the shipped local liveness/readiness/config acceptance baseline. | Partial | Keep `/healthz`, `/readyz`, and `/api/v1/config` scoped to the current local baseline while adding production-grade dependency health, upgrade readiness, observability, and deployment automation in later operator slices. |
| Durable metadata DB readiness and migration workflow | `docker-compose.yml` can launch Postgres; `Makefile` plus `scripts/bootstrap_local_metadata_dev.sh` now provide a bounded local `make metadata-dev-bootstrap` workflow that validates the local Postgres URL contract, waits for readiness, ensures the dedicated `*_test` database exists, runs `make sqlx-migrate`, and then runs `make sqlx-test`; `crates/api/src/storage.rs` now routes `DATABASE_URL` through bounded PostgreSQL catalog list/detail reads plus one explicitly requested local Git repository import handoff that transactionally upserts the local connection and repository row, while `crates/api/src/auth.rs` covers durable bootstrap/local-session/local-account/invite/API-key/OAuth-client metadata, repository-sync-job lifecycle, ask-thread/message storage, review-agent-run lifecycle rows, review-webhook delivery-attempt rows, and PostgreSQL-backed audit-event merge/persistence through the `OrganizationState` compatibility envelope. | Partial | Land the remaining durable metadata slices: connection management, recursive/provider repository import, analytics aggregates, repo-permission sync, broader organization aggregates beyond the covered audit-event envelope, richer review-agent orchestration/retries, and upgrade-safe readiness parity beyond the current bootstrap-and-compatibility wrapper. |
| Background worker invocation and automation control plane | `crates/worker/src/main.rs` loads env config, builds the organization-state store from `config.organization_state_path`, logs the resolved one-tick/default plus bounded-loop runtime baseline and selected stub outcomes, logs repository-sync terminal job identifiers and failure reasons when a sync job is processed, runs a timeout-bounded local-Git working-tree preflight plus bounded HEAD/content-discovery/current-branch probes that persist terminal revision, branch, and tracked-content file-count metadata, write a bounded repo-local tracked-content manifest, materialize a sibling snapshot tree from tracked `HEAD` file bytes, and persist a sibling `search-index.json` artifact that can be loaded without rewalking the snapshot before completing queued jobs tied to configured `local` connections; Generic Git repository-sync jobs reject option-like and control-character-bearing base URLs before spawning Git, require HEAD-symref branch/revision metadata to be advertised by `--heads`, and fail closed on unadvertised HEAD symrefs plus malformed/unexpected non-`HEAD` HEAD metadata and kill hung children once retained stdout/stderr or combined stdout+stderr exceeds the bounded output cap, in addition to the existing blank/credential-bearing/malformed/oversized guards; the API can also load requested visible-repository search from the latest authorized local sync `search-index.json` artifact when present, derive bounded visible-repository no-revision tree/blob reads from the latest authorized local snapshot and derive index-status counts from the latest authorized local sync `search-index.json` artifact when present (falling back to pre-artifact snapshot traversal) when the startup browse/search stores have no tree/blob/status, and settings sync-history rows compute those local manifest/snapshot artifact paths for operators, while the worker still runs one tick by default or an explicitly configured bounded multi-tick loop; the shipped local baseline now lets operators start that default one-tick/bounded-loop worker through `make worker` with the same `.env`/`SOURCEBOT_DATA_DIR` contract used by `make api`, while `specs/acceptance/worker-runtime.md` records the current baseline truthfully. | Partial | Add real fetch/import/reindex execution beyond bounded local content discovery/manifest/snapshot/search-index artifact status, continuous scheduling, retries, supervision, and operator-visible runtime controls instead of over-claiming the current one-tick/default plus bounded-loop local-metadata worker bring-up as a full control plane. |
| Operator observability and admin-visible runtime surfaces | `crates/api/src/main.rs` already exposes authenticated audit, analytics, review-webhook, delivery-attempt, and review-agent-run visibility endpoints; repo and UI surfaces already expose repository `sync_state`; API and worker binaries both initialize `tracing_subscriber` logging. | Partial | Keep the current scope limited to logs plus authenticated JSON visibility and add production-grade observability, richer runtime status, and dedicated admin/operator observability specs later. |
| Upgrade-safe deployment, recovery, and runbook parity | `README.md` now documents the local operator maintenance baseline; `scripts/backup_local_runtime_state.sh` plus `make runtime-backup` capture the current file-backed runtime state; `scripts/restore_local_runtime_state.sh` plus `make runtime-restore BACKUP_DIR=...` now validate the captured manifest's resolved runtime paths against the current `SOURCEBOT_DATA_DIR`/explicit state-file overrides before copying files, so a backup from a different local runtime root fails closed; `scripts/backup_local_metadata_db.sh` plus `make metadata-backup` now capture a local Postgres SQL dump with a redacted manifest; `scripts/restore_local_metadata_db.sh` plus `make metadata-restore BACKUP_DIR=...` replay that local metadata dump; together they form a local Postgres metadata dump/restore baseline around the current SQLx migration workflow plus repo-update/API-worker restart sequence. This slice still does not yet mean every runtime surface is durable or that production deployment guarantees exist. | Partial | Extend the current local maintenance runbook from manifest-validated file-backed/runtime plus local metadata recovery into readiness checks and production-grade deployment/recovery workflows before claiming broader operator parity. |

### Evidence notes

- The rewrite already has a real **operator-facing local baseline**: env-driven config, a shared `SOURCEBOT_DATA_DIR` runtime-path contract with explicit per-file overrides, `/healthz`, bounded `/readyz` metadata readiness, `/api/v1/config`, authenticated audit/analytics/review-runtime visibility endpoints, repo-local `make api` plus `make worker` bring-up commands, and one bounded stub-backed smoke command that crosses auth, integrations, search, ask, and review-agent surfaces in a single local run.
- Even so, the current ops evidence is still **bootstrap-oriented and local-dev focused**. The new `make metadata-dev-bootstrap` helper only validates the local metadata schema bootstrap contract plus the focused compatibility test flow; even with bounded PostgreSQL durability now extended through catalog list/detail reads, one explicit local Git import handoff plus queued worker handoff, bootstrap/local-session/local-account/invite/API-key/OAuth-client metadata, repository-sync-job, ask-thread/message, review-agent-run lifecycle, review-webhook delivery-attempt rows, and audit-event merge/persistence through the `OrganizationState` compatibility envelope, connection management, recursive/provider repository import, analytics aggregates, repo-permission sync, broader whole-aggregate runtime durability, and richer review-agent orchestration/retry behavior are still unfinished, `/healthz` is not a readiness contract, and worker orchestration remains an explicit local one-tick/default or bounded-loop path rather than supervised or continuously scheduled.
- The acceptance corpus now includes `specs/acceptance/operator-runtime.md` for the shipped local runtime baseline and `specs/acceptance/operator-maintenance.md` for the current file-backed backup/restore plus local metadata dump/restore and SQLx migration workflow baseline, but worker runtime, richer observability, durable metadata across all runtime surfaces, and broader operator workflows remain explicitly incomplete.
- Because roadmap Phase 1, Phase 2, Phase 8, Phase 9, and Phase 11 still reserve migrations, durable stores, sync/index operations, retry/scheduling, supervised runtimes, and production-grade deployment/observability/runbook work for later tasks, this slice keeps ops capability areas at **Partial** or **Missing** instead of over-claiming parity from the new local baseline.

## Domain coverage status

Task `03` now has canonical gap-report coverage for backend/API, worker, integrations, frontend, auth/admin, and ops. Later roadmap slices should update the evidence inside these existing domain sections rather than create a second parity gap report.
