# Final parity audit and release checklist

## Purpose

This document now closes the Task 100 release-candidate re-audit by mapping every
current `specs/FEATURE_PARITY.md` row to shipped rewrite evidence, the
release-candidate smoke result, and the blockers that still prevent an honest
"practical Sourcebot replacement" release claim. It supersedes the original Task
85 audit wording without upgrading any row beyond the repo-local evidence.

## Scope and evidence policy

- This audit is grounded only in repo-local evidence already shipped in code, tests,
  acceptance specs, and the canonical gap report.
- A row is **Partial** when the rewrite has a real bounded baseline but still has
  explicit deferred gaps.
- A row is **Missing** when the repo has no truthful end-to-end baseline for that
  feature beyond shared models, placeholder routes, or future-facing docs.
- This audit does **not** upgrade any row to `Complete` unless the current repo
  evidence actually closes the practical parity contract.

## Release verdict

**Release verdict: NOT READY for a truthful practical Sourcebot replacement claim.**

The Task 100 release-candidate smoke contract passed (`bash
scripts/check_end_to_end_smoke_matrix_contract.sh /opt/data/projects/sourcebot-rewrite`),
which proves one bounded repo-local path across local bootstrap/login,
authenticated connections, search, ask, review-webhook intake, repository-sync
queue visibility, and a default one-tick worker completion. That smoke is useful
operator evidence, but it is not a release certificate: the live repo still has
explicit blockers in durable metadata breadth, real provider integrations,
end-to-end OIDC/SSO, production search/indexing depth, richer thread and admin
lifecycles, worker scheduling/retries/supervision, MCP protocol/client
interoperability, and production-grade operator/runtime parity.

## Audit summary by domain

### Core user features

| Feature | Status | Code / tests / docs evidence | Remaining blockers |
| --- | --- | --- | --- |
| Cross-repo and cross-branch code search | Partial | Code: `/api/v1/search` in `crates/api/src/main.rs` plus `#/search` in `web/src/App.tsx`; the API returns bounded `limit`/`offset` pagination metadata with visibility-scoped `total_count`/`has_more` for the current result page, fails closed for hidden/unknown/blank present `repo_id` filters instead of broadening search, exposes explicit `mode=boolean|literal|regex` matching across startup and local-sync artifact-backed paths, fails closed for empty/whitespace-only regex queries instead of matching every indexed line, the startup/local-sync search store now matches boolean query text case-insensitively, treats unquoted space-separated query words as all-term line matches within bounded boolean groups, preserves quoted phrases as literal line substrings, applies a bounded unquoted `OR` separator between line-term groups, fails closed for oversized boolean queries before allocating unbounded OR groups, fails closed for oversized literal-mode queries before allocating/lowercasing unbounded query strings, fails closed for persisted local-sync search-index artifacts whose stored result line content contains NUL/binary payloads or exceeds the runtime file-size budget and whose stored result paths exceed the runtime path budget, refuses to write serialized local-sync search-index artifacts above the loader's bounded size cap, applies minimal unquoted `lang:` filters plus substring and glob-style `path:` result-path filters, and returns deterministic repo/path/line ordering for identical indexed content and primary-index or local-sync fallback API results before pagination, falls back to caller-authorized local-sync artifact matches for visible repositories that are absent from the primary startup index only when the primary startup search response is empty, treats primary startup index `error` status as unavailable so requested-repo search can still use the latest authorized local-sync artifact, skips `.sourcebot` runtime metadata under the repository root so local-sync artifacts are not indexed as source content, rejects persisted local-sync search-index artifacts that try to reintroduce runtime/generated/binary paths skipped by the live indexer, including skipped extensions regardless of casing, requires persisted local-sync search-index artifact schema versions to be present and supported, rejects symlinked artifact parents before creating nested writer directories through them, and the frontend now renders bounded result-count plus previous/next page controls and a boolean/literal/regex mode selector that request the corresponding API offsets and mode while preserving deep-link context; tests/docs: `local_search_store_matches_queries_case_insensitively`, `local_search_store_matches_all_space_separated_query_terms`, `local_search_store_matches_quoted_phrases_and_unquoted_terms`, `local_search_store_matches_bounded_boolean_or_groups`, `local_search_store_rejects_persisted_index_artifact_binary_line_content`, `local_search_store_supports_explicit_literal_and_regex_modes`, `local_search_store_applies_lang_and_path_filters_without_line_term_broadening`, `local_search_store_returns_deterministic_repo_path_line_order`, `search_supports_explicit_literal_and_regex_modes`, `search_matches_all_space_separated_terms_without_exact_phrase`, `search_trims_query_before_searching_and_reporting_response`, `search_returns_paginated_results_with_total_count_and_has_more`, `search_rejects_blank_present_repo_id_without_broadening_results`, `specs/acceptance/search.md`, `web/src/App.test.tsx`, `docs/reports/2026-04-18-parity-gap-report.md` (backend/API + frontend search rows). | Still lacks a real indexing pipeline, richer branch/index availability handling, richer filter grammar beyond bounded literal/regex/phrase/path/language/OR/order components, streaming search, and stronger stable-pagination/relevance guarantees across changing indexes. |
| Regex, literal, boolean, repo/language/path filters | Partial | Code/docs: `specs/acceptance/search.md` grounds regex/literal/boolean and repo/language/path filter expectations on top of `/api/v1/search`; the API now accepts explicit `mode=boolean|literal|regex`, the frontend exposes those modes on `#/search` without changing the default boolean URL/API shape, the startup/local-sync search store has a bounded literal and regex matching baseline that fails closed for empty/whitespace-only regex queries, covered by `local_search_store_supports_explicit_literal_and_regex_modes` and `search_supports_explicit_literal_and_regex_modes`, a bounded unquoted `lang:` filter plus substring/glob-style `path:` result-path filter baseline covered by `local_search_store_applies_lang_and_path_filters_without_line_term_broadening` and `local_search_store_applies_path_glob_filters_to_result_paths`, and a bounded unquoted `OR` line-term-group separator covered by `local_search_store_matches_bounded_boolean_or_groups`. | Filter grammar and validation are not yet fully proven to upstream parity beyond the bounded path-glob baseline, regex support remains a bounded line-level baseline rather than full indexed regex semantics, boolean support is limited to this bounded `OR` baseline, and real index-backed semantics remain open. |
| File explorer with tree browsing and syntax highlighting | Partial | Code: repo detail browse shell in `web/src/App.tsx`, tree/blob APIs in `crates/api/src/main.rs`; docs/tests: `specs/acceptance/browse.md`, `web/src/App.test.tsx`, gap-report browse/frontend rows. | Nested browse works, NUL-byte tree/blob paths are rejected before revisioned Git lookup, local and revision-backed blob content reads are bounded and fail closed instead of returning oversized source payloads, revision-backed browse/blob Git stdout/stderr capture is bounded and fails closed instead of returning partial revision data, local tree/blob reads and glob results fail closed for symlinks resolving outside the repository root, local-sync snapshot fallback rejects a symlinked snapshot root before tree/blob reads, revisioned Git symlink blobs fail closed instead of returning symlink target payloads as file contents, blob-as-tree revision browse requests fail closed as not found instead of surfacing backend Git errors, revision tree reads preserve embedded-newline Git paths through NUL-delimited parsing, and the backend now reports non-UTF-8 local/revisioned blobs as binary metadata instead of broken text, but production-grade syntax highlighting, frontend binary/download affordances, richer large-file UX, and broader browse UX parity remain deferred. |
| Code navigation language coverage | Partial | Code/tests: `extract_symbols_returns_top_level_rust_definitions`, `extract_symbols_returns_top_level_typescript_and_javascript_definitions`, and `definitions_returns_supported_typescript_definitions` now prove bounded top-level Rust plus TypeScript/JavaScript symbol extraction for definitions and Python/Go text-reference scanning for references. | Still lacks tree-sitter-backed multi-language precision, nested/member symbol indexing, cross-file reference indexing, branch-aware symbol indexes, and stale/degraded index status. |
| Repository page and repo list page | Partial | Code: `RepoListPage` and `RepoDetailPage` in `web/src/App.tsx`, `/api/v1/repos`, `/api/v1/repos/{repo_id}`, and `/api/v1/repos/{repo_id}/index-status` in `crates/api/src/main.rs`; the repository detail UI now shows a read-only index-status panel with indexed/error state, indexed file count, indexed line count, skipped file count, and optional index error text; docs/tests: `specs/acceptance/browse.md`, `specs/acceptance/repository-operations.md`, `web/src/App.test.tsx`. | Current shell is useful but still shallow versus full multi-page parity, richer repo metadata, and durable catalog-backed parity. |
| File source view | Partial | Code: blob retrieval plus repo detail browse/source rendering in `crates/api/src/main.rs` and `web/src/App.tsx`; docs/tests: `specs/acceptance/browse.md`, `web/src/App.test.tsx`. | Safe source rendering exists, backend blob retrieval distinguishes binary blobs with `is_binary`, local/revisioned blob reads fail closed above the bounded source payload budget, and the frontend now shows binary metadata without text rendering or code-navigation controls, but syntax-highlighting depth, binary download affordances, richer large-file UX, and broader source-view polish remain open. |
| Commit list, commit detail, and diff view | Partial | Code: commit routes in `crates/api/src/main.rs`, `LocalCommitStore` in `crates/api/src/commits.rs`, `CommitsPanel` in `web/src/App.tsx`; docs/tests: `specs/acceptance/browse.md`, focused backend tests for offset pagination metadata and large per-file diff-patch capping, `web/src/App.test.tsx`, gap-report browse rows. | Backend commit history now gates authenticated seeded commit list/detail/diff reads through repository visibility before serving primary commit data, supports bounded `limit`/`offset` pagination metadata (`page_info.has_next_page`/`next_offset`), and per-file commit-diff patch text treats Git pathspec metacharacters literally before loading and is capped at 64 KiB with `patch_truncated` status while oversized Git stdout/stderr capture and oversized changed-file metadata now fail closed before truncated commit/diff metadata or per-file patch loads are parsed, while commit-list revision selectors plus commit detail/diff selectors are rejected when empty or control-character-bearing before Git invocation, and explicit commit-list revision selectors outside the successful local-sync snapshot are rejected instead of exposing newer unsynced local history; when the primary commit store has no rows for a visible repository, no-revision local-sync fallback list/detail/diff reads are anchored to the successful job's recorded `synced_revision` so later unsynced local commits are not exposed; the frontend now renders truncation status as an explicit notice instead of displaying the backend marker as a complete patch, but broader commit-page UX and richer large-change affordances remain open. |
| Code navigation: definitions and references | Partial | Code: definitions/references endpoints in `crates/api/src/main.rs`; UI: browse panel symbol navigation in `web/src/App.tsx`; docs/tests: `specs/acceptance/code-nav.md`, `web/src/App.test.tsx`. | Works as a bounded baseline, no-revision requests can now fall back to the latest caller-authorized local sync snapshot with terminal sync revision links when the primary browse/code-navigation store has no blob, blank present revision values are rejected before default/snapshot fallback, and the frontend clears stale symbol results/input after opening a navigation target; deeper multi-language correctness, stale-index status, real provider fetch/import/reindex, durable/background indexing, and richer UX parity remain deferred. |
| Ask the codebase with inline citations | Partial | Code: `/api/v1/ask/completions` and citation filtering in `crates/api/src/main.rs`; UI: `#/ask` route in `web/src/App.tsx`; docs/tests: `specs/acceptance/ask.md`, `web/src/App.test.tsx`, backend/API ask row in the gap report. | Repo-scoped ask baseline exists, but richer retrieval/model/runtime behavior and broader ask UX parity remain open. |
| Chat threads, history, rename, visibility, delete | Partial | Code: `/api/v1/ask/threads` plus `GET`/`PATCH`/`DELETE /api/v1/ask/threads/{thread_id}` in `crates/api/src/main.rs`; UI: `#/chat` in `web/src/App.tsx`; docs/tests: `specs/acceptance/ask.md`, `web/src/App.test.tsx`. | Thread list/detail/reopen, backend title/private-shared visibility updates, focused frontend rename/visibility controls, and a bounded caller-owned visible-thread delete lifecycle now exist, but archive controls, richer conversation/source-preview UX, streaming/progress states, and full chat parity remain open. |
| Search contexts / saved scopes | Partial | Code/tests: `/api/v1/auth/search-contexts` supports create/list/delete and `/api/v1/search?context_id=...` applies caller-owned saved scopes in `crates/api/src/main.rs`; the bounded `#/search` UI in `web/src/App.tsx` now lists caller-owned contexts, saves a context from the current repository filter, selects one for search, deletes the selected context, and preserves blank-context fail-closed semantics; docs: `specs/acceptance/index.md` and `specs/acceptance/search.md`; gap report API-key/admin row. | Backend saved-context lifecycle, bounded frontend management, and fail-closed search narrowing exist, but SQL-backed context durability, richer grammar, relevance tuning, and stable pagination parity remain open. |

### Admin and org features

| Feature | Status | Code / tests / docs evidence | Remaining blockers |
| --- | --- | --- | --- |
| First-run onboarding | Partial | Code: `/api/v1/auth/bootstrap` in `crates/api/src/main.rs`; UI: `#/auth` onboarding/login flow in `web/src/App.tsx`; docs/tests: `specs/acceptance/auth.md`, `web/src/App.test.tsx`, auth/admin gap-report baseline row. | Works only as a local baseline; durable metadata, richer onboarding UX, and broader account-management parity remain open. |
| Organizations, membership, invites, roles | Partial | Code/models: org/member/invite/role state in `crates/models/src/lib.rs` and auth handlers in `crates/api/src/main.rs`; UI/docs/tests: `#/settings/members`, minimal admin invite-create/cancel/member-role update/member-removal, invite-redeem flow, `specs/acceptance/auth.md`, auth/admin gap-report org row. | Admin-visible inventory, bounded pending-invite creation/cancellation, bounded member-role updates, bounded member removal without deleting local accounts, durable PostgreSQL invite-create/cancel/member-role/member-removal storage, fail-closed self-removal and last-admin removal/demotion policy gates, durable PostgreSQL audit-event rows for those local member lifecycle mutations, file-backed audit visibility fallback, and invite redemption exist, but email delivery/resend, broader CRUD/admin management, broad audit analytics/filtering/export, external-provider identity audit events, retention/export policy, and fuller role lifecycle parity are still missing. |
| API keys | Partial | Code: `/api/v1/auth/api-keys` plus revoke in `crates/api/src/main.rs`; UI: `#/settings/api-keys` in `web/src/App.tsx`; docs/tests: `specs/acceptance/auth.md`, `specs/acceptance/settings-navigation.md`, `web/src/App.test.tsx`. | Minimal inventory/create/revoke baseline exists with one-time plaintext secret reveal and basic newline-delimited scope entry, but richer rotation, bulk management, advanced scoping UX, and full credential-management lifecycle parity are still incomplete. |
| Connection management | Partial | Code: authenticated connection CRUD plus bounded admin-only repository-sync enqueue/retry with active duplicate suppression in `crates/api/src/main.rs`; UI: `#/settings/connections` in `web/src/App.tsx`; docs/tests: `specs/acceptance/integrations.md`, `specs/acceptance/generic-local-git.md`, `web/src/App.test.tsx`, and focused repository-sync route tests. | Current shell and backend enqueue/retry baseline are still limited and do not yet prove provider-specific auth, discovery, real sync execution, automated retry/backoff, or durable catalog/runtime parity. |
| Sync state and indexing status | Partial | Code: repo `sync_state`, `GET/POST /api/v1/auth/repository-sync-jobs`, bounded `POST /api/v1/auth/repository-sync-jobs/{job_id}/retry`, repository-sync terminal worker logs with job/repo/connection identifiers plus failure reasons, stale/malformed running repository-sync lease recovery before the next claim plus exactly-one automatic replacement for old recovered lease failures, incomplete Generic Git HEAD symrefs without paired revisions failing closed before `--heads` fallback, explicit bounded multi-tick processing for multiple queued jobs in one invocation with oversized tick counts above 1,000,000 and idle sleeps above one hour failing closed before execution, optional supervisor-readable worker status snapshots exposed through authenticated `GET /api/v1/auth/worker-status` with last tick/outcome plus last work-item id/status and bounded/redacted terminal error summary, and a timeout-bounded local-Git preflight plus combined-output-capped revision/content-discovery/current-branch/tracked-file probes that validate HEAD revisions before persisting terminal revision, branch, and tracked-content file-count metadata, write a bounded tracked-content manifest, materialize a sibling snapshot tree from tracked `HEAD` file bytes, persist a sibling `search-index.json` artifact that can be loaded without rewalking the snapshot before completing jobs tied to configured `local` connections while rejecting malformed current-branch metadata and tracked content paths with manifest-delimiting/control characters before line-oriented manifest/snapshot/search-index persistence and bounding oversized local Git failure details before surfacing operator-visible errors, and let requested visible-repo search, unscoped/search-context search across visible repositories, and index-status load the persisted local sync `search-index.json` artifact when present, with unscoped/context search falling back only when the primary startup search response is empty rather than merging local-sync artifacts into non-empty primary results (and with present malformed regular-file or non-regular artifacts failing closed for search instead of falling back to mutable snapshot or partial unscoped success, malformed/non-regular artifacts reported as `error` index-status plus a stable redacted `malformed local-sync search index artifact` parse/load reason, and search-index artifact writes rejecting symlinked temp artifacts without clobbering external targets), and let no-revision tree/blob reads fall back to the latest successful local sync snapshot under the caller-authorized `(organization_id, repository_id)` boundary when the startup search/browse stores have no rows/tree/blob/status for that repo; UI/docs/tests: `specs/acceptance/repository-operations.md`, `specs/acceptance/worker-runtime.md`, `web/src/App.tsx` (including repository-detail index-status counts and local manifest/snapshot artifact paths in sync history), gap-report repository-operations rows, and focused local-sync artifact-backed search/index-status plus snapshot browse/code-navigation regressions in `crates/api/src/main.rs`. | Coarse sync visibility, backend-only queued-job creation/retry, frontend read-only index counts, richer worker terminal logs, default one-tick worker progress plus explicitly bounded multi-tick coverage, optional local status snapshots, stale-running lease recovery, local-Git working-tree preflight plus persisted HEAD/content-discovery/current-branch status/manifest/snapshot handling, and bounded requested-repo artifact-backed search/index-status plus snapshot tree/blob/code-navigation fallback exists, but truthful persisted/background index-status, real provider fetch/import/reindex worker execution, automated retry/backoff/progress beyond bounded manual retry plus exactly-one lease-recovery replay, and durable repository-operations parity remain blocked. |
| Linked external accounts | Partial | Code/models: `ExternalAccount` and `OrganizationState.external_accounts` in `crates/models/src/lib.rs`, migration `0013_external_accounts`, PostgreSQL auth metadata loading in `crates/api/src/auth.rs`, and `/api/v1/auth/linked-accounts` in `crates/api/src/main.rs`; UI/docs/tests: `#/settings/linked-accounts`, `specs/acceptance/auth.md`, `specs/acceptance/settings-navigation.md`, `web/src/App.test.tsx`, focused route tests, and the migration inventory test. | Bounded same-user external-account inventory now exists when records are already persisted, but real provider login/callback exchange, external-account creation/linking, account-merge policy, and broader management UX remain missing. |
| Access / permission sync | Partial | Code: visibility enforcement in `crates/core/src/lib.rs` and protected API handlers in `crates/api/src/main.rs`; UI/docs: `#/settings/access`, `specs/acceptance/auth.md`, auth/admin gap-report access row. | Read-only visibility and fail-closed checks exist, but durable permission-sync lifecycle parity remains open. |

### Integrations

| Feature | Status | Code / tests / docs evidence | Remaining blockers |
| --- | --- | --- | --- |
| GitHub | Partial | Code/models: `ConnectionKind::GitHub`, review-webhook surfaces in `crates/api/src/main.rs`, fixture-backed GitHub webhook/review-agent flows; docs: `specs/acceptance/integrations.md`, integrations gap-report GitHub row. | Real GitHub auth/configuration, repo enumeration/import, and hardened runtime behavior remain open. |
| GitLab | Partial | Code/models: `ConnectionKind::GitLab` and shared connection CRUD; docs: `specs/acceptance/integrations.md`, integrations gap-report GitLab/generic-local row. | Provider-specific GitLab auth, discovery, and runtime parity are still missing. |
| Gitea | Missing | Evidence is limited to shared `ConnectionKind::Gitea` modeling in `crates/models/src/lib.rs`; integrations gap report marks provider-specific behavior absent. | No real auth, enumeration/import, or runtime parity. |
| Gerrit | Missing | Evidence is limited to shared `ConnectionKind::Gerrit` modeling in `crates/models/src/lib.rs`; integrations gap report marks provider-specific behavior absent. | No real auth, enumeration/import, or runtime parity. |
| Bitbucket | Missing | Evidence is limited to shared `ConnectionKind::Bitbucket` modeling in `crates/models/src/lib.rs`; integrations gap report marks provider-specific behavior absent. | No real auth, enumeration/import, or runtime parity. |
| Azure DevOps | Missing | Evidence is limited to shared `ConnectionKind::AzureDevOps` modeling in `crates/models/src/lib.rs`; integrations gap report marks provider-specific behavior absent. | No real auth, enumeration/import, or runtime parity. |
| Generic Git host / local Git | Partial | Code: generic/local connection CRUD, a local import baseline with PostgreSQL catalog handoff, admin-organization visibility binding, and queued repository-sync handoff for one explicitly requested local Git working tree, bounded admin-only repository-sync enqueue/retry with active duplicate suppression in `crates/api/src/main.rs`, repo detail connection metadata in `crates/core/src/lib.rs`, timeout-bounded worker-side blank-config guards, local-Git preflight plus combined-output-capped persisted revision/content-count/current-branch/tracked-file probes, bounded tracked-content manifests, sibling tracked-`HEAD` snapshots, persisted sibling search-index artifacts for queued jobs tied to configured `local` connections, fail-closed rejection of malformed current-branch metadata and tracked content paths with manifest-delimiting/control characters before artifact persistence, Generic Git remote HEAD metadata fail-closed handling for unsupported schemes or transport prefixes, option-like, non-`file://` empty-authority, control-character-bearing, and credential/userinfo-bearing HTTP(S), SSH-URL, or scp-like base URLs before spawning Git, non-interactive Git authentication environment overrides before remote probes, malformed symref advertisements, unexpected non-`HEAD` records in the HEAD metadata probe, malformed direct HEAD revision advertisements, ambiguous duplicate HEAD revision advertisements, and HEAD symref branch/revision values that are not also advertised by `--heads`, plus bounded failure when retained stdout/stderr or combined stdout+stderr exceeds the output cap, stale/malformed running repository-sync lease recovery before the next claim plus exactly-one automatic lease-recovery replay, and requested visible-repo search/index-status/tree/blob/code-navigation plus unscoped/search-context search fallback and commit list/detail/diff fallback over the latest successful local sync artifact/snapshot or local Git working tree under the caller-authorized `(organization_id, repository_id)` boundary, failing closed when duplicate authorized organization bindings make a shared repository id ambiguous; UI/docs/tests: `#/settings/connections` (including local manifest/snapshot artifact paths in sync history), `specs/acceptance/generic-local-git.md`, `web/src/App.test.tsx`, focused PostgreSQL catalog import, focused repository-sync/worker tests including explicit bounded multi-tick processing, and the local-sync artifact-backed requested-repo search/index-status, unscoped/context search, and snapshot search/browse/code-navigation regressions. | One bounded local-import/settings/backend-enqueue/retry/local-content-discovery manifest/snapshot/metadata/search baseline exists, but generic-host discovery, recursive import, real provider fetch/import/reindex execution, automated retries/backoff beyond bounded manual retry plus exactly-one lease-recovery replay, supervised runtime status beyond the optional local status snapshot, durable/background indexing, and full durable provider/runtime parity remain open. |
| OIDC / SSO providers | Missing | Docs: `specs/acceptance/integrations.md`, `specs/FEATURE_PARITY.md`, and integrations gap report all explicitly ground provider login as missing; `#/auth` only shows truthful callback-status notices, while `GET /api/v1/auth/linked-accounts` and `#/settings/linked-accounts` now expose bounded same-user external-account inventory when records already exist. | No real external-provider login/callback exchange, external-account creation/linking flow, or account-merge policy. |
| MCP server | Partial | Code: `crates/mcp/src/lib.rs` ships the manifest and repository-aware retrieval tool definitions plus execution support; `crates/api/src/main.rs` now exposes authenticated HTTP/API MCP manifest, tool-list, and tool-call routes under `/api/v1/mcp/...`, plus the authenticated JSON-RPC `/api/v1/mcp` bridge with focused tests for authentication, JSON-RPC protocol errors including non-object MCP method `params`, batch/notification behavior, permission-scoped `list_repos`, and closed retrieval tool-list input schemas that advertise `additionalProperties: false` to match runtime unknown-field rejection; docs: `specs/acceptance/integrations.md`, `specs/FEATURE_PARITY.md`, integrations gap report. | Full MCP protocol/session transport interoperability, client integration, richer tool coverage, and broader end-to-end replacement evidence remain open. |
| Public REST API | Partial | Code: versioned `/api/v1/...` routes across config, repos, search, auth, ask, OAuth clients, and review webhooks in `crates/api/src/main.rs`; docs: `specs/acceptance/integrations.md`, integrations gap report. | Many route families are still bounded baselines rather than full connector/admin/operator parity. |

### Later-phase advanced features

| Feature | Status | Code / tests / docs evidence | Remaining blockers |
| --- | --- | --- | --- |
| Audit logs | Partial | Code: `/api/v1/auth/audit-events` in `crates/api/src/main.rs`; UI: `#/settings/observability` in `web/src/App.tsx`; docs/tests: `specs/acceptance/auth.md`, `specs/acceptance/settings-navigation.md`, auth/admin + ops gap-report rows. | Read-only visibility exists; local member invite create/cancel, member-role update, and member-removal audit events persist as PostgreSQL rows when `DATABASE_URL` is configured; and the `OrganizationState` compatibility envelope now merges/persists audit events through PostgreSQL instead of leaving audit rows stranded in the fallback file. File-backed visibility remains fallback; richer filtering/export, broad audit analytics, external-provider identity events, retention/export policy, and operational parity are not closed. |
| Analytics | Partial | Code: `/api/v1/auth/analytics` in `crates/api/src/main.rs`; UI: `#/settings/observability`; docs/tests: `specs/acceptance/auth.md`, `specs/acceptance/settings-navigation.md`, auth/admin + ops gap-report rows. | Read-only visibility exists, but analytics depth and operator workflows remain incomplete. |
| OAuth client / token flows | Partial | Code: `/api/v1/auth/oauth-clients` inventory/create in `crates/api/src/main.rs`; UI: `#/settings/oauth-clients` in `web/src/App.tsx`; docs/tests: `specs/acceptance/auth.md`, `specs/acceptance/settings-navigation.md`, auth/admin gap-report rows. | Client inventory/create baseline exists, but authorization, token, revocation, and broader manage UX remain missing. |
| Review agent / webhook automation | Partial | Code: authenticated review-webhook, delivery-attempt, and review-agent-run APIs plus public webhook intake in `crates/api/src/main.rs`; UI: `#/settings/review-automation` and `#/agents` in `web/src/App.tsx`; tests/docs: `scripts/check_end_to_end_smoke_matrix.sh`, `specs/acceptance/ask.md`, `specs/acceptance/auth.md`, operator-runtime + gap-report rows. | Visibility and bounded smoke coverage exist, but durable orchestration, retries, scheduling, and richer operator controls remain open. |
| Enterprise entitlement controls | Missing | Present evidence is limited to roadmap and parity-matrix missing-status entries; the canonical gap report does not claim a shipped entitlement-control surface. | No truthful entitlement implementation or acceptance baseline exists yet. |

## Full-parity release checklist

- [x] `specs/FEATURE_PARITY.md` now has audited row statuses and evidence pointers instead of `Needs audit` / `_TBD_` placeholders.
- [x] This audit maps every current matrix row to code/tests/docs evidence plus remaining blockers.
- [x] Task 100 release-candidate smoke contract passed locally: `bash scripts/check_end_to_end_smoke_matrix_contract.sh /opt/data/projects/sourcebot-rewrite`.
- [x] Remaining blockers were carried forward into `docs/plans/2026-04-28-sourcebot-practical-replacement-blockers-roadmap.md` instead of pretending Task 86-100 completion is release parity.
- [ ] Durable metadata and migration parity are complete across catalog, auth, org, ask-thread, and review-agent state.
- [ ] Search/indexing parity is complete beyond the current bounded API-and-UI baseline.
- [ ] Browse/source/commit/code-nav parity is complete beyond the current bounded shell.
- [ ] Ask/chat parity is complete beyond the current thread rename/delete/visibility baseline, including archive/richer conversation lifecycle UX.
- [ ] Auth/admin parity is complete, including durable stores, richer org/invite management, API-key lifecycle, and external-provider identity flows.
- [ ] Provider parity is complete across GitHub, GitLab, Gitea, Gerrit, Bitbucket, Azure DevOps, and generic/local Git.
- [ ] MCP parity is complete beyond the new bounded authenticated HTTP/API bridge, including full MCP protocol/session transport interoperability, client integration, richer tool coverage, and broader end-to-end evidence.
- [ ] Worker/runtime parity is complete with real scheduling, retries, supervision, and richer observability.
- [ ] Operator/runtime parity is complete beyond the current local stub-backed and manifest-validated local maintenance baseline.
- [ ] The rewrite can honestly be described as a practical Sourcebot replacement rather than a bounded clean-room subset.

## Conclusion

Task 100 closes as an honest release-candidate smoke and parity re-audit closure.
The smoke contract is green and the feature matrix is grounded, but the truthful
answer remains that practical Sourcebot replacement parity is **not** complete.
The unchecked checklist items above are still blocking work, now carried forward
under `docs/plans/2026-04-28-sourcebot-practical-replacement-blockers-roadmap.md`
so future runs can resume from a blocker-focused roadmap instead of reopening the
completed Task 86-100 sprint or claiming false 100% parity.
