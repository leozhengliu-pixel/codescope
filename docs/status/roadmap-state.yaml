version: 1
roadmap:
  path: docs/plans/2026-04-28-sourcebot-practical-replacement-blockers-roadmap.md
  total_tasks: 9
project:
  repo_path: /opt/data/projects/sourcebot-rewrite
  branch: master
  head: 156450c0ab8588dd2f617e3250954e5d8bd594e9
last_completed_task:
  id: task102m
  title: Indexed-empty search artifacts and sync retry visibility
  status: done
  summary: >-
    Closed one Hermes parallel-subagent batch across the practical replacement
    blockers. Task 102 now distinguishes successful but searchable-empty index
    builds as `indexed_empty` in the search store, repository index-status API
    consumers, and frontend detail UI; the worker accepts and persists
    skipped-only local Git search-index artifacts instead of treating them as
    failed index builds. Task 106/108 now exposes a bounded authenticated sync
    history `retryable` flag that is true only for admin-visible failed jobs
    with no queued/running same-target job, matching the manual retry endpoint's
    current fail-closed behavior. Task 105 now writes a durable PostgreSQL
    audit event for invite redemption and fixes invite test fixtures so they do
    not expire as wall-clock time moves forward. Specs record the bounded
    claims without certifying full production indexing, automated retry/backoff,
    complete OIDC/provider identity, or final practical replacement parity.
  tests:
    - cargo fmt --check: passed
    - git diff --check: passed
    - 'cargo test -p sourcebot-search local_search_store_distinguishes_indexed_empty_repositories -- --nocapture': passed
    - cargo test -p sourcebot-search: passed with 19 unit tests and 0 doctests
    - 'cargo test -p sourcebot-worker run_repository_sync_claim_tick_persists_index_artifact_for_skipped_only_local_repositories -- --nocapture': passed
    - 'cargo test -p sourcebot-worker run_repository_sync_claim_tick_succeeds_for_real_local_git_repository_path -- --nocapture': passed
    - 'cargo test -p sourcebot-api auth_repository_sync_jobs_list_only_jobs_visible_to_current_user_newest_first -- --nocapture': passed
    - 'cargo test -p sourcebot-api auth_repository_sync_jobs -- --nocapture --test-threads=1': passed with 12 focused sync-job tests
    - 'cargo test -p sourcebot-api pg_org_auth_metadata_redeeming_invite_writes_audit_event -- --nocapture --test-threads=1': passed in lib and bin targets
    - 'cargo test -p sourcebot-api invite_redeem_ -- --nocapture --test-threads=1': passed with 8 invite redeem tests
    - cargo check: passed with existing unused-code warnings
    - 'npm test -- --run': passed with 180 frontend tests
  review:
    code: Hermes delegated implementation plus independent spec/code re-reviews completed; manual Codex integration review fixed admin-only retryability, active same-target suppression, date-drifted invite fixtures, and skipped-only local index artifact semantics before verification
  commit: 156450c0ab8588dd2f617e3250954e5d8bd594e9
  next_task: task102
current_task:
  id: task102
  parent_task: practical-replacement-blockers
  title: Production search/indexing runtime parity
  status: pending
  summary: >-
    Continue Task 102 with the next meaningful search/indexing runtime closure.
    Prefer a slice that moves beyond bounded pagination metadata, frontend paging
    controls, explicit blank `repo_id` fail-closed hardening,
    case-insensitive local/index-artifact matching, space-separated all-term
    local/index-artifact matching, bounded quoted-phrase local/index-artifact
    matching, bounded unquoted `lang:`/`path:` local/index-artifact filters,
    deterministic repo/path/line ordering for identical indexed content,
    bounded unquoted OR line-term groups, and explicit bounded
    `mode=boolean|literal|regex` search modes plus their dedicated frontend
    selector/deep-link preservation, and repository-detail read-only
    index-status visibility plus `indexed_empty`/skipped-only local artifact
    semantics into practical indexing/runtime behavior,
    operator-visible index lifecycle/status, failure recovery, richer
    full-boolean/filter grammar, branch/index availability, streaming behavior,
    or deeper pagination/relevance stability, while preserving fail-closed
    visibility boundaries, focused verification, bounded docs claims,
    independent review, commit, state update, and push.

history:
  - id: task102m
    status: done
    summary: >-
      Closed one Hermes parallel-subagent batch across the practical replacement
      blockers. Task 102 now distinguishes successful but searchable-empty index
      builds as `indexed_empty` in the search store, repository index-status API
      consumers, and frontend detail UI; the worker accepts and persists
      skipped-only local Git search-index artifacts instead of treating them as
      failed index builds. Task 106/108 now exposes a bounded authenticated sync
      history `retryable` flag that is true only for admin-visible failed jobs
      with no queued/running same-target job, matching the manual retry
      endpoint's current fail-closed behavior. Task 105 now writes a durable
      PostgreSQL audit event for invite redemption and fixes invite test
      fixtures so they do not expire as wall-clock time moves forward. Specs
      record the bounded claims without certifying full production indexing,
      automated retry/backoff, complete OIDC/provider identity, or final
      practical replacement parity.
    tests:
      - cargo fmt --check: passed
      - git diff --check: passed
      - 'cargo test -p sourcebot-search local_search_store_distinguishes_indexed_empty_repositories -- --nocapture': passed
      - cargo test -p sourcebot-search: passed with 19 unit tests and 0 doctests
      - 'cargo test -p sourcebot-worker run_repository_sync_claim_tick_persists_index_artifact_for_skipped_only_local_repositories -- --nocapture': passed
      - 'cargo test -p sourcebot-worker run_repository_sync_claim_tick_succeeds_for_real_local_git_repository_path -- --nocapture': passed
      - 'cargo test -p sourcebot-api auth_repository_sync_jobs_list_only_jobs_visible_to_current_user_newest_first -- --nocapture': passed
      - 'cargo test -p sourcebot-api auth_repository_sync_jobs -- --nocapture --test-threads=1': passed with 12 focused sync-job tests
      - 'cargo test -p sourcebot-api pg_org_auth_metadata_redeeming_invite_writes_audit_event -- --nocapture --test-threads=1': passed in lib and bin targets
      - 'cargo test -p sourcebot-api invite_redeem_ -- --nocapture --test-threads=1': passed with 8 invite redeem tests
      - cargo check: passed with existing unused-code warnings
      - 'npm test -- --run': passed with 180 frontend tests
    review:
      code: Hermes delegated implementation plus independent spec/code re-reviews completed; manual Codex integration review fixed admin-only retryability, active same-target suppression, date-drifted invite fixtures, and skipped-only local index artifact semantics before verification
    commit: 156450c0ab8588dd2f617e3250954e5d8bd594e9
    next_task: task102
  - id: task102l
    status: done
    summary: >-
      Closed one manual Task 102 repository-operations/indexing visibility slice
      by exposing the existing `/api/v1/repos/{repo_id}/index-status` contract
      in the repository detail UI. Repo detail now renders a read-only Index
      status panel with indexed/error state, indexed file count, indexed line
      count, skipped file count, and optional index error text while keeping the
      endpoint's existing fail-closed authorization boundary. README,
      repository-operations acceptance, parity-gap, and final-audit wording now
      ground the frontend-visible baseline while keeping durable/background
      indexing, queue depth, retry controls, and real provider reindex execution
      open.
    tests:
      - 'npm test -- --run src/App.test.tsx -t "loads repository detail"': passed with 1 focused repo-detail test and 179 skipped
      - npm run build: passed with Vite production build
      - 'npm test -- --run src/App.test.tsx': passed with 180 frontend tests
      - git diff --check: passed
      - added-lines security scan: reviewed documentation-only API-key wording; no secrets added
    review:
      code: manual Codex review passed for typed index-status response handling, repo-detail lifecycle isolation, loading/error states, existing repository visibility contract reuse, focused UI coverage, and bounded-doc-claim accuracy
    commit: 01276e230e4f053314440a89b26d74cfe03e5167
    next_task: task102
  - id: task102k
    status: done
    summary: >-
      Closed one manual Task 102 frontend search usability slice by exposing the
      previously shipped explicit `mode=boolean|literal|regex` search capability
      on the dedicated `#/search` page. The UI now includes a search-mode
      selector, keeps default boolean searches on the existing URL/API shape,
      adds `mode=literal|regex` only when selected, restores mode from deep
      links, preserves mode across result pagination and repository-detail back
      links, and updates README, acceptance, parity-gap, and final-audit wording
      without overclaiming real indexing, richer grammar, branch availability,
      relevance, streaming, or stable-pagination parity.
    tests:
      - 'npm test -- --run src/App.test.tsx -t "search"': passed with 9 focused search tests and 171 skipped
      - npm run build: passed with Vite production build
      - 'npm test -- --run src/App.test.tsx': passed with 180 frontend tests
      - git diff --check: passed
      - added-lines security scan: reviewed documentation-only API-key wording; no secrets added
    review:
      code: manual Codex review passed for route parsing, default boolean compatibility, explicit mode API propagation, pagination/back-link preservation, test coverage, and bounded-doc-claim accuracy
    commit: ee9f767df1d6b28b64104ffcdfbe9daf3f33d9ca
    next_task: task102
  - id: task103a
    status: done
    summary: >-
      Closed one manual Task 103 browse/code-navigation slice by extending the
      bounded symbol extractor beyond Rust to top-level TypeScript and
      JavaScript declarations. The definitions API can now return supported
      results for `.ts`, `.tsx`, `.js`, and `.jsx` files covering functions,
      classes, interfaces, type aliases, enums, constants, and arrow-function
      constants, while unsupported language handling remains graceful. README,
      code-navigation acceptance, parity-gap, and final-audit wording now ground
      the shipped baseline while keeping tree-sitter precision, nested/member
      symbols, cross-file reference indexing, branch-aware symbol indexes,
      syntax highlighting, and production code-navigation parity open.
    tests:
      - 'cargo test -p sourcebot-search extract_symbols_returns_top_level_typescript_and_javascript_definitions -- --nocapture': red first for single-line declaration range bleed, then passed with 1 unit test and 17 filtered out
      - 'cargo test -p sourcebot-api definitions_returns_supported_typescript_definitions -- --nocapture': passed with 0 lib-target tests and 1 bin-target test
      - 'cargo test -p sourcebot-search': passed with 18 unit tests and 0 doctests
      - 'cargo test -p sourcebot-api definitions_ -- --nocapture': passed with 0 lib-target tests and 9 bin-target tests
      - 'cargo test -p sourcebot-api references_ -- --nocapture': passed with 0 lib-target tests and 8 bin-target tests
      - cargo fmt --check: passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: manual Codex review passed for language dispatch, top-level-only symbol boundaries, no-regression Rust extraction, definitions API compatibility, bounded-doc-claim accuracy, and unsupported-language behavior
    commit: ff6c480eb36e6aa3d712e65e58ee0069e90e29d6
    next_task: task102
  - id: task102j
    status: done
    summary: >-
      Closed a larger manual Task 102 search/indexing grammar slice by adding
      explicit `mode=boolean|literal|regex` support to `/api/v1/search` and the
      startup/local-sync search store. Boolean remains the default bounded
      parser, literal mode treats the trimmed query as an exact
      case-insensitive line substring, regex mode performs bounded
      case-insensitive line-level regex matching, and invalid regex syntax fails
      closed to zero matches instead of broadening results or crashing. The
      selected mode is returned in the search response, local-sync artifact
      fallback uses the same mode, README, acceptance, parity-gap, and
      final-audit wording now ground the shipped baseline, while real indexed
      regex execution, cross-branch index availability, relevance ranking,
      streaming search, and production index lifecycle semantics remain open.
    tests:
      - 'cargo test -p sourcebot-search local_search_store_supports_explicit_literal_and_regex_modes -- --nocapture': passed with 1 unit test and 16 filtered out
      - 'cargo test -p sourcebot-api search_supports_explicit_literal_and_regex_modes -- --nocapture': passed with 0 lib-target tests and 1 bin-target test
      - 'cargo test -p sourcebot-search': passed with 17 unit tests and 0 doctests
      - 'cargo test -p sourcebot-api search_ -- --nocapture': passed with 0 lib-target tests and 28 bin-target tests
      - cargo fmt --check: passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: manual Codex review passed for mode propagation, local-sync fallback consistency, invalid-regex fail-closed behavior, existing boolean default compatibility, docs bounded-claim accuracy, and added dependency scope
    commit: 6e332559583b18fc5f8f1c14d28812554c898e23
    next_task: task102
  - id: task102i
    status: done
    summary: >-
      Closed one bounded Task 102 search/indexing grammar slice by making the
      startup/local-sync `LocalSearchStore` treat an unquoted `OR` token as a
      boolean separator between line-term groups. Existing no-OR queries keep the
      all-term semantics, quoted `or` text remains a literal phrase, malformed
      leading/trailing/empty OR groups fail closed to no matches, and global
      `lang:`/`path:` filters continue to constrain every OR group. README,
      search acceptance, parity-gap, and final-audit wording now ground this
      bounded OR baseline while keeping real indexing, regex/full boolean
      grammar, cross-branch semantics, relevance ranking, streaming search,
      stronger stable-pagination guarantees, and practical replacement readiness
      open.
    tests:
      - 'cargo test -p sourcebot-search local_search_store_matches_bounded_boolean_or_groups -- --nocapture': red first for missing OR grouping, then passed with 1 unit test and 15 filtered out
      - 'cargo test -p sourcebot-search': passed with 16 unit tests and 0 doctests
      - 'cargo test -p sourcebot-api search_ -- --nocapture': passed with 0 lib-target tests and 27 bin-target tests
      - cargo fmt --check: passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking OR parsing, malformed-query fail-closed, quoted literal, filter-scope, bounded-doc-claim, performance, or security issues remained
    commit: 9022be6c19058e6c42d48ac56a5b9df2cee576e9
    next_task: task102
  - id: task102h
    status: done
    summary: >-
      Closed one bounded Task 102 search/indexing ordering slice by making the
      startup/local-sync `LocalSearchStore` return deterministic repo/path/line
      ordering for identical indexed content. Repository ids are sorted before
      unscoped local searches, recursive directory entries are sorted before
      indexing, and the new focused regression locks stable cross-repo and
      cross-path result order without changing authorization, query parsing,
      symlink/file-safety, or bounded local/index-artifact scope. README, search
      acceptance, parity-gap, and final-audit wording now ground this
      deterministic ordering baseline while keeping real indexing, richer
      grammar, cross-branch semantics, relevance ranking, streaming search,
      stronger stable-pagination guarantees across changing indexes/revisions,
      and practical replacement readiness open.
    tests:
      - 'cargo test -p sourcebot-search local_search_store_returns_deterministic_repo_path_line_order -- --nocapture': red first for nondeterministic repo/path order, then passed with 1 unit test and 14 filtered out
      - 'cargo test -p sourcebot-search': passed with 15 unit tests and 0 doctests
      - 'cargo test -p sourcebot-api search_ -- --nocapture': passed with 0 lib-target tests and 27 bin-target tests
      - cargo fmt --check: passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking deterministic-order, visibility-boundary, symlink/file-safety, bounded-doc-claim, performance, or security issues remained
    commit: 156e3153167a738d50624f5c9e3933679b2b17db
    next_task: task102
  - id: task102g
    status: done
    summary: >-
      Closed one bounded Task 102 search/indexing filter-grammar slice by making
      the startup/local-sync `LocalSearchStore` apply minimal unquoted `lang:`
      and `path:` filters to indexed result paths while preserving
      case-insensitive all-term line matching and quoted-phrase literals.
      Malformed empty filters such as `lang:` now fail closed to no matches
      instead of broadening to a plain line-term search, and quoted text such as
      `"path:src"` remains a literal phrase rather than becoming a filter.
      README, search acceptance, parity-gap, and final-audit wording now ground
      this bounded filter support while keeping real indexing,
      regex/boolean/full filter grammar, cross-branch semantics, relevance
      ranking, streaming search, stable-pagination parity, and practical
      replacement readiness open.
    tests:
      - 'cargo test -p sourcebot-search local_search_store_applies_lang_and_path_filters_without_line_term_broadening -- --nocapture': red first for missing lang/path filters, red again for reviewer-found empty-filter broadening, then passed with 1 unit test
      - 'cargo test -p sourcebot-search local_search_store_matches_quoted_phrases_and_unquoted_terms -- --nocapture': passed with 1 unit test
      - 'cargo test -p sourcebot-search': passed with 14 unit tests and 0 doctests
      - 'cargo test -p sourcebot-api search_ -- --nocapture': passed with 0 lib-target tests and 27 bin-target tests
      - cargo fmt --check: passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: passed independent read-only review after fixing quoted filter literals and empty-filter fail-open behavior; no blocking query-parsing, fail-closed malformed-filter, quoted-literal, docs-overclaim, performance, or security issues remained
    commit: eb39168cc9681fd6977c0df3372dfea7b10d90a8
    next_task: task102
  - id: task102f
    status: done
    summary: >-
      Closed one bounded Task 102 search/indexing semantics slice by making the
      startup/local-sync `LocalSearchStore` parse balanced quoted query segments
      as case-insensitive literal phrase components while preserving the existing
      all-term semantics for unquoted space-separated query words. Empty quoted
      or otherwise termless local searches now return no local matches instead of
      broadening to every indexed line. README, search acceptance, parity-gap,
      and final-audit wording now ground this bounded phrase support while
      keeping real indexing, richer grammar beyond this phrase baseline,
      cross-branch semantics, relevance ranking, streaming search,
      stable-pagination parity, and practical replacement readiness open.
    tests:
      - 'cargo test -p sourcebot-search local_search_store_matches_quoted_phrases_and_unquoted_terms -- --nocapture': red first with 1 failing unit test for phrase+term matching, then red again for reviewer-found empty quoted phrase broadening, then passed with 1 unit test
      - 'cargo test -p sourcebot-search local_search_store_matches_all_space_separated_query_terms -- --nocapture': passed with 1 unit test
      - 'cargo test -p sourcebot-search': passed with 13 unit tests and 0 doctests
      - 'cargo test -p sourcebot-api search_ -- --nocapture': passed with 0 lib-target tests and 27 bin-target tests
      - cargo fmt --check: passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: passed independent read-only review after tightening empty quoted/no-term searches; no blocking quoted-phrase semantics, API compatibility, bounded-doc-claim, performance-regression, or security issues remained
    commit: 2369ced9f78f9b845410d2268d05b7f5b54fdb60
    next_task: task102
  - id: task102e
    status: done
    summary: >-
      Closed one bounded Task 102 search/indexing semantics slice by making the
      startup/local-sync `LocalSearchStore` treat space-separated query words as
      case-insensitive all-term line matches while preserving single-term
      substring matching, the API's trimmed query response, and existing
      repository visibility/fail-closed boundaries. This improves practical
      search usability for the current bounded local/index-artifact baseline and
      updates README, acceptance, parity-gap, and final-audit wording without
      claiming real indexing, full filter grammar, cross-branch semantics,
      relevance ranking, streaming search, stable-pagination parity, or practical
      replacement readiness.
    tests:
      - 'cargo test -p sourcebot-search local_search_store_matches_all_space_separated_query_terms -- --nocapture': red first with 1 failing unit test, then passed with 1 unit test
      - 'cargo test -p sourcebot-api search_matches_all_space_separated_terms_without_exact_phrase -- --nocapture': red first with 0 lib-target tests and 1 failing bin-target test, then passed with 0 lib-target tests and 1 bin-target test
      - 'cargo test -p sourcebot-search': passed with 12 unit tests and 0 doctests
      - 'cargo test -p sourcebot-api search_ -- --nocapture': passed with 0 lib-target tests and 27 bin-target tests
      - cargo fmt --check: passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking query-semantics, auth/visibility-boundary, empty-query, bounded-doc-claim, performance-regression, or security issues remained
    commit: 76f30dc272c2b1317c46fefb280adc408e6440d2
    next_task: task102
  - id: task102d
    status: done
    summary: >-
      Closed one bounded Task 102 search/indexing semantics slice by making the
      startup/local-sync `LocalSearchStore` match indexed line text
      case-insensitively while preserving the caller's trimmed query text in the
      API response. This improves practical search usability for the current
      bounded local/index-artifact search baseline and updates acceptance,
      parity-gap, and final-audit wording without claiming real indexing, richer
      grammar, full relevance ranking, streaming search, stable-pagination
      parity, or practical replacement readiness.
    tests:
      - 'cargo test -p sourcebot-search local_search_store_matches_queries_case_insensitively -- --nocapture': red first with 1 failing unit test, then passed with 1 unit test
      - 'cargo test -p sourcebot-api search_trims_query_before_searching_and_reporting_response -- --nocapture': passed with 0 lib-target tests and 1 bin-target test
      - 'cargo test -p sourcebot-search': passed with 11 unit tests and 0 doctests
      - 'cargo test -p sourcebot-api search_ -- --nocapture': passed with 0 lib-target tests and 26 bin-target tests
      - cargo fmt --check: passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking search-semantics, API-trim, authorization/fallback-scope, docs-overclaim, performance-regression, or security issues remained
    commit: c6bca3addd3bdf1e58caa1dac82f85742f38b9ed
    next_task: task102
  - id: task102c
    status: done
    summary: >-
      Closed one bounded Task 102 search/indexing authorization-hardening slice
      by making `/api/v1/search` distinguish an absent `repo_id` from a present
      but blank/whitespace `repo_id`. Explicit blank repository filters such as
      `repo_id=%20%20` now fail closed with `404` instead of being treated as an
      absent filter that broadens the search across all visible repositories. The
      acceptance spec, parity-gap report, and final audit/checklist now document
      this bounded fail-closed repository-filter behavior while preserving open
      blockers for real production indexing, richer grammar, branch/index
      availability, relevance/stability, streaming search, SQL-backed saved
      contexts, and full practical search parity.
    tests:
      - 'cargo test -p sourcebot-api search_rejects_blank_present_repo_id_without_broadening_results -- --nocapture': red first with 0 lib-target tests and 1 bin-target test failing 200 vs 404, then passed with 0 lib-target tests and 1 bin-target test
      - 'cargo test -p sourcebot-api search_ -- --nocapture': passed with 0 lib-target tests and 25 bin-target tests
      - cargo fmt --check: passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking absent-vs-blank repo_id, hidden repo_id, pagination/saved-context visibility, bounded-doc-claim, or security issues remained
    commit: f6817a7e3460c959b3e82fa3b21acfaa3f69db76
    next_task: task102
  - id: task102b
    status: done
    summary: >-
      Closed one bounded Task 102 search/indexing UI slice by wiring the
      existing API pagination metadata into `#/search`. The frontend now
      requests `/api/v1/search` with bounded `limit=20` and explicit `offset`,
      renders result-count plus previous/next page controls from the API
      pagination response, preserves non-zero offsets in search hashes and
      repository-detail back links, and updates search acceptance/parity wording
      with a bounded UI claim. This improves practical search usability while
      keeping production indexing, richer grammar, branch/index availability,
      relevance tuning, streaming search, SQL-backed saved contexts, and full
      stable-pagination parity open.
    tests:
      - 'npm test -- --run src/App.test.tsx -t "opens repository source from dedicated search results"': passed with 1 test and 178 skipped
      - 'npm test -- --run src/App.test.tsx -t "renders API pagination metadata"': passed with 1 test and 178 skipped
      - 'npm test -- --run src/App.test.tsx -t "search"': passed with 8 tests and 171 skipped
      - 'npm test -- --run src/App.test.tsx': passed with 179 tests
      - 'npm run build': passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: passed independent read-only final review after adding direct non-zero-offset repository back-link and previous-page coverage; no blocking hash/URL, stale-state, previous/next, docs-overclaim, coverage, or security issues remained
    commit: 38d7b098b6b92631456d92b37bea66a574017be7
    next_task: task102
  - id: task102a
    status: done
    summary: >-
      Closed one bounded Task 102 search/indexing runtime slice by adding
      API-level `limit`/`offset` pagination metadata to `/api/v1/search`. The
      response now includes `pagination.limit`, `pagination.offset`,
      visibility-scoped `pagination.total_count`, and `pagination.has_more`
      after the existing repository visibility, saved-context, API-key-scope,
      and local sync fallback filters have selected the authorized result set.
      This advances practical search parity by making result paging/counting an
      explicit contract surface, but remains bounded: real production indexing,
      richer grammar, branch/index availability handling, relevance tuning,
      streaming search, and full stable-pagination parity across changing
      indexes remain open.
    tests:
      - cargo test -p sourcebot-api search_returns_paginated_results_with_total_count_and_has_more -- --nocapture: passed with 0 lib-target and 1 bin-target test
      - cargo test -p sourcebot-search: passed with 10 unit tests and 0 doctests
      - cargo test -p sourcebot-api search_ -- --nocapture: passed with 0 lib-target and 24 bin-target tests
      - cargo fmt --check: passed
      - git diff --check: passed
      - added-lines security scan: passed
      - docs pagination marker checks: passed
    review:
      code: passed independent read-only review; no blocking visibility-scope, response-shape, constructor-compatibility, bounded-doc-claim, or security issues remained
    commit: 98bcbbd04e29b28be1e194da1626c10709c658a2
    next_task: task102
  - id: task101
    status: done
    summary: >-
      Closed the first practical-replacement blocker roadmap durable-metadata
      slice by moving audit-event merge/persistence through the PostgreSQL-backed
      `PgOrganizationStore` compatibility envelope. In `DATABASE_URL` mode,
      `OrganizationState` now reads audit events from PostgreSQL, writes
      state-level audit events into PostgreSQL in the same store transaction used
      by other durable organization rows, and clears audit events from the
      fallback file so rows are not stranded in split-backend state. This
      advances release parity by making a real audit/runtime metadata surface
      durable under SQLx coverage, but it remains bounded: broad audit analytics,
      filtering/export/retention, external identity audit coverage, connection
      management durability, and full practical replacement parity remain open.
    tests:
      - repo-local PostgreSQL preflight for DATABASE_URL and TEST_DATABASE_URL: passed
      - cargo test -p sourcebot-api pg_organization_store_ -- --nocapture: passed with 11 lib-target and 11 bin-target tests
      - cargo fmt --check: passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking SQL, FK, rollback, fixture-isolation, file-backed compatibility, documentation-overclaim, or security issues remained
    commit: e5e0bbad704bb841202af5c373b3a2c70bfced31
    next_task: task102
  - id: task100
    status: done
    summary: >-
      Closed the release-candidate smoke and parity re-audit path by proving the
      bounded smoke matrix contract still passes, updating the final parity audit
      to record a truthful NOT READY verdict for practical Sourcebot replacement
      parity, and creating `docs/plans/2026-04-28-sourcebot-practical-replacement-blockers-roadmap.md`
      as the new blocker-focused roadmap. This closure intentionally fails closed:
      the smoke covers a useful local path across bootstrap/login, authenticated
      connections, search, ask, review-webhook intake, repository-sync queue
      visibility, and default one-tick worker completion, but durable metadata
      breadth, real provider/identity flows, production search/indexing depth,
      richer ask/admin lifecycle parity, MCP client/protocol interoperability,
      worker supervision/retry orchestration, and production-grade operator
      runtime behavior still block the practical replacement claim.
    tests:
      - bash scripts/check_end_to_end_smoke_matrix_contract.sh /opt/data/projects/sourcebot-rewrite: passed
      - task100 audit/roadmap marker checks: passed
      - git diff --check: passed
      - added-lines security scan: passed
    review:
      code: passed independent read-only review after fixing stale placeholder wording; no blocking bounded-claim, final-audit, acceptance-index, or follow-on-roadmap issues remained
    commit: b8f5dac07cea3b2fecbae942af6804a0b6914d9a
    next_task: task101
  - id: task99b
    status: done
    summary: >-
      Closed one bounded Task 99 operator recovery/deployment-safety slice by
      making runtime restore validate the captured backup manifest's resolved
      bootstrap, local-session, and organization-state paths against the current
      `SOURCEBOT_DATA_DIR` / explicit state-file override set before creating
      parent directories or copying files. Cross-runtime restore attempts now
      fail closed instead of replaying file-backed state into the wrong local
      runtime root. The claim remains bounded to local manifest-validated restore
      safety, not production rollback automation, zero-downtime deployment,
      supervised recovery, complete durable metadata parity, or practical
      replacement parity.
    tests:
      - bash scripts/check_runtime_backup_uniqueness.sh /opt/data/projects/sourcebot-rewrite: red first, then passed
      - bash scripts/check_operator_maintenance_contract.sh /opt/data/projects/sourcebot-rewrite: passed
      - bash scripts/check_local_metadata_backup_contract.sh /opt/data/projects/sourcebot-rewrite: passed
      - bash scripts/check_local_metadata_backup_restore_smoke.sh /opt/data/projects/sourcebot-rewrite: passed
      - sh -n touched shell scripts, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking shell/restore logic, fail-closed manifest mismatch, docs overclaiming, contract-wrapper drift, security, or bounded-claim issues remained
    commit: 576029c979e78e262340163dca0de595d4e02717
    next_task: task100
  - id: task99a
    status: done
    summary: >-
      Closed one bounded Task 99 operator/runtime observability slice by
      exposing the optional supervisor-readable worker last-run JSON snapshot
      through authenticated `GET /api/v1/auth/worker-status`. The endpoint
      requires a local session, keeps `SOURCEBOT_WORKER_STATUS_PATH` out of
      public config, reports `not_configured` when unset, returns bounded JSON
      snapshots when readable, and reports structured errors for missing,
      non-file, invalid, or oversized snapshots. The claim remains bounded to
      local authenticated status visibility, not production metrics, durable
      worker metadata, heartbeat/supervision, retry orchestration, migration
      automation, or full operator-runtime parity.
    tests:
      - cargo test -p sourcebot-config worker_status -- --nocapture: passed
      - cargo test -p sourcebot-api worker_status -- --nocapture: passed
      - cargo test -p sourcebot-api config_endpoint_hides_database_url_value -- --nocapture: passed
      - cargo test -p sourcebot-api --lib: passed after repo-local PostgreSQL preflight as hermes
      - cargo fmt --check, git diff --check, docs marker checks, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking auth-boundary, path/status snapshot safety, unbounded read/parse, public-config leakage, security, logic, or bounded-doc-claim issues remained
    commit: e6f8ff05953171b9ef92f6b86b4d533a236e6852
    next_task: task99b
  - id: task98
    status: done
    summary: >-
      Closed one bounded Task 98 worker/runtime operator-visibility slice by
      adding optional `SOURCEBOT_WORKER_STATUS_PATH` support to the real worker
      binary. The worker preflights the configured status path before claiming
      work and writes a supervisor-readable JSON last-run snapshot after
      successful bounded completion with schema version, completion flag,
      configured/completed tick counts, last tick/outcome, and nullable last
      work-item id/status. The claim remains bounded to a local operator artifact,
      not daemon supervision, production observability, durable worker metadata,
      broad retry/backoff, provider fetch/import/reindex, or full runtime parity.
    tests:
      - cargo test -p sourcebot-worker worker_binary_writes_supervisor_status_snapshot_for_no_work_tick -- --nocapture: red first, then passed
      - cargo test -p sourcebot-worker: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking path/status write safety, fail-closed preflight, bounded docs-claim, security, or logic issues remained
    commit: 64d7e77016c1d30a90998e5effe49a46ac036350
    next_task: task99
  - id: task97
    status: done
    summary: >-
      Closed one bounded Task 97 MCP runtime/auth slice by exposing authenticated
      HTTP/API MCP manifest, tool-list, and tool-call routes under
      `/api/v1/mcp/...`. The bridge reuses the existing web/API auth and
      repository visibility boundary, requires caller-visible `repo_id` for
      active-repository content tools, fails hidden repo ids closed, and proves
      `list_repos` returns only caller-visible repositories. The release claim
      remains bounded to authenticated HTTP/API MCP wiring, not full MCP
      protocol/session transport interoperability, client integration, richer
      tool coverage, or practical replacement parity.
    tests:
      - cargo test -p sourcebot-api mcp_http -- --nocapture: passed
      - cargo test -p sourcebot-mcp: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only re-review after fixing active repository selection for HTTP/API content tools; no blocking auth scope, hidden-repo leakage, schema truthfulness, route insertion, security, or bounded-doc-claim issues remained
    commit: c5b5f9fa0304f22b753c3947fb1ee43fef50a0dd
    next_task: task98
  - id: task96ad
    status: done
    summary: >-
      Closed one bounded Task 96 provider/runtime runtime slice by making
      configured Generic Git repository-sync jobs run a timeout-bounded real
      `git ls-remote --heads -- <base_url>` metadata probe before stub outcomes.
      Reachable remotes with at least one advertised branch persist terminal
      revision/current-branch metadata only; unreachable, empty, no-branch, or
      option-like base URLs fail closed with the operator-visible `generic Git
      repository sync execution failed` prefix. The worker drains stdout/stderr
      while waiting to avoid deadlock on large head listings, and Generic Git
      still does not claim clone/fetch/import/reindex, content-file counts,
      local artifacts, durable/background indexing, broad retry/backoff,
      supervision, provider discovery, or full provider/runtime parity.
    tests:
      - cargo test -p sourcebot-worker generic_git_remote -- --nocapture: passed
      - cargo test -p sourcebot-worker repository_sync_claim_tick -- --nocapture: passed
      - cargo test -p sourcebot-worker: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking path/shell safety, option-injection, timeout/output-drain, fail-closed, metadata-only, security, or bounded-doc-claim issues remained
    commit: fc8db4f7420b2bfac0ae29fc0e3d8dd1a5a258cd
    next_task: task97
  - id: task96ac
    status: done
    summary: >-
      Closed one bounded Task 96 provider/runtime queue-safety slice by
      rejecting manual enqueue/retry attempts with `409 Conflict` when a queued
      or running repository-sync job already exists for the same
      organization/repository/connection target. The conflict paths run under
      the current organization-state write lock immediately before append,
      preserve terminal history, and leave existing sync-job history unchanged.
      Real provider fetch/import/reindex, durable/background indexing, broad
      automated retry/backoff, supervision, and full provider/runtime parity
      remain follow-up work.
    tests:
      - cargo test -p sourcebot-core repository_sync_active_target_helper_matches_only_queued_or_running_same_target -- --nocapture: passed
      - cargo test -p sourcebot-api auth_repository_sync_jobs_create_rejects_duplicate_queued_target_without_mutation -- --nocapture: passed
      - cargo test -p sourcebot-api auth_repository_sync_jobs_retry_rejects_when_active_target_exists_without_mutation -- --nocapture: passed
      - cargo test -p sourcebot-api auth_repository_sync_jobs -- --nocapture: passed after repo-local PostgreSQL preflight with `.env` as hermes
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking authz/check-ordering, no-mutation, retry-history preservation, race semantics for the current bounded store path, security, or bounded-doc-claim issues remained
    commit: adc3c81d27b5bab13a1bd19904250db0eaadf613
    next_task: task96ad
  - id: task96ab
    status: done
    summary: >-
      Closed one bounded Task 96 provider/runtime indexing slice by allowing
      authenticated unscoped and search-context scoped `/api/v1/search`
      requests to fall back to latest successful caller-visible local sync
      `search-index.json` artifacts when the primary startup search store has no
      matching rows. The slice preserves requested-repo behavior, primary-search
      precedence, visibility narrowing, deterministic visible-repo iteration,
      and fail-closed malformed artifact handling; real provider
      fetch/import/reindex, durable/background indexing, broad retry/backoff,
      supervision, and full provider/runtime parity remain follow-up work.
    tests:
      - cargo test -p sourcebot-api search_unscoped_uses_latest_successful_local_sync_search_index_artifact_for_visible_repo -- --nocapture: red first, then passed
      - cargo test -p sourcebot-api search_context_uses_latest_successful_local_sync_search_index_artifact_for_scoped_repo -- --nocapture: passed
      - cargo test -p sourcebot-api local_sync -- --nocapture: passed
      - cargo test -p sourcebot-api search_ -- --nocapture: passed
      - cargo fmt --check, git diff --check, docs marker checks, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking auth-scope, primary-precedence, deterministic fallback, malformed-artifact, path/connection-safety, security, or bounded-claim issues remained
    commit: 9d26bd5b55a84f73e7495ca1cb81c5efca30bedb
    next_task: task96ac
  - id: task96aa
    status: done
    summary: >-
      Closed one bounded Task 96 provider/runtime repository-operations slice by
      letting authenticated no-revision commit list/detail/diff reads fall back
      to the latest successful caller-authorized local Git sync job when the
      primary commit store has no seeded history for the visible repository.
      The fallback preserves primary-store precedence, API-key repo-scope
      filtering, caller-authorized organization/repository selection,
      duplicate-connection fail-closed behavior, and timeout-bounded Git
      execution. Real provider fetch/import/reindex, durable/background
      indexing, broad retry/backoff, supervision, and full provider/runtime
      parity remain follow-up work.
    tests:
      - cargo test -p sourcebot-api repo_commits -- --nocapture: passed
      - cargo test -p sourcebot-api local_commit_store -- --nocapture: passed
      - cargo test -p sourcebot-api local_sync -- --nocapture: passed
      - cargo test -p sourcebot-api repo_commit -- --nocapture: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking auth-scope, primary-precedence, duplicate-connection, bounded-Git-execution, security, or bounded-claim issues remained
    commit: 0377af550b1790332523f551367b34f9389b6e36
    next_task: task96ab
  - id: task96z
    status: done
    summary: >-
      Closed one bounded Task 96 provider/runtime status slice by making
      requested-repo index-status fallback load the latest successful authorized
      local sync `search-index.json` artifact when present instead of rewalking
      the mutable snapshot tree. Search and index-status now share fail-closed
      artifact handling: malformed present artifacts return `500`, while
      snapshot traversal remains only for pre-artifact rows. The existing
      visibility boundary, primary-store precedence, and bounded claim language
      remain intact; real provider fetch/import/reindex, durable/background
      indexing, broad retry/backoff, supervision, and full provider/runtime
      parity remain follow-up work.
    tests:
      - cargo test -p sourcebot-api malformed_local_sync_search_index_artifact -- --nocapture: passed
      - cargo test -p sourcebot-api repository_index_status -- --nocapture: passed
      - cargo test -p sourcebot-api local_sync -- --nocapture: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only re-review after fixing artifact fail-closed alignment for both search and index-status; no blocking auth-boundary, primary-precedence, artifact-failure, path-safety, or bounded-claim issues remained
    commit: 9a8f75a01256f9a518516ec1a520bb37cf6a1ee8
    next_task: task96aa
  - id: task96y
    status: done
    summary: >-
      Closed one bounded Task 96 provider/runtime search slice by making
      requested-repo search fallback load the latest successful authorized local
      sync `search-index.json` artifact when present instead of rewalking the
      mutable snapshot tree. The existing visibility boundary, primary
      search-store precedence, and pre-artifact snapshot fallback remain intact;
      real provider fetch/import/reindex, durable/background indexing, broad
      retry/backoff, supervision, and full provider/runtime parity remain
      follow-up work.
    tests:
      - cargo test -p sourcebot-api search_uses_latest_successful_local_sync_search_index_artifact_for_visible_requested_repo -- --nocapture: red first, then passed
      - cargo test -p sourcebot-api local_sync -- --nocapture: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking artifact path-safety, auth-boundary, hidden-org leakage, failure-propagation, or bounded-claim issues remained
    commit: 95b0bfdeab8b916ca5c934e4fe6729712605b083
    next_task: task96z
  - id: task96x
    status: done
    summary: >-
      Closed one bounded Task 96 provider/runtime artifact slice by making
      sourcebot-search own a self-contained search-index artifact writer/loader
      and making the local repository-sync worker persist a sibling
      `search-index.json` after successful tracked-HEAD snapshot materialization.
      The artifact can be loaded without rewalking the snapshot, and failures
      fail local sync closed before terminal success. The slice keeps real
      provider fetch/import/reindex, durable/background indexing, broad
      retry/backoff, scheduling/supervision, provider discovery, and full
      provider/runtime parity as follow-up work.
    tests:
      - cargo test -p sourcebot-search local_search_store_can_round_trip_persisted_index_artifact_without_rewalking_snapshot -- --nocapture: passed
      - cargo test -p sourcebot-worker succeeds_for_real_local_git_repository_path -- --nocapture: passed
      - cargo test -p sourcebot-search and cargo test -p sourcebot-worker: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking path-safety, failure-propagation, serialization, or bounded-claim issues remained
    commit: fbb02287931efc17ff39a14baf4cdaca24c1add7
    next_task: task96y
  - id: task96w
    status: done
    summary: >-
      Closed one bounded Task 96 provider/runtime UI visibility slice by
      refetching authenticated repository-sync history after a successful local
      repository import so the queued worker handoff appears on the same local
      connection card from the real API inventory. The slice also guards slow
      initial history responses from overwriting the post-import refresh and
      preserves already visible history rows when the post-import refresh fails,
      while keeping real provider fetch/import/reindex, durable/background
      indexing, broad retry/backoff, scheduling/supervision, and full
      provider/runtime parity as follow-up work.
    tests:
      - npm test -- --run src/App.test.tsx -t "refreshes local repository sync history": passed
      - npm test -- --run src/App.test.tsx -t "keeps refreshed local import sync history": passed
      - npm test -- --run src/App.test.tsx -t "preserves existing local import sync history": red first, then passed
      - npm test -- --run src/App.test.tsx -t "local import": passed
      - npm test -- --run src/App.test.tsx and npm run build: passed
      - git diff --check, docs marker check, and added-lines security scan: passed
    review:
      code: passed independent read-only re-review after fixing the preserveOnError/UI-rendering blocker; no blocking stale-response, preservation, docs-claim, security, or coverage issues remained
    commit: 89e6e329852d97a15cab0c4597bfd2c8b1cd01d7
    next_task: task96x
  - id: task96v
    status: done
    summary: >-
      Closed one bounded Task 96 provider/runtime robustness slice by switching
      local tracked-content discovery to NUL-delimited `git ls-tree -rz
      --name-only HEAD`, preserving significant leading/trailing/surrounding
      spaces in tracked file names during repo-local snapshot materialization.
      The slice keeps the claim bounded to local tracked `HEAD`
      manifest/snapshot robustness and does not claim real provider
      fetch/import/reindex, broad retry/backoff, supervised scheduling,
      durable/background indexing, or full provider/runtime parity.
    tests:
      - cargo test -p sourcebot-worker snapshots_tracked_paths_with_surrounding_spaces -- --nocapture: red first, then passed
      - cargo test -p sourcebot-worker: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking NUL-delimited parsing, path-preservation, snapshot safety, regression, security, or bounded-claim issues remained
    commit: 90e3fa3eb1454cd5e30426daba64eaec95e071f9
    next_task: task96w
  - id: task96u
    status: done
    summary: >-
      Closed one meaningful Task 96/98 worker-runtime recovery slice by adding
      one bounded automatic replay for repository-sync jobs failed by stale
      running lease recovery. The worker preserves the failed terminal row,
      creates at most one `_auto_retry_1` replacement after an additional
      one-hour backoff, resets runtime/result fields on the replacement, skips
      auto-retry source jobs, and suppresses duplicate replacements for the
      same organization/repository/connection target including prior terminal
      auto-retry history. Docs keep broad retry/backoff, scheduling,
      supervision, real provider fetch/import/reindex, durable/background
      indexing, and full provider/runtime parity as follow-up work.
    tests:
      - cargo test -p sourcebot-worker stale_lease -- --nocapture: passed
      - cargo test -p sourcebot-worker terminal_retry_for_same_target -- --nocapture: passed
      - cargo test -p sourcebot-worker: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking stale-lease replay, duplicate-suppression, reset-field, security, or bounded-claim issues remained
    commit: a9ada442a90b459151d3e702fef81a4b76646e55
    next_task: task96v
  - id: task96t
    status: done
    summary: >-
      Closed one meaningful Task 96/98 worker-runtime closure by adding an
      explicit operator-bounded multi-tick worker loop while preserving the
      default one-tick baseline. `SOURCEBOT_WORKER_MAX_TICKS` now bounds repeat
      ticks, `SOURCEBOT_WORKER_IDLE_SLEEP_MS` controls no-work sleeps, invalid
      bounded-loop configuration fails closed before mutation, and docs keep the
      claim narrow: bounded local repeat-tick execution, not automated
      retry/backoff, daemonization, production scheduling/supervision, real
      provider fetch/import/reindex, durable/background indexing, or full
      provider/runtime parity.
    tests:
      - cargo test -p sourcebot-worker worker_binary_bounded_runtime_processes_multiple_repository_sync_jobs_when_tick_limit_is_configured -- --nocapture: passed
      - cargo test -p sourcebot-worker invalid -- --nocapture and cargo test -p sourcebot-worker zero_tick -- --nocapture: passed
      - cargo test -p sourcebot-worker worker_binary_logs_runtime_baseline_and_no_work_path -- --nocapture: passed
      - cargo test -p sourcebot-worker: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking bounded-loop, fail-closed config, priority/no-work, stale-recovery, security, or bounded-claim issues remained
    commit: b2994e806faae44328a7ef02f7be53605815e9a2
    next_task: task96u
  - id: task96s
    status: done
    summary: >-
      Closed one meaningful Task 96/98 provider-runtime queue-recovery slice by
      making the one-shot repository-sync worker mark stale `running` sync jobs
      failed after a one-hour lease before claiming the next queued job. The
      closure preserves fresh running jobs, continues to process the queued job
      in the same tick, updates worker-runtime/generic-local-git/README/parity
      wording, and keeps the claim bounded: this is one-shot stale-running lease
      recovery, not automated retry/backoff, scheduling, supervision, real
      provider fetch/import/reindex execution, durable/background indexing, or
      full provider/runtime parity.
    tests:
      - cargo test -p sourcebot-worker run_repository_sync_claim_tick_recovers_stale_running_jobs_before_claiming_next -- --nocapture: passed
      - cargo test -p sourcebot-worker: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking security, logic, queue-semantics, path-safety, or bounded-claim issues remained
    commit: 7eec1e88c2af8dcf7c5175cab35806f2c1e3236e
    next_task: task96t
  - id: task96r
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime code-navigation slice by
      letting no-revision repository definitions/references requests use the
      latest successful local sync snapshot when the primary browse/code-nav
      store has no blob for that visible repository. The fallback is gated by
      API-key/session visible repo scope plus the caller-authorized
      `(organization_id, repository_id)` boundary, preserves primary browse
      results and explicit-revision semantics, emits terminal sync revisions in
      response metadata and browse URLs, and remains bounded local snapshot
      search/browse/status/code-navigation visibility only: no real provider
      fetch/import/reindex, durable/background indexing, automated retry/backoff,
      supervision, or full provider/runtime parity is claimed.
    tests:
      - cargo test -p sourcebot-api repository_definitions_and_references_fall_back_to_authorized_local_sync_snapshot -- --nocapture: red first on API-key scoped fallback returning 200, then passed
      - cargo test -p sourcebot-api definitions -- --nocapture: passed
      - cargo test -p sourcebot-api references -- --nocapture: passed
      - cargo test -p sourcebot-api repo_blob -- --nocapture: passed
      - cargo test -p sourcebot-api repository_tree -- --nocapture: passed
      - cargo fmt --check, git diff --check, docs marker checks, and added-lines security scan: passed
    review:
      code: passed independent read-only re-review after fixing reviewer-found API-key repo-scope fallback blocker; no blocking authz, fallback-behavior, path-safety, revision, test, or bounded-claim issues remained
    commit: e24790c7f28a3b3b51eac9bb7f664caa1e8f212b
    next_task: task96s
  - id: task96q
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime browse slice by letting
      no-revision repository tree/blob requests use the latest successful local
      sync snapshot when the startup browse store has no tree/blob for that
      visible repository. The fallback is gated by the caller-authorized
      `(organization_id, repository_id)` boundary, preserves primary browse
      results and explicit-revision semantics, and remains bounded local
      snapshot search/browse/status visibility only: no real provider
      fetch/import/reindex, durable/background indexing, automated
      retry/backoff, supervision, or full provider/runtime parity is claimed.
    tests:
      - cargo test -p sourcebot-api repository_tree_and_blob_fall_back_to_authorized_local_sync_snapshot -- --nocapture: passed
      - cargo test -p sourcebot-api repo_blob -- --nocapture: passed
      - cargo test -p sourcebot-api repository_tree -- --nocapture: passed
      - cargo test -p sourcebot-api search_ -- --nocapture: passed
      - cargo fmt --check, git diff --check, docs marker checks, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking authz, fallback-behavior, path-safety, test, or bounded-claim issues remained
    commit: ad79ec96311342e594de5f6ae45093778a7fd3e7
    next_task: task96r
  - id: task96p
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime status slice by letting
      requested-repo index-status use the latest successful local sync snapshot
      when the startup search store has no status for that visible repository.
      The fallback is gated by the caller-authorized `(organization_id,
      repository_id)` boundary, preserves primary search-store status when it
      exists, and remains bounded local snapshot search/status visibility only:
      no real provider fetch/import/reindex, durable/background indexing,
      automated retry/backoff, supervision, or full provider/runtime parity is
      claimed.
    tests:
      - cargo test -p sourcebot-api repository_index_status_falls_back_to_authorized_local_sync_snapshot -- --nocapture: red first, then passed
      - cargo test -p sourcebot-api repository_index_status -- --nocapture: passed
      - cargo test -p sourcebot-api search_ -- --nocapture: passed
      - cargo fmt --check, git diff --check, docs marker checks, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking security, logic, auth-scope, path-safety, fallback-behavior, test, or bounded-claim issues remained
    commit: ad42187f29ed301a5bb4515af073d9a6b0a8316e
    next_task: task96q
  - id: task96o
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime search slice by letting an
      explicitly scoped requested-repo search use the latest successful local
      sync snapshot when the startup search store has no rows for that visible
      repository. The fallback is gated by the caller-authorized
      `(organization_id, repository_id)` boundary and remains bounded local
      snapshot search/status only: no real provider fetch/import/reindex,
      durable/background indexing, automated retry/backoff, supervision, or full
      provider/runtime parity is claimed.
    tests:
      - cargo test -p sourcebot-api search_local_sync_snapshot_fallback_stays_inside_authorized_organization_scope -- --nocapture: red first, then passed
      - cargo test -p sourcebot-api search_ -- --nocapture: passed
      - cargo fmt --check, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking auth-scope, path-safety, fallback-behavior, test, or bounded-claim issues remained
    commit: 66fe9ff1b36efc26b22f7ef71d517ea5870de027
    next_task: task96p
  - id: task96n
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime operator-visibility slice by
      showing deterministic local sync artifact locations in authenticated
      settings connection sync-history rows for successful local jobs. The UI
      derives the manifest and sibling snapshot paths with the worker-compatible
      sanitizer and renders them as plain text only for successful local jobs
      with terminal revision metadata. The claim remains bounded local operator
      artifact visibility only: no real provider fetch/import/reindex execution,
      automated retry/backoff, supervision, durable index status, or full
      provider/runtime parity is claimed.
    tests:
      - npm test -- --run src/App.test.tsx -t "local sync artifact paths": red first on absent artifact-path text, then passed
      - npm test -- --run src/App.test.tsx -t "repository sync": passed
      - npm test -- --run src/App.test.tsx: passed
      - npm run build: passed
      - git diff --check and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking security, path-safety, UI scope, or bounded-claim issues remained
    commit: 7747e0d2b87dbb9cf07bffe7449c5e2083dfc06f
    next_task: task96o
  - id: task96m
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime execution slice by writing a
      bounded sibling `snapshot/` tree for successful local repository-sync jobs
      after the existing bounded Git preflight, revision, current-branch, and
      tracked-path probes pass. The snapshot is materialized from tracked `HEAD`
      bytes, preserves nested relative paths, excludes untracked files, rejects
      unsafe relative paths, and keeps snapshot creation/materialization/finalize
      failures on the fail-closed local sync execution path before terminal
      success is persisted. The claim remains bounded manifest/snapshot/status
      evidence only: no real provider fetch/import/reindex execution, automated
      retry/backoff, supervision, durable index status, or full provider/runtime
      parity is claimed.
    tests:
      - cargo test -p sourcebot-worker run_repository_sync_claim_tick_succeeds_for_real_local_git_repository_path -- --nocapture: red first on missing snapshot file, then passed
      - cargo fmt --check: passed
      - git diff --check: passed
      - cargo test -p sourcebot-worker: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review twice; no blocking security, logic, path-safety, failure-propagation, or bounded-claim issues remained
    commit: 7fe8cc333a104af4948b476214deedef3ce0ffca
    next_task: task96n
  - id: task96l
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime worker slice by writing a
      deterministic repo-local tracked-content manifest for successful local
      repository-sync jobs after bounded Git preflight, HEAD, tracked-path, and
      current-branch probes pass. Dynamic manifest path components are sanitized,
      the manifest records revision/current branch/tracked HEAD paths while
      excluding untracked files, and manifest write/finalize failures stay on the
      fail-closed local sync execution path before terminal success is persisted.
      The claim remains bounded manifest/status evidence only: no real provider
      fetch/import/reindex execution, automated retry/backoff, supervision,
      durable index status, or full provider/runtime parity is claimed.
    tests:
      - cargo test -p sourcebot-worker local_sync_manifest_path_components_cannot_escape_manifest_root -- --nocapture: passed
      - cargo fmt --check: passed
      - cargo test -p sourcebot-worker -- --nocapture: passed
      - git diff --check and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking security, path-safety, failure-propagation, or bounded-claim issues remained
    commit: 277eb0d79218f2867f66e57f8bff256900238140
    next_task: task96m
  - id: task96k-api-ui
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime visibility slice by exposing
      already-persisted local repository-sync terminal metadata through
      authenticated sync-history responses and the settings connection
      sync-history UI. Existing auth/visibility filtering remains unchanged, and
      the UI conditionally renders nullable revision/current-branch/tracked-file
      count fields only when the API returns them. The claim remains read-only
      metadata/status visibility only: no real provider fetch/import/reindex
      execution, automated retry/backoff, supervision, durable index status, or
      full provider/runtime parity is claimed.
    tests:
      - cargo fmt --check: passed
      - cargo test -p sourcebot-api auth_repository_sync_jobs_list_only_jobs_visible_to_current_user_newest_first -- --nocapture: passed
      - npm test -- --run src/App.test.tsx -t "renders each connection's repository sync history newest-first": passed
      - npm test -- --run src/App.test.tsx -t "repository sync": passed
      - npm run build, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking security, logic, auth-scope, nullability, test, or bounded-claim issues remained
    commit: 510976064b0a5c2e7cd9e8f89a97990bafc7163e
    next_task: task96l
  - id: task96k
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime metadata slice by persisting
      successful local repository-sync terminal metadata: nullable synced
      revision, current branch, and tracked-content file-count fields now move
      through the model, PostgreSQL migration 0015, PgOrganizationStore
      row-mapping/upsert paths, and the worker success path for configured
      `local` connections. SQLx repeatability was also hardened by resetting
      catalog connections in the org-auth metadata test helper. The claim remains
      local Git metadata/status persistence only: no real provider
      fetch/import/reindex execution, automated retry/backoff, supervision,
      durable index status, or full provider/runtime parity is claimed.
    tests:
      - repo-local PostgreSQL preflight as hermes proved both DATABASE_URL and TEST_DATABASE_URL
      - cargo test -p sourcebot-worker run_repository_sync_claim_tick_succeeds_for_real_local_git_repository_path -- --nocapture: passed
      - cargo test -p sourcebot-worker -- --nocapture: passed (21 worker lib + worker smoke targets)
      - cargo test -p sourcebot-api catalog_migration_inventory_bootstraps_catalog_org_repository_permissions_sessions_ask_threads_review_agent_runs_delivery_attempts_repository_sync_jobs_auth_audit_events_and_external_accounts -- --nocapture: passed
      - cargo test -p sourcebot-api --bin sourcebot-api pg_org_auth_metadata_ -- --nocapture --test-threads=1: passed
      - cargo test -p sourcebot-api pg_organization_store_persists_repository_permission_binding_and_sync_job_together -- --nocapture: passed
      - make sqlx-test: passed after repo-local PostgreSQL preflight
      - cargo fmt --check, metadata/sqlx doc wrappers, doc truth-marker checks, git diff --check, and added-lines security scan: passed
    review:
      code: passed independent read-only review; no blocking security, logic, persistence, or claim-boundary issues remained
    commit: f81812e839b1eaa059e80c14729f453e7ebecfd2
    next_task: task96l
  - id: task96j
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime handoff slice by moving the
      authenticated local-repository import route beyond catalog-only persistence:
      canonical-path-backed PostgreSQL catalog identity, same-basename/idempotent
      imports, admin-visible organization binding, and exactly one queued
      repository-sync job now move together through the organization-store
      boundary. PostgreSQL catalog import and handoff paths use transaction-scoped
      advisory locks to avoid duplicate/racy imports or sync jobs. The claim
      remains explicit local import-to-worker handoff only: no real provider
      fetch/import/reindex execution, automated retry/backoff, supervision,
      durable index progress, or full provider/runtime parity is claimed.
    tests:
      - repo-local PostgreSQL preflight as hermes proved both DATABASE_URL and TEST_DATABASE_URL
      - cargo test -p sourcebot-api --bin sourcebot-api auth_local_repository_import -- --nocapture --test-threads=1: passed (6 focused route tests in make sqlx-test)
      - cargo test -p sourcebot-api pg_catalog_store_imports_local_repository -- --nocapture: passed
      - cargo test -p sourcebot-api pg_organization_store_persists_repository_permission_binding_and_sync_job_together -- --nocapture: passed
      - cargo test -p sourcebot-api local_repository -- --nocapture: passed (1 lib + 7 main tests)
      - make sqlx-test: passed after repo-local PostgreSQL preflight (migration reset through 0014_local_repository_paths plus storage/catalog/auth/repository-sync selectors)
      - cargo fmt --check: passed
      - metadata/bootstrap/sqlx/local-metadata contract scripts: passed
      - git diff --check: passed
      - python3 added-lines security scan over git diff --cached --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review after reviewer-forced catalog/handoff concurrency fixes; no blocking security or logic issues remained
    commit: e3706e38c28f876dd3b10c973659e200f79c43fa
    next_task: task96k
  - id: task96i
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime catalog handoff slice by
      moving the PostgreSQL catalog path beyond read-only list/detail queries for
      one explicitly requested local Git working tree. PgCatalogStore now
      validates and canonicalizes the requested worktree, derives the current
      branch, transactionally upserts the local connection plus repository row,
      and returns the imported repository through normal list/detail catalog
      surfaces. The authenticated route keeps unsupported catalog backends
      fail-closed with a deterministic fake-store regression. The claim remains
      explicit local import handoff only: no recursive/provider discovery, real
      fetch/import/reindex execution, connection-management durability, durable
      index status, or full provider/runtime parity is claimed.
    tests:
      - cargo test -p sourcebot-api pg_catalog_store_imports_local_repository_into_postgres_catalog -- --nocapture: passed (storage import regression passed in both lib and main harnesses)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_local_repository_import -- --nocapture --test-threads=1: passed (5 auth local import route tests)
      - make sqlx-test: passed after repo-local PostgreSQL preflight (storage::tests 7 passed; pg_catalog_store_ 4 passed; plus existing SQLx metadata/auth selectors)
      - cargo fmt --check: passed
      - bash scripts/check_metadata_bootstrap_docs.sh README.md: passed
      - bash scripts/check_sqlx_test_workflow.sh /opt/data/projects/sourcebot-rewrite: passed
      - git diff --check: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved the bounded PostgreSQL local-import handoff, fail-closed auth/path validation, bind-parameter SQL and transaction shape, unsupported-backend fake-store decoupling, and docs that keep recursive/provider discovery, real fetch/import/reindex execution, and full durable provider/runtime parity deferred
    commit: 1a3d87f6840d2a5a6cc89ca8f5ca15f2d156bc16
    next_task: task96j
  - id: task96h
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime execution slice by moving
      the local repository-sync worker baseline beyond revision/current-branch
      metadata probes into bounded tracked-content discovery. Jobs tied to
      configured `local` connections now run `git -C <repo_path> ls-tree -r
      --name-only HEAD` after the existing working-tree and HEAD checks;
      empty-tree repositories with a valid commit fail closed with `local
      repository sync execution failed` instead of recording success, while
      repositories with at least one tracked content path continue to the
      current-branch probe and bounded terminal success path. The claim remains
      content discovery/status only: no real fetch/import/reindex, automated
      retry/backoff, supervision, durable index progress, or full
      provider/runtime parity is claimed.
    tests:
      - cargo test -p sourcebot-worker run_repository_sync_claim_tick_fails_closed_for_local_git_repository_without_tracked_content -- --nocapture: passed (1 passed; 20 filtered in worker lib, remaining targets 0-test)
      - cargo test -p sourcebot-worker: passed (21 worker lib tests, 5 explicit completed smoke tests, 1 invalid outcome smoke, 4 idle smoke tests, 2 repository-sync claim smoke tests, 2 worker-runtime logging smoke tests, and 0 doctests)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 doc truth-marker checks for README, worker-runtime acceptance, generic-local acceptance, parity-gap report, and final parity audit content-discovery wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved the bounded `git ls-tree -r --name-only HEAD` content-discovery probe, empty-tree fail-closed terminal error, non-zero/timeout/execution-error handling through the existing execution-failure paths, success logging of tracked content count, focused empty-tree regression, and docs that keep real fetch/import/reindex, retries/backoff, supervision, durable progress, and full provider/runtime parity deferred
    commit: c156d86e3c3d40b8cfb9de39a50dbc0095927525
    next_task: task96i
  - id: task96g
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime execution slice by moving
      the local repository-sync worker baseline beyond working-tree preflight and
      into bounded real Git metadata probes. Jobs tied to configured `local`
      connections still fail closed for missing, invalid, bare, or hung working
      trees through the existing `local repository sync preflight failed` path,
      and now also run `git -C <repo_path> rev-parse HEAD` plus `git -C
      <repo_path> symbolic-ref --short HEAD` before success. Empty or detached
      local repositories fail closed with `local repository sync execution
      failed`, while a real local Git repository with readable current
      revision/current branch metadata can still complete successfully even when
      the old stub failure outcome is configured. The claim remains
      metadata/status only: no real fetch/import/reindex, automated
      retry/backoff, supervision, durable index progress, or full
      provider/runtime parity is claimed.
    tests:
      - cargo test -p sourcebot-worker run_repository_sync_claim_tick_fails_closed_for_empty_local_git_repository_path -- --nocapture: passed (1 passed; 19 filtered in worker lib, remaining targets 0-test)
      - cargo test -p sourcebot-worker local_git_repository_path -- --nocapture: passed (3 worker lib tests: bare, empty, and real local repository; remaining targets 0-test)
      - cargo test -p sourcebot-worker: passed (20 worker lib tests, 5 explicit completed smoke tests, 1 invalid outcome smoke, 4 idle smoke tests, 2 repository-sync claim smoke tests, 2 worker-runtime logging smoke tests, and 0 doctests)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 doc truth-marker checks for README, worker-runtime acceptance, generic-local acceptance, parity-gap report, and final parity audit current-branch metadata wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved the bounded `rev-parse HEAD` and `symbolic-ref --short HEAD` metadata probes after local working-tree preflight, preserved preflight-vs-execution failure prefixes, empty/detached fail-closed behavior, existing bare/real local coverage, current-branch wording correction, and docs that keep real fetch/import/reindex, retries/backoff, supervision, durable progress, and full provider/runtime parity deferred
    commit: 5bd9df285342704f13e1616cfdd324ffa6029b34
    next_task: task96h
  - id: task96f
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime hardening slice by bounding
      the worker-side local-Git preflight instead of splitting implementation,
      regression coverage, acceptance wording, parity-audit synchronization, and
      review into paperwork-only runs. Repository-sync jobs tied to configured
      `local` connections still run `git -C <repo_path> rev-parse
      --is-inside-work-tree` before terminal completion, but a hung Git preflight
      is now killed and fails closed with an operator-visible `local repository
      sync preflight failed: git preflight timed out` error rather than wedging
      the one-shot worker path. Missing, invalid, bare, valid, and now hung
      local-Git preflight outcomes remain bounded status evidence only; this
      does not claim real fetch/import/reindex execution, retries/backoff,
      supervision, durable index progress, or full provider/runtime parity.
    tests:
      - cargo test -p sourcebot-worker local_repository_sync_preflight_times_out_and_fails_closed -- --nocapture: passed (1 passed; 18 filtered in worker lib, remaining targets 0-test)
      - cargo test -p sourcebot-worker: passed (19 worker lib tests, 5 explicit completed smoke tests, 1 invalid outcome smoke, 4 no-work smoke tests, 2 repository-sync claim smoke tests, 2 worker-runtime logging smoke tests, and 0 doctests)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 doc truth-marker checks for README, worker-runtime acceptance, generic-local acceptance, parity-gap report, and final parity audit timeout wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved the timeout-bounded local-Git preflight implementation, child kill/wait cleanup, preserved success/false/stderr semantics, fail-closed terminal status/error behavior, deterministic hung-preflight regression, and synchronized docs/audit wording that keeps real fetch/import/reindex, retries/backoff, supervision, durable progress, and full provider/runtime parity deferred
    commit: b8b8e67348747eb1a31dfe889df3016827e9d6e7
    next_task: task96g
  - id: task96e
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime execution slice instead of
      splitting store callback plumbing, local-Git preflight behavior, focused
      worker/API verification, acceptance wording, parity-audit synchronization,
      and review into paperwork-only runs. Repository-sync claim execution now
      receives an organization-state snapshot, so jobs tied to configured `local`
      connections run a bounded `git -C <repo_path> rev-parse
      --is-inside-work-tree` preflight before terminal completion. Missing or
      invalid local paths, including bare repositories whose preflight prints
      `false`, fail closed with an operator-visible `local repository sync
      preflight failed` error, while real local Git working trees can complete
      successfully even when the old stub failure outcome is configured. The
      claim remains deliberately narrow: this proves local working-tree
      preflight/status handling only, not real fetch/import/reindex execution,
      host discovery, automated retry/backoff, supervision, durable index status,
      or full provider runtime parity.
    tests:
      - cargo test -p sourcebot-worker local_repository_path -- --nocapture: passed (1 passed; 17 filtered in worker lib, remaining targets 0-test)
      - cargo test -p sourcebot-worker local_git_repository_path -- --nocapture: passed (2 passed; 16 filtered in worker lib, remaining targets 0-test)
      - cargo test -p sourcebot-worker worker -- --nocapture: passed (1 worker unit test, 14 integration smoke tests, and one 0-test bin target)
      - repo-local PostgreSQL preflight as hermes: bundled pg_ctl reported a live server and bundled psql proved both DATABASE_URL and TEST_DATABASE_URL with select 1
      - cargo test -p sourcebot-api --bin sourcebot-api auth_repository_sync_jobs -- --nocapture --test-threads=1: passed with .env/PostgreSQL preflight (10 passed; 368 filtered)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 callback-form search for stale fn(RepositorySyncJob) / execute(claimed_job) forms: passed
      - python3 doc truth-marker checks for README, worker-runtime acceptance, generic-local acceptance, parity-gap report, and final parity audit wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved the HRTB callback update, absence of stale callback forms, preserved claim-and-complete semantics, shell-free local Git preflight with bare-repo stdout=false fail-closed handling, focused missing/bare/valid local repo tests, stub preservation, and docs that keep real fetch/import/reindex, retries, supervision, and full provider parity deferred; non-blocking notes recorded that the legacy pure-stub helper intentionally uses default state and that a future runtime slice may add an explicit git preflight timeout
    commit: ddc28722316b05f6da725994779c393e36b43f22
    next_task: task96f
  - id: task96d
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime observability slice instead
      of splitting worker log fields, focused smoke coverage, README/runtime
      acceptance wording, parity-report synchronization, and review into
      paperwork-only runs. The one-shot worker now records repository-sync
      terminal job context for operator triage when processing the bounded stub
      path: job id, organization id, repository id, connection id, terminal
      status, and failure reason when one exists. The claim remains deliberately
      narrow: this improves the current one-shot/stub runtime baseline and does
      not claim real provider fetch/import execution, automated retry/backoff,
      continuous scheduling, supervision, frontend controls, durable index status,
      or full provider/runtime parity.
    tests:
      - cargo test -p sourcebot-worker --test repository_sync_claim_smoke -- --nocapture: passed (2 passed; 0 failed)
      - cargo test -p sourcebot-worker worker -- --nocapture: passed (1 worker unit test, 14 integration smoke tests, and one 0-test bin target)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 doc truth-marker checks for README, worker-runtime acceptance, parity-gap report, and final parity audit wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only final review; reviewer approved the bounded scope, repository-sync terminal log fields, smoke-test assertions for job/org/repo/connection/status/failure reason, and docs that keep real execution, retries, supervision, scheduling, and durable parity deferred, with only non-blocking notes about future log redaction and empty error-field semantics for successful jobs
    commit: e20131bfc10fb28158d4e8d3a216aabc82c222a3
    next_task: task96e
  - id: task96c
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime hardening slice instead of
      splitting retry-route behavior, fail-closed authz ordering regressions,
      acceptance wording, parity-audit synchronization, PostgreSQL-backed route
      family verification, and review into paperwork-only runs. Authenticated
      organization admins can now POST
      `/api/v1/auth/repository-sync-jobs/{job_id}/retry` for an admin-visible
      failed repository-sync job to append a fresh queued retry with the same
      organization, repository, and catalog-derived connection while preserving
      the original terminal history row. Hidden/cross-org jobs fail closed before
      terminal status checks, visible non-admin jobs fail closed before terminal
      status checks, non-failed visible admin targets return conflict without
      mutation, and docs keep the release claim bounded to manual backend retry
      rather than automated backoff, real provider fetch/import execution,
      supervised scheduling, frontend controls, durable index status, or full
      provider/runtime parity.
    tests:
      - cargo test -p sourcebot-api --bin sourcebot-api auth_repository_sync_jobs_retry -- --nocapture: passed (4 passed; 374 filtered)
      - repo-local PostgreSQL preflight as hermes: bundled pg_ctl reported a live server and bundled psql proved both DATABASE_URL and TEST_DATABASE_URL with select 1
      - cargo test -p sourcebot-api --bin sourcebot-api auth_repository_sync_jobs -- --nocapture --test-threads=1: passed with .env/PostgreSQL preflight (10 passed; 368 filtered)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 doc truth-marker checks for repository-operations, generic-local-git, acceptance index, parity-gap report, and final parity audit wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved retry-route authentication, visibility-before-status ordering for hidden non-failed jobs, admin-before-status ordering for visible non-admin non-failed jobs, no-mutation failure paths, terminal-history preservation, and bounded manual-retry docs, with only non-blocking notes about practically negligible retry-id collision risk and docs wording that was tightened from generic retries to automated retries/backoff before commit
    commit: b3fd90a5f2b6dea3f6073b49125cd8db0d1ec8c9
    next_task: task96d
  - id: task96b
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime smoke closure instead of
      splitting the contract wrapper, smoke assertions, worker-progress evidence,
      acceptance wording, and review into paperwork-only runs. The end-to-end
      smoke matrix now proves a bounded repository-sync enqueue-to-progress path:
      an authenticated admin enqueues a visible repository sync job, authenticated
      history observes it as queued, the real one-shot worker runs after
      review-agent work is drained with the configured succeeded stub outcome, and
      authenticated history plus persisted organization state observe the same job
      as succeeded with start/finish timestamps. The contract wrapper now requires
      the phase-specific `[repository-sync] worker completed queued sync job`
      marker rather than a generic token that could be satisfied by the final PASS
      line. The release claim remains deliberately bounded to one-shot stub
      progress, not real provider fetch/import execution, retries, daemonized
      scheduling, frontend controls, durable index status, or full provider/runtime
      parity.
    tests:
      - bash -n scripts/check_end_to_end_smoke_matrix.sh scripts/check_end_to_end_smoke_matrix_contract.sh: passed
      - git diff --check: passed
      - bash scripts/check_end_to_end_smoke_matrix_contract.sh /opt/data/projects/sourcebot-rewrite: passed in background with completion notification; output PASS: end-to-end smoke matrix contract verified
      - python3 doc/script marker checks for the phase-specific repository-sync marker and bounded acceptance wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved the admin enqueue, queued-history, one-shot worker, terminal-history, persisted organization-state assertions, the phase-specific contract marker, and bounded docs, with only a non-blocking note that the queued-list GET assertion checks presence/status rather than null start/finish timestamps
    commit: 7f286616b0dfcb0bb860a33418a7439ebe5ef668
    next_task: task96c
  - id: task96a
    status: done
    summary: >-
      Closed one meaningful Task 96 provider/runtime slice instead of splitting
      backend route behavior, focused authz/no-mutation coverage, acceptance
      wording, parity-audit synchronization, and review into paperwork-only runs.
      Authenticated organization admins can now POST to
      `/api/v1/auth/repository-sync-jobs` with an administered and currently
      visible repository to persist a queued repository-sync job whose
      `connection_id` is derived from catalog repository detail rather than caller
      input. Blank payload fields, non-admin callers, hidden repositories, and
      catalog-missing repositories fail closed before mutation. The release claim
      remains deliberately bounded: this is a backend-only enqueue baseline, not
      real provider discovery/import execution, worker retry/progress handling,
      frontend sync controls, persisted index status, or full provider runtime
      parity.
    tests:
      - cargo test -p sourcebot-api --bin sourcebot-api auth_repository_sync_jobs_create -- --nocapture: passed (2 passed; 372 filtered)
      - repo-local PostgreSQL preflight as hermes: bundled pg_ctl reported a live server and bundled psql proved both DATABASE_URL and TEST_DATABASE_URL with select 1
      - cargo test -p sourcebot-api --bin sourcebot-api auth_repository_sync_jobs -- --nocapture --test-threads=1: passed with .env/PostgreSQL preflight (6 passed; 368 filtered)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 doc truth checks for repository-operations, generic-local-git, acceptance index, parity-gap report, and final parity audit wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved the local-session/admin/visible-binding/catalog-presence gates before mutation, catalog-derived connection id, docs claim bounds, and existing GET behavior preservation, with a non-blocking note that blank-field, hidden-binding, and catalog-missing no-mutation cases could be locked by additional tests later
    commit: e419c08f1fcda1b12bf112d50d1b371a434ec750
    next_task: task96b
  - id: task95
    status: done
    summary: >-
      Closed one meaningful Task 95 external-provider identity slice instead of
      splitting model, migration, route, UI copy, acceptance wording, parity-audit
      sync, focused verification, and review into smaller paperwork-only runs. The
      rewrite now has a bounded `ExternalAccount` model plus `OrganizationState`
      persistence, migration `0013_external_accounts`, PostgreSQL auth metadata
      loading for same-local-user external identities, and authenticated linked
      accounts responses/settings UI that show the current local identity first
      followed by same-user provider identities while excluding external identities
      that belong to other local users. The claim remains deliberately narrow:
      external provider login/callback exchange, external-account creation/linking,
      account-merge policy, SSO login, provider token exchange, and broader
      linked-account management UX remain follow-up work.
    tests:
      - repo-local PostgreSQL preflight: bundled pg_ctl reported a live server and bundled psql proved both DATABASE_URL and TEST_DATABASE_URL with select 1
      - cargo test -p sourcebot-models external_accounts -- --nocapture: passed (2 passed; 48 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_linked_accounts_returns_postgres_backed_memberships_when_database_url_is_configured -- --nocapture --test-threads=1: passed with .env/PostgreSQL preflight (1 passed; 371 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_linked_accounts -- --nocapture --test-threads=1: passed with .env/PostgreSQL preflight (4 passed; 368 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_linked_accounts_returns_current_local_identity_and_visible_memberships -- --nocapture: passed (1 passed; 371 filtered)
      - cargo test -p sourcebot-api storage::tests::catalog_migration_inventory_bootstraps_catalog_org_repository_permissions_sessions_ask_threads_review_agent_runs_delivery_attempts_repository_sync_jobs_auth_audit_events_and_external_accounts -- --nocapture: passed in lib and bin targets (1 passed each)
      - make sqlx-test: passed after the PostgreSQL preflight
      - cargo fmt --check: passed
      - git diff --check: passed
      - npm test -- --run src/App.test.tsx -t "linked accounts inventory": passed (1 passed; 173 skipped)
      - npm test -- --run src/App.test.tsx -t "linked accounts": passed (2 passed; 172 skipped)
      - npm test -- --run src/App.test.tsx: passed (174 passed)
      - npm run build: passed in background (Vite build complete)
      - python3 doc/UI stale-phrase truth checks for acceptance specs, gap report, final audit, App copy, and App tests: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only final review; reviewer approved the same-user scope boundary, bounded provider-login/linking claims, migration inventory synchronization, UI copy/test consistency, and docs/final-audit synchronization, with only a non-blocking note that external identities reuse the response field `created_at` for the persisted `linked_at` display timestamp
    commit: 5f8e1bd31f895cc121df99c1a67667257ec7051f
    next_task: task96
  - id: task94h
    status: done
    summary: >-
      Closed one meaningful Task 94h auth/admin durability slice instead of
      splitting migration inventory, transaction-bound audit writes, legacy
      compatibility audit visibility, focused SQLx verification, acceptance
      wording, parity-report updates, and independent review into smaller
      paperwork-only runs. Local member invite create/cancel, member-role update,
      and member-removal mutations now write durable PostgreSQL audit-event rows
      in the same transaction as the auth metadata mutation when DATABASE_URL is
      configured. The audit-events API reads PostgreSQL rows scoped to the
      caller's visible organizations while best-effort merging visible file-backed
      compatibility rows for older API-key/OAuth audit events, so durable member
      lifecycle audit visibility no longer depends on a readable fallback
      aggregate. Docs keep the claim bounded: filtering/export, broad audit
      analytics, external-provider identity audit events, retention/export policy,
      broader org/member CRUD, and full audit parity remain follow-up work.
    tests:
      - repo-local PostgreSQL preflight: bundled pg_ctl reported a live server and bundled psql proved both DATABASE_URL and TEST_DATABASE_URL with select 1
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_returns_postgres_backed_member_and_invite_rosters_when_database_url_is_configured -- --nocapture --test-threads=1: passed (1 passed; 371 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_remove_keeps_postgres_mutation_and_audit_when_compatibility_audit_read_fails -- --nocapture --test-threads=1: passed (1 passed; 371 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_ -- --nocapture --test-threads=1: passed (11 passed; 361 filtered)
      - cargo test -p sourcebot-api storage::tests::catalog_migration_inventory_bootstraps_catalog_org_repository_permissions_sessions_ask_threads_review_agent_runs_delivery_attempts_repository_sync_jobs_and_auth_audit_events -- --nocapture: passed in lib and bin targets (1 passed each)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_api_key_routes_use_postgres_metadata_when_database_url_is_configured -- --nocapture --test-threads=1: passed (1 passed; 371 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_oauth_clients_use_postgres_metadata_when_database_url_is_configured -- --nocapture --test-threads=1: passed (1 passed; 371 filtered)
      - make sqlx-test: passed after the PostgreSQL preflight, including older API-key/OAuth audit-visibility SQLx selectors (5 passed; 367 filtered) and invite_redeem_ (8 passed; 364 filtered)
      - cargo fmt --check: passed
      - git diff --check: passed
      - bash scripts/check_metadata_bootstrap_docs.sh README.md: passed
      - bash scripts/check_sqlx_test_workflow.sh .: passed
      - python3 raw-content doc truth checks for auth acceptance, settings acceptance, parity-gap report, and final audit wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only re-review after fixing reviewer-found split-backend blockers; reviewer approved transaction-bound PostgreSQL audit inserts for invite create/cancel, role update, and member removal; best-effort compatibility audit writes with no PostgreSQL rollback/restore; visible-organization scoping for PostgreSQL audit reads; and merged visible file-backed compatibility rows for older audit events
    commit: 3523484dc733ffd410172ea519dd1400f7e81ea0
    next_task: task95
  - id: task94g
    status: done
    summary: >-
      Closed one meaningful Task 94g auth/admin lifecycle hardening slice instead
      of splitting PostgreSQL locking fixes, file-backed route policy gates,
      focused SQL-backed/file-backed regressions, acceptance wording, parity-report
      updates, and independent review into smaller paperwork-only runs.
      Authenticated admin-visible member removal and role-update paths now reject
      self-removal plus last-admin removal/demotion without mutation; the
      PostgreSQL auth-metadata store enforces the same invariant inside the
      update/delete transaction by locking concrete admin membership rows instead
      of using invalid aggregate `COUNT(*) ... FOR UPDATE`; and docs keep the
      claim bounded to fail-closed policy gates rather than broad role lifecycle
      or full org/member CRUD parity.
    tests:
      - repo-local PostgreSQL preflight: pg_ctl reported a live bundled server; DATABASE_URL and TEST_DATABASE_URL both returned select 1
      - cargo test -p sourcebot-api auth::tests::pg_org_auth_metadata_rejects_last_admin_removal_without_mutation -- --nocapture --test-threads=1: RED initially failed because PostgreSQL remove_member returned Some and allowed last-admin removal; after the fix, passed in lib and bin targets (1 passed each; 62/371 filtered)
      - cargo test -p sourcebot-api auth::tests::pg_org_auth_metadata_rejects_last_admin_demotion_without_mutation -- --nocapture --test-threads=1: passed in lib and bin targets (1 passed each; 62/371 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_role_update -- --nocapture --test-threads=1: passed (2 passed; 370 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_remove -- --nocapture --test-threads=1: passed (3 passed; 369 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_ -- --nocapture --test-threads=1: passed (11 passed; 361 filtered)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 raw-content doc truth checks for auth acceptance, settings acceptance, parity-gap report, and final audit wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved file-backed self-removal and last-admin removal/demotion gates before membership mutation/audit writes, PostgreSQL transaction-bound last-admin checks using row-level locks instead of invalid aggregate FOR UPDATE, no-mutation behavior, and bounded docs, with only non-blocking future suggestions about deterministic lock ordering and split-backend audit atomicity under races
    commit: eb6b26b29a25bb4d01fdcb5a6a3c2a5526e88701
    next_task: task94h
  - id: task94f
    status: done
    summary: >-
      Closed one meaningful Task 94f auth/admin lifecycle slice instead of
      splitting backend member-removal routes, PostgreSQL membership deletion,
      shipped file-backed audit visibility, rollback hardening, settings UI
      controls, acceptance wording, parity-report updates, focused SQL-backed and
      file-backed route coverage, and independent review into smaller paperwork
      fragments. Authenticated local-session admins can now remove visible active
      organization members through `DELETE /api/v1/auth/members/{user_id}` with
      an administered `organization_id`; blank ids, hidden organizations,
      non-admin callers, and unknown member targets fail closed without mutation;
      both file-backed and PostgreSQL-backed paths delete only the membership and
      never the local account; PostgreSQL-backed deletion preserves shipped
      file-backed `auth.member.removed` audit visibility and best-effort restores
      the durable membership if audit read/write fails; and the
      `#/settings/members` panel exposes bounded remove controls only from the
      loaded admin-visible roster. Docs keep the claim bounded: email delivery,
      invite resend workflow, durable audit-event storage, last-admin/self-removal
      policy enforcement, broad org/member CRUD, and full org/member lifecycle
      parity remain follow-up work.
    tests:
      - repo-local PostgreSQL preflight: pg_ctl reported a live bundled server; DATABASE_URL and TEST_DATABASE_URL both returned select 1
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_remove -- --nocapture --test-threads=1: passed (2 passed; 366 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_ -- --nocapture --test-threads=1: passed (9 passed; 359 filtered)
      - cargo test -p sourcebot-api auth::tests::pg_org_auth_metadata_removes_and_restores_memberships_without_deleting_accounts -- --nocapture --test-threads=1: passed in lib and bin targets (1 passed each; 60/367 filtered)
      - cargo fmt --check: passed
      - git diff --check: passed
      - npm test -- --run src/App.test.tsx -t "removes a member": passed (1 passed; 173 skipped)
      - npm test -- --run src/App.test.tsx -t "members": passed (7 passed; 167 skipped)
      - npm test -- --run src/App.test.tsx: passed (174 passed)
      - npm run build: passed after correcting stale root-owned web/dist asset ownership
      - python3 raw-content doc/UI truth checks for auth acceptance, settings acceptance, parity-gap report, final audit wording, and settings copy: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved hidden/non-admin fail-closed gates, membership-only deletion without local-account deletion, parameterized PostgreSQL membership delete/restore, split-backend audit visibility with best-effort rollback on audit failures, bounded settings UI controls, and bounded docs with no blocking security or logic issues
    commit: e295db80ce52006be5d7cd88a90b4c36ba8e563f
    next_task: task94g
  - id: task94e
    status: done
    summary: >-
      Closed one meaningful Task 94e auth/admin lifecycle slice instead of
      splitting the backend role-update route, PostgreSQL mutation path, shipped
      file-backed audit visibility, settings UI controls, acceptance wording,
      parity-report updates, focused SQL-backed/file-backed route coverage, and
      independent review into smaller paperwork-only runs. Authenticated
      local-session admins can now update visible organization member roles
      through `PATCH /api/v1/auth/members/{user_id}/role`; invalid roles,
      hidden organizations, hidden member targets, and non-admin callers fail
      closed without mutation; PostgreSQL-backed role updates persist to
      `organization_memberships` while preserving shipped file-backed
      `auth.member_role.updated` audit visibility; and the `#/settings/members`
      panel exposes bounded role selects only from the loaded admin-visible
      roster while disabling conflicting invite/member mutations. Docs keep the
      claim bounded: email delivery, resend workflow, member removal, durable
      audit-event storage, broader org/member CRUD, last-admin/self-demotion
      policy, and full role lifecycle parity remain follow-up work.
    tests:
      - repo-local PostgreSQL preflight: pg_ctl reported a live bundled server; DATABASE_URL and TEST_DATABASE_URL both returned select 1
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_role_update -- --nocapture --test-threads=1: passed (1 passed; 364 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_ -- --nocapture --test-threads=1: passed (7 passed; 358 filtered), including the PostgreSQL-backed member/invite roster route test now covering durable role update, hidden-org rejection, unchanged hidden role, and audit visibility
      - cargo fmt --check: passed
      - git diff --check: passed
      - npm test -- --run src/App.test.tsx -t "updates a member role": passed (1 passed; 172 skipped)
      - npm test -- --run src/App.test.tsx -t "members": passed (6 passed; 167 skipped)
      - npm test -- --run src/App.test.tsx: passed (173 passed)
      - npm run build: passed
      - python3 raw-content doc/UI truth checks for auth acceptance, settings acceptance, parity-gap report, final audit wording, and settings copy: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved admin-visible scope gates, hidden target rejection, parameterized PostgreSQL update, shipped file-backed audit visibility, bounded settings UI controls, and bounded docs with no blocking security or logic issues
    commit: 483bb8944c98d70693e24570a792a3a549096d6f
    next_task: task94f
  - id: task94d
    status: done
    summary: >-
      Closed one meaningful Task 94d auth/admin lifecycle slice instead of
      splitting backend pending-invite cancellation, PostgreSQL conditional-delete
      hardening, settings UI controls, acceptance wording, parity-report updates,
      focused SQL-backed/file-backed route coverage, and independent review into
      smaller paperwork-only runs. Authenticated local-session admins can now
      cancel pending invites for administered organizations through `DELETE
      /api/v1/auth/members/invites/{invite_id}`; accepted, hidden, missing, and
      unauthorized invites fail closed without deletion; PostgreSQL-backed
      cancellation uses a conditional pending-only delete and preserves shipped
      file-backed `auth.member_invite.cancelled` audit visibility; and the
      `#/settings/members` panel exposes cancel controls only for pending invite
      rows. Docs keep the claim bounded: email delivery, resend workflow, role
      editing, member removal, durable audit-event storage, broader org/member
      CRUD, and full lifecycle parity remain follow-up work.
    tests:
      - repo-local PostgreSQL preflight: pg_ctl reported a live bundled server; DATABASE_URL and TEST_DATABASE_URL both returned select 1
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_invite_cancel -- --nocapture --test-threads=1: passed (1 passed; 363 filtered)
      - cargo test -p sourcebot-api auth::tests::pg_org_auth_metadata_deletes_only_pending_invites -- --nocapture --test-threads=1: passed in lib and bin targets (1 passed each; 59/363 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_ -- --nocapture --test-threads=1: passed (6 passed; 358 filtered)
      - cargo fmt --check: passed
      - git diff --check: passed
      - npm test -- --run src/App.test.tsx -t "cancels a pending member invite": passed (1 passed; 171 skipped)
      - npm test -- --run src/App.test.tsx -t "members": passed (5 passed; 167 skipped)
      - npm test -- --run src/App.test.tsx: passed (172 passed)
      - npm run build: passed after correcting stale root-owned web/dist asset ownership
      - python3 raw-content doc truth checks for auth acceptance, settings acceptance, parity-gap report, and final audit wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only re-review after fixing reviewer-found blockers; reviewer approved the pending-only PostgreSQL conditional delete, route-level auth/admin visibility gates, file-backed audit visibility ordering with best-effort audit rollback on durable delete failure, pending-only settings UI controls, parameterized SQL, and bounded docs with no remaining blocking issues
    commit: 91ec3ab0f41878b2249c11e19d288db3bb435b17
    next_task: task94e
  - id: task94c
    status: done
    summary: >-
      Closed one meaningful Task 94c durable auth/admin lifecycle slice instead of
      splitting PostgreSQL invite creation, audit visibility, settings/auth
      acceptance wording, parity-report updates, focused SQL-backed route coverage,
      and independent review into smaller paperwork-only runs. `POST
      /api/v1/auth/members/invites` now uses PostgreSQL auth metadata when
      `DATABASE_URL` is configured instead of returning `501`; authenticated
      admins can create pending invite rows for administered organizations, hidden
      and non-admin organizations still fail closed without inserts, the response
      is built from the durable members roster, and `auth.member_invite.created`
      remains visible through the shipped file-backed audit-events surface with
      rollback/delete on audit read/write failure. Docs keep the claim bounded:
      email delivery, resend/cancel, role editing, member removal, durable
      audit-event storage, broader org/member CRUD, and full lifecycle parity
      remain follow-up work.
    tests:
      - repo-local PostgreSQL preflight: pg_ctl reported a live bundled server; DATABASE_URL and TEST_DATABASE_URL both returned select 1
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_returns_postgres_backed_member_and_invite_rosters_when_database_url_is_configured -- --nocapture --test-threads=1: RED failed before implementation with 501 vs expected 201 (0 passed; 1 failed; 361 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_invite_create -- --nocapture --test-threads=1: passed (1 passed; 361 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_ -- --nocapture --test-threads=1: passed (5 passed; 357 filtered)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 raw-content doc truth checks for auth acceptance, settings acceptance, parity-gap report, and final audit wording: passed
      - python3 added-lines security scan over git diff --unified=0: passed (0 findings)
    review:
      code: passed independent read-only final review after coupled docs updates; reviewer approved admin authorization before insert, parameterized SQL, fail-closed hidden/non-admin behavior without inserts, refreshed PostgreSQL member responses, file-backed audit visibility with best-effort rollback on audit failures, and bounded docs with no blocking security or logic issues
    commit: 5684bc0af62c0e3ee9e8e73d1aab45eaa7d26e89
    next_task: task94d
  - id: task94b
    status: done
    summary: >-
      Closed one meaningful Task 94 auth/admin lifecycle slice instead of
      splitting backend invite creation, frontend settings controls, fail-closed
      load/error behavior, acceptance wording, parity-report truth updates,
      focused Rust/Vitest coverage, PostgreSQL fallback evidence, and independent
      review into smaller paperwork-only runs. `POST /api/v1/auth/members/invites`
      now lets an authenticated local-session admin create a pending local invite
      for an administered organization on the shipped file-backed baseline; it
      trims fields, rejects invalid role/email inputs, fails closed for hidden
      organizations and non-admin memberships, persists audit visibility in the
      same organization aggregate consumed by `GET /api/v1/auth/members`, and
      returns `501` instead of silently writing fallback files when PostgreSQL auth
      metadata is configured. The `#/settings/members` panel exposes the bounded
      create flow only after inventory loads successfully, posts the administered
      organization id plus trimmed email/role, updates the matching organization
      inventory from the response, clears the submitted email, and keeps docs/copy
      explicit that email delivery, resend/cancel, role editing, member removal,
      durable PostgreSQL invite creation, and full org/member lifecycle parity
      remain follow-up work.
    tests:
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_invite_create -- --nocapture: RED failed first for invalid email `@` with 201 before validation hardening, then passed (1 passed; 361 filtered)
      - repo-local PostgreSQL preflight: pg_ctl status reported live server; DATABASE_URL and TEST_DATABASE_URL both returned select 1
      - cargo test -p sourcebot-api --bin sourcebot-api auth_members_ -- --nocapture: passed (5 passed; 357 filtered), including PG-backed roster coverage and 501 file-backed-create fallback evidence
      - cargo fmt --check: passed
      - npm test -- --run src/App.test.tsx -t "creates a member invite": passed (1 passed; 170 skipped)
      - npm test -- --run src/App.test.tsx -t "members": passed (4 passed; 167 skipped)
      - npm test -- --run src/App.test.tsx: passed (171 passed)
      - npm run build: passed
      - git diff --check: passed
      - python3 doc/ui truth checks for auth acceptance, settings acceptance, parity-gap report, final audit, and UI copy: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed independent read-only re-review after fixing the reviewer-found invalid-email validation blocker and adding file-backed audit persistence plus PostgreSQL 501 fallback coverage; reviewer approved the bounded pending local-invite scope with no remaining blocking issues
    commit: dfa4af0f9fb29c68f8cd8b7d13a73323cad6d55d
    next_task: task94c
  - id: task94a
    status: done
    summary: >-
      Closed one meaningful Task 94 auth/admin lifecycle slice instead of
      splitting the frontend create form, one-time secret handling, fail-closed
      management controls, acceptance wording, parity-report truth updates,
      focused Vitest coverage, and independent review into smaller paperwork-only
      runs. The `#/settings/api-keys` panel now keeps the existing authenticated
      inventory and revoke behavior, adds a minimal create form that posts a
      trimmed name plus optional newline-delimited repository scope to
      `/api/v1/auth/api-keys`, prepends the created key metadata to the visible
      inventory without copying the plaintext secret into inventory rows, and
      reveals the returned secret only in the immediate create-success area.
      Initial inventory load failures fail closed by hiding management controls.
      Docs keep the claim bounded to minimal inventory/create/revoke with
      one-time secret reveal and explicitly defer rotation, bulk management,
      advanced scoping UX, and full credential lifecycle parity.
    tests:
      - npm test -- --run src/App.test.tsx -t "creates an api key": passed (1 passed; 169 skipped) after the carry-over TDD test was added before implementation
      - npm test -- --run src/App.test.tsx -t "restores an existing local session": passed (1 passed; 169 skipped) for the corrected empty-state copy
      - npm test -- --run src/App.test.tsx -t "api key": passed (4 passed; 166 skipped)
      - npm test -- --run src/App.test.tsx: passed (170 passed)
      - npm run build: passed
      - git diff --check: passed
      - python3 doc/ui truth checks for auth acceptance, settings acceptance, parity-gap report, final audit, and empty-state copy: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed independent read-only re-review after fixing stale auth/report/empty-state wording; reviewer approved the minimal create/inventory/revoke scope, one-time secret reveal, fail-closed load-error controls, bounded docs, and focused verification with no remaining blocking issues
    commit: 21abcd9527f46854d6f7fae8207d32dc14c6cdc3
    next_task: task94b
  - id: task93c
    status: done
    summary: >-
      Closed one meaningful Task 93 Phase B ask/chat lifecycle slice instead of
      splitting backend DELETE wiring, frontend chat-route delete controls, stale
      async response hardening, focused Rust/Vitest coverage, acceptance wording,
      and parity-report truth updates into smaller paperwork-only runs.
      Authenticated callers can now delete their own currently visible ask thread
      through `DELETE /api/v1/ask/threads/{thread_id}`, hidden/out-of-scope/
      missing/other-user threads fail closed without removal, and the dedicated
      `#/chat` route exposes a selected-thread delete control that removes the
      summary/detail, preserves repo scope while clearing `thread_id`, and ignores
      stale delete outcomes after switching threads. Docs keep the claim bounded
      to thread rename/private-shared visibility/delete lifecycle and explicitly
      defer archive controls, richer conversation/source-preview UX, streaming/
      progress states, and full chat parity.
    tests:
      - cargo test -p sourcebot-api --bin sourcebot-api ask_threads_delete_removes_only_authenticated_visible_thread -- --nocapture: RED failed first with 405 before route implementation, then passed (1 passed; 360 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api ask_threads_ -- --nocapture: passed (5 passed; 356 filtered; selector also matched the storage migration inventory test)
      - cargo fmt --check: passed
      - npm test -- --run src/App.test.tsx -t "chat route deletes the selected thread": RED failed first before the delete control existed, then passed (1 passed; 168 skipped)
      - npm test -- --run src/App.test.tsx -t "chat route ignores stale delete responses": passed after reviewer-forced stale pending-state hardening (1 passed; 168 skipped)
      - npm test -- --run src/App.test.tsx -t "chat route": passed (8 passed; 161 skipped)
      - npm test -- --run src/App.test.tsx: passed (169 passed)
      - npm run build: passed
      - git diff --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed independent read-only re-review after fixing the reviewer-found blocking issue where a stale DELETE response after thread switch could leave `deleteSubmitting` stuck; reviewer approved the caller-owned visible-thread backend delete gate, no-content citation-sanitization posture, stale delete response guard, focused tests, and bounded docs with no remaining blocking issues
    commit: 1ea887271a2b7b78bd5be59fd06cb148d23c4ab6
    next_task: task94
  - id: task93b
    status: done
    summary: >-
      Closed one meaningful Task 93 Phase B frontend lifecycle slice instead of
      splitting chat-route controls, stale-response hardening, focused Vitest
      coverage, acceptance wording, and parity-report truth updates into smaller
      paperwork-only runs. The dedicated `#/chat` route now lets a user rename
      the selected visible ask thread and switch private/shared visibility through
      the authenticated PATCH lifecycle API, refreshes the visible detail/header
      and summary state from the sanitized response, keeps the hash pinned to the
      same chat thread, and ignores stale metadata responses after the user switches
      to another thread. Docs keep the claim bounded to focused frontend
      rename/visibility controls and explicitly defer delete/archive lifecycle,
      richer conversation/source-preview UX, streaming/progress states, and full
      chat parity.
    tests:
      - npm test -- --run src/App.test.tsx -t "chat route updates the selected thread title": passed (1 passed; 166 skipped)
      - npm test -- --run src/App.test.tsx -t "chat route ignores stale thread metadata responses": passed (1 passed; 166 skipped)
      - npm test -- --run src/App.test.tsx -t "chat route": passed (6 passed; 161 skipped)
      - npm test -- --run src/App.test.tsx: passed (167 passed)
      - npm run build: passed
      - git diff --check: passed
      - python3 raw-content doc truth checks for ask acceptance, parity-gap report, and final audit wording: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved stale PATCH response invalidation, request body/UI lifecycle correctness, bounded acceptance/parity wording, and absence of blocking React state, TypeScript, XSS/security, or scope-creep issues
    commit: 566e046fa58b25fc6386ec1c6b1ab3aa5aa016f5
    next_task: task93c
  - id: task93a
    status: done
    summary: >-
      Closed one meaningful Task 93 Phase B backend/API slice instead of splitting
      route wiring, permission validation, citation sanitization, focused Rust regression,
      acceptance wording, and parity-report truth updates into smaller paperwork-only runs.
      `PATCH /api/v1/ask/threads/{thread_id}` now lets an authenticated caller update a
      currently visible ask thread's title and private/shared visibility metadata, trims
      titles, rejects empty/no-op/unknown-visibility bodies, fails closed for hidden or
      out-of-scope threads before mutating, and returns the same citation-sanitized thread
      response shape as the existing detail read. Docs keep the claim bounded to backend
      title/visibility metadata updates and explicitly defer delete plus frontend
      rename/visibility controls, richer conversation UX, source-preview behavior, and
      full ask/chat parity.
    tests:
      - cargo test -p sourcebot-api --bin sourcebot-api ask_threads_patch_updates_title_and_visibility_for_visible_thread_only -- --nocapture: RED failed first with 405 before route implementation, RED failed again after independent review exposed unsanitized PATCH citations, then passed after the shared response-sanitization fix (1 passed; 359 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api ask_threads_ -- --nocapture: passed (4 passed; 356 filtered)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 raw-content doc truth checks for ask acceptance, acceptance index, parity-gap report, and final audit wording: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed independent read-only re-review after fixing the reviewer-found blocking issue that PATCH returned unsanitized persisted citations; reviewer approved the visibility gate, validation, shared citation-filtering helper, bounded regression, and scoped docs with no remaining blocking issues
    commit: f55ee6826f46400fe4920a33604530d72b757a05
    next_task: task93b
  - id: task92
    status: done
    summary: >-
      Closed one meaningful Task 92 Phase B slice instead of splitting binary/source-view
      contract alignment, focused Rust regressions, acceptance wording, and parity-report
      truth updates into smaller paperwork-only runs. The backend blob contract now reports
      non-UTF-8 local and revisioned blobs with path/size metadata, empty text content, and
      `is_binary: true` instead of failing UTF-8 decoding or lossy-rendering source. The
      retrieval-tool `ReadFileResult` carries the same marker, and revisioned reference
      scanning skips binary blobs before iterating source lines. Docs keep the claim bounded
      to backend/API binary metadata and explicitly defer frontend binary/download affordances,
      large-file handling, syntax-highlighting depth, richer branch/ref UX, and full code-nav
      parity.
    tests:
      - cargo test -p sourcebot-core read_file_tool_returns_blob_contents_for_active_repo -- --nocapture: RED failed as expected before close-out because `ReadFileResult` lacked the `is_binary` field required by the binary/source-view contract alignment
      - cargo test -p sourcebot-core read_file_tool_ -- --nocapture: passed (5 passed; 19 filtered)
      - cargo test -p sourcebot-api local_browse_store_returns_binary_blob_metadata -- --nocapture: passed (2 bin tests; lib selector matched 0 tests)
      - cargo test -p sourcebot-api repo_blob_returns_ -- --nocapture: passed (6 bin tests; lib selector matched 0 tests)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 raw-content doc/state truth checks for browse acceptance, parity reports, and roadmap-state task92/task93/SHA wording: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed independent read-only review; reviewer approved consistent `is_binary` propagation across BlobResponse, RepositoryBlob, ReadFileResult, BrowseBlobStoreAdapter, local/revisioned byte reads, binary reference-scan skipping, and bounded docs with no blocking issues
    commit: 0503b55138771774dd29c5924847d2d2a4c79e50
    next_task: task93
  - id: task91
    status: done
    summary: >-
      Closed one meaningful Task 91 Phase B slice instead of splitting saved-context
      lifecycle implementation, search-contract hardening, acceptance wording, and
      parity-report truth updates into smaller paperwork-only runs. `/api/v1/auth/search-contexts`
      now supports authenticated create/list/delete for caller-owned saved contexts, and
      `/api/v1/search?context_id=...` narrows results to that caller-owned context's
      currently visible repository scope. Unknown, other-user, blank, or visible-empty
      contexts fail closed, and explicit `repo_id` outside the saved context scope is
      rejected instead of broadening search. Docs keep the claim bounded to backend/API
      file-backed behavior, not frontend UI, SQL-backed durability, richer grammar,
      relevance tuning, or stable pagination parity.
    tests:
      - cargo test -p sourcebot-api --bin sourcebot-api auth_search_contexts_post_creates_context_for_current_user_and_hides_stale_repo_ids -- --nocapture: passed (1 passed; 356 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api auth_search_contexts_delete_removes_only_callers_own_context -- --nocapture: passed (1 passed; 356 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api search_context_id_narrows_results_and_explicit_repo_must_be_in_context -- --nocapture: RED failed as expected for blank `context_id` broadening search before the fix, then passed after fail-closed handling was added (1 passed; 356 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api search_context -- --nocapture: passed (6 passed; 351 filtered)
      - cargo test -p sourcebot-api --bin sourcebot-api search_ -- --nocapture: passed (16 passed; 341 filtered)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 raw-content doc truth checks for search acceptance and parity reports: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed independent read-only re-review after fixing the reviewer-found blank-`context_id` broadening bug; reviewer approved caller-owned create/list/delete, visibility/API-key scope intersection, visible-empty fail-closed behavior, explicit repo/context narrowing, and bounded docs with no remaining blocking issues
    commit: 10ceaf2aa52d4a14b764ee33693a06dc1815db36
    next_task: task92
  - id: task90
    status: done
    summary: >-
      Closed one meaningful Task 90 Phase B slice instead of splitting indexed-search
      implementation, index-status API wiring, acceptance wording, and verification into
      smaller paperwork-only runs. `sourcebot-search` now builds a startup-time in-memory
      line index for configured local repository roots, serves search from that prebuilt
      index, records per-repository indexed/error status plus indexed-file, indexed-line,
      and skipped-item counts, and exposes those counts at `/api/v1/repos/{repo_id}/index-status`
      behind the existing repository-visibility gate. README and repository-operations
      acceptance wording explicitly keep the claim bounded: no Tantivy, persistent index,
      background reindexing, queue depth, retries, failure recovery, frontend management
      controls, or production-grade indexing parity yet.
    tests:
      - cargo test -p sourcebot-search local_search_store -- --nocapture: passed (4 passed)
      - cargo test -p sourcebot-api repository_index_status_reports_visible_repo_and_fails_closed_for_hidden_repo -- --nocapture: passed (1 bin test; lib selector matched 0 tests)
      - cargo test -p sourcebot-api search_api_key_filters_unscoped_results_to_repo_scope -- --nocapture: passed (1 bin test; lib selector matched 0 tests)
      - cargo test -p sourcebot-search -- --nocapture: passed (9 passed)
      - cargo fmt --check: passed
      - git diff --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed one independent read-only post-implementation review; reviewer approved the startup-built in-memory line index, visibility-gated repository index-status route, search response compatibility, skipped-heavy-directory behavior, and bounded non-overclaiming docs with no blocking issues
    commit: e0d96f945ffdc2a3f9bc3d67bf078ed395f2b5bc
    next_task: task91
  - id: task89
    status: done
    summary: >-
      Closed one meaningful Task 89 operator/recovery smoke slice instead of splitting
      endpoint implementation, file-backed/PostgreSQL readiness regressions, smoke-matrix
      wiring, acceptance docs, and parity-report wording into smaller paperwork runs.
      `/readyz` now reports file-backed local metadata readiness when `DATABASE_URL` is
      absent and fail-closed PostgreSQL metadata readiness when a bounded connect plus
      SQLx `_sqlx_migrations` inventory read cannot complete. The smoke matrix and
      acceptance/parity docs ground the bounded readiness baseline without overclaiming
      production dependency health, supervised workers, complete migration automation,
      richer observability, or upgrade orchestration.
    tests:
      - repo-local PostgreSQL preflight with bundled binaries plus `.env`-backed `DATABASE_URL` / `TEST_DATABASE_URL` connectivity probes: passed with server already running as `hermes` and both configured URLs probed with bundled `psql` via the ELF loader
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api readyz_ -- --nocapture --test-threads=1: passed (3 passed)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' make sqlx-test: passed
      - cargo fmt --check: passed
      - bash scripts/check_end_to_end_smoke_matrix.sh /opt/data/projects/sourcebot-rewrite: passed
      - bash scripts/check_metadata_bootstrap_docs.sh README.md: passed
      - bash scripts/check_local_metadata_env_contract.sh .: passed
      - git diff --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed independent read-only review after fixing the reviewer-found blocking issue that the migration-inventory query was outside the readiness timeout; reviewer approved the bounded connect-plus-query timeout, liveness/readiness separation, fail-closed PostgreSQL behavior, and scoped docs/smoke wording with no remaining blocking issues
    commit: 5ab2876d8f6ba7f196d3bc8d0dd789546ca19707
    next_task: task90
  - id: task88c
    status: done
    summary: >-
      Closed one meaningful Task 88 durable review-webhook delivery-attempt slice instead of
      splitting PostgreSQL attempt-store wiring, fractional timestamp/fallback hardening,
      SQLx-wrapper contract wording, and truthful parity docs into smaller paperwork runs.
      `crates/api/src/auth.rs` now loads and stores `review_webhook_delivery_attempts`
      through `PgOrganizationStore` when `DATABASE_URL` is configured, preserves UTC
      fractional `accepted_at` precision on PostgreSQL round trips, keeps unrelated
      organization aggregate fields file-backed, and avoids clearing file-backed delivery
      attempts until the PostgreSQL transaction succeeds. README, Makefile help text, paired
      checker scripts, and the parity-gap report now ground this bounded delivery-attempt
      durability baseline without overclaiming richer review-agent orchestration, retries,
      analytics/audit, provider runtime, local repository import, or full organization
      aggregate durability.
    tests:
      - repo-local PostgreSQL preflight with bundled binaries plus `.env`-backed `DATABASE_URL` / `TEST_DATABASE_URL` connectivity probes: passed with server already running as `hermes` and both configured URLs probed with bundled `psql` via the ELF loader
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --lib pg_organization_store_delivery_attempts_preserve_ -- --nocapture --test-threads=1: passed (2 passed after the expected RED failure exposed fractional timestamp truncation and fallback clearing on PostgreSQL upsert failure)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api pg_organization_store_delivery_attempts_ -- --nocapture --test-threads=1: passed (3 lib tests + 3 bin tests)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api pg_organization_store_ -- --nocapture --test-threads=1: passed (9 lib tests + 9 bin tests)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' make sqlx-test: passed
      - cargo fmt --check: passed
      - bash scripts/check_sqlx_test_workflow.sh .: passed
      - bash scripts/check_metadata_bootstrap_docs.sh README.md: passed
      - bash scripts/check_local_metadata_env_contract.sh .: passed
      - git diff --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed one independent read-only post-implementation review; reviewer approved the PostgreSQL-backed delivery-attempt store/read path, fractional accepted_at preservation, transaction-before-fallback-clearing failure behavior, docs/checker wording, and bounded non-overclaiming scope with no blocking security or logic issues
    commit: 87bd7b66adac249212dcb2d7798a23912405a5ec
    next_task: task89
  - id: task88b
    status: done
    summary: >-
      Closed one meaningful Task 88 durable review-agent slice instead of splitting
      PostgreSQL run-store wiring, focused lifecycle regressions, SQLx-wrapper contract wording,
      and truthful parity docs into smaller paperwork runs. `crates/api/src/auth.rs` now loads
      and stores `review_agent_runs` through `PgOrganizationStore` when `DATABASE_URL` is
      configured, keeps unrelated organization aggregate fields file-backed, claims the oldest
      queued run with PostgreSQL row locking, completes/fails only claimed runs, and preserves
      terminal completed/failed status against stale writes. README, Makefile help text, paired
      checker scripts, and the parity-gap report now ground this bounded review-agent-run
      lifecycle baseline without overclaiming delivery-attempt durability, richer orchestration,
      retries, analytics/audit, or full organization aggregate durability.
    tests:
      - repo-local PostgreSQL preflight with bundled binaries plus `.env`-backed `DATABASE_URL` / `TEST_DATABASE_URL` connectivity probes: passed after restarting the repo-local server as `hermes` and probing both configured URLs with bundled `psql` via the ELF loader
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api pg_organization_store_review_agent_runs_ -- --nocapture --test-threads=1: passed (2 passed; RED phase previously failed as expected before implementation)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api pg_organization_store_ -- --nocapture --test-threads=1: passed (6 lib tests + 6 bin tests; selector includes the 2 new review-agent-run tests in each target)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' make sqlx-test: passed
      - cargo fmt --check: passed
      - bash scripts/check_sqlx_test_workflow.sh .: passed
      - bash scripts/check_metadata_bootstrap_docs.sh README.md: passed
      - bash scripts/check_local_metadata_env_contract.sh .: passed
      - git diff --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed one independent read-only post-implementation review; reviewer approved the PostgreSQL-backed review-agent-run lifecycle store, stale-write terminal preservation, docs/checker wording, and bounded non-overclaiming scope with no blocking security or logic issues
    commit: c8378ff5288c5bcfcfa12767a61257597bbacb7f
    next_task: task88c
  - id: task88a
    status: done
    summary: >-
      Closed the first meaningful Task 88 durable ask lifecycle slice instead of splitting
      migration inventory, PostgreSQL ask-thread store wiring, SQLx wrapper coverage, and
      acceptance/docs wording into smaller paperwork runs. `crates/api/src/ask.rs` now provides
      `PgAskThreadStore` for ask-thread metadata and message arrays when `DATABASE_URL` is
      configured, `crates/api/src/main.rs` selects that durable store at API startup, migration
      `0011` adds bounded `ask_threads.messages` storage, and the README/Makefile/checker
      contract now includes the focused ask-thread regression. This grounds durable thread
      create/append/list/detail owner-scoped reads without overclaiming review-agent
      persistence, richer ask/chat lifecycle parity, provider runtime behavior, analytics/audit,
      local repository import, or full organization aggregate durability.
    tests:
      - repo-local PostgreSQL preflight with bundled binaries plus `.env`-backed `DATABASE_URL` / `TEST_DATABASE_URL` connectivity probes: passed with server already running as `hermes`
      - cargo test -p sourcebot-api --lib migration_inventory -- --nocapture: passed (1 passed after the expected RED failure exposed the missing 0011 inventory entry)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api pg_ask_thread_store_ -- --nocapture --test-threads=1: passed (1 passed)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' make sqlx-test: passed
      - cargo fmt --check: passed
      - bash scripts/check_sqlx_test_workflow.sh .: passed
      - bash scripts/check_metadata_bootstrap_docs.sh README.md: passed
      - bash scripts/check_local_metadata_env_contract.sh .: passed
      - git diff --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed one independent read-only post-implementation review; reviewer approved the PostgreSQL-backed ask-thread/message migration, store implementation, startup selection, migration inventory, and docs/checker scope with no blocking issues
    commit: d6c429edd4f4cfb4c12cc3df0b9af46d45b8aa1a
    next_task: task88b
  - id: task87c4
    status: done
    summary: >-
      Closed one meaningful Task 87c4 durable organization/runtime slice instead of splitting
      SQL migration `0010`, PostgreSQL organization-store wiring, atomic sync-job claim/complete
      behavior, worker/API routing, SQLx wrapper coverage, and truthful docs/checkers into
      smaller paperwork runs. `crates/api/src/auth.rs` now provides `PgOrganizationStore`
      for repository-sync-job rows when `DATABASE_URL` is configured, keeps the broader
      organization aggregate file-backed for fields not yet moved, preserves terminal sync-job
      progress against stale whole-state writes, and offers a transactional claim-and-complete
      path that avoids an observable intermediate `running` write for one-shot workers.
      The new migration, API/worker wiring, README, Makefile, and checker updates ground this
      bounded lifecycle baseline without overclaiming full organization aggregate, ask-thread,
      review-agent, analytics/audit, local repository import, or provider runtime durability.
    tests:
      - repo-local PostgreSQL preflight with bundled binaries plus `.env`-backed `DATABASE_URL` / `TEST_DATABASE_URL` connectivity probes: passed with server already running as `hermes`
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api pg_organization_store_ -- --nocapture --test-threads=1: passed (4 lib tests + 4 bin tests)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' make sqlx-test: passed
      - cargo fmt --check: passed
      - bash scripts/check_sqlx_test_workflow.sh .: passed
      - bash scripts/check_metadata_bootstrap_docs.sh README.md: passed
      - bash scripts/check_local_metadata_env_contract.sh .: passed
      - bash scripts/check_local_metadata_dev_bootstrap_contract.sh .: passed
      - git diff --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed one independent read-only post-implementation review; reviewer found the PostgreSQL-backed repository-sync-job lifecycle migration, `PgOrganizationStore` implementation, API/worker wiring, and docs/checker scope consistent with the bounded task and no blocking issues
    commit: 5dbfb03237bebb34daadf462427cc72fdce20bc5
    next_task: task88
  - id: task87c3
    status: done
    summary: >-
      Closed one meaningful Task 87c3 durable organization-auth slice instead of splitting
      route authorization, a PostgreSQL-backed regression, SQLx wrapper wiring, and
      acceptance/docs contract updates into smaller paperwork runs. `/api/v1/auth/repository-sync-jobs`
      still reads sync-job records from the configured organization-state store, but when
      `DATABASE_URL` is configured it now filters those records through PostgreSQL-backed
      organization membership and `repository_permission_bindings` so stale file-backed
      organization-auth fixtures cannot leak hidden repository jobs; `make sqlx-test` now
      includes the focused `auth_repository_sync_jobs_` route family, and README plus
      repository-operations acceptance wording ground this bounded permission-filtering
      baseline without overclaiming full repo-permission sync lifecycle, persisted
      index-status, retry/recovery, operator controls, or broader organization aggregates.
    tests:
      - repo-local PostgreSQL preflight with bundled binaries plus `.env`-backed `DATABASE_URL` / `TEST_DATABASE_URL` connectivity probes: passed after stale local Postgres pid/socket recovery and restart as `hermes`
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api auth_repository_sync_jobs_ -- --nocapture --test-threads=1: passed (4 passed)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' make sqlx-test: passed
      - bash scripts/check_sqlx_test_workflow.sh .: passed
      - bash scripts/check_metadata_bootstrap_docs.sh README.md: passed
      - bash scripts/check_local_metadata_env_contract.sh .: passed
      - bash scripts/check_local_metadata_dev_bootstrap_contract.sh .: passed
      - git diff --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      code: passed one independent post-implementation review; reviewer confirmed the diff correctly separates file-backed sync-job records from PostgreSQL-backed visibility metadata, proves hidden jobs are filtered by live repository-permission bindings, and keeps Makefile/README/checker/spec wording within the bounded parity scope
    commit: d1bb9e41b24ccce0fde6715215671b3066c3141f
    next_task: task87c4
  - id: task87c2
    status: done
    summary: >-
      Closed one meaningful Task 87c2 durable-catalog slice instead of splitting the
      PostgreSQL `PgCatalogStore` read implementation, focused catalog regressions, SQLx
      wrapper wiring, and truthful operator/docs contract updates into separate paperwork
      runs. `crates/api/src/storage.rs` now lists repository summaries and repository
      details from PostgreSQL when `DATABASE_URL` is configured, keeps local import
      explicitly unimplemented, and makes destructive catalog tests require
      `TEST_DATABASE_URL`; `Makefile` extends `make sqlx-test` with the catalog selector
      while preserving deterministic test-database wiring; and the README, operator
      acceptance, parity-gap report, and paired checker scripts now ground the bounded
      catalog read baseline without overclaiming local repository import, connection
      management, analytics/audit aggregates, repo-permission sync, broader organization
      aggregates, ask-thread, or review-agent durability.
    tests:
      - repo-local PostgreSQL preflight with bundled binaries plus `.env`-backed `DATABASE_URL` / `TEST_DATABASE_URL` connectivity probes: passed
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api pg_catalog_store_ -- --nocapture --test-threads=1: passed (3 passed)
      - DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api storage::tests -- --nocapture: passed (6 passed)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' make sqlx-test: passed
      - bash scripts/check_sqlx_test_workflow.sh .: passed
      - bash scripts/check_metadata_bootstrap_docs.sh README.md: passed
      - bash scripts/check_local_metadata_env_contract.sh .: passed
      - bash scripts/check_local_metadata_dev_bootstrap_contract.sh .: passed
      - rustfmt --edition 2021 --check crates/api/src/storage.rs: passed
      - git diff --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      spec: passed independent review; reviewer confirmed the live Task 87c2 diff matches the bounded PostgreSQL catalog list/detail contract, keeps docs/checkers truthful, and stays within the directly coupled catalog/readiness surfaces
      code: approved by independent re-review after the `TEST_DATABASE_URL` fail-closed adjustment; reviewer found no blocking security, logic, or spec issues and confirmed the deterministic test-database wiring closes the destructive-test safety concern
    commit: 5c603e3802dfc208caa1fe82b8f591246dc71385
    next_task: task87c3
  - id: task87c1
    status: done
    summary: >-
      Closed one meaningful Task 87c auth/admin durability slice instead of splitting SQL
      migration `0009`, PostgreSQL auth-metadata helpers, route wiring, focused SQLx/auth
      regressions, and truthful docs into separate paperwork runs. `crates/api/src/auth.rs`
      now stores and loads API-key and OAuth-client metadata from PostgreSQL, including
      bearer-auth repo-scope validation and admin-visible OAuth-client inventory/create
      behavior; `crates/api/src/main.rs` routes `/api/v1/auth/api-keys`,
      `/api/v1/auth/api-keys/{id}/revoke`, API-key bearer auth, and
      `/api/v1/auth/oauth-clients` through that durable path whenever `DATABASE_URL` is
      configured while preserving the file-backed fallback without `DATABASE_URL`; and the
      SQLx wrapper plus README/auth/parity wording now ground that narrower shipped baseline
      without overclaiming catalog persistence, analytics/audit aggregates, repo-permission
      durability, or broader whole-aggregate organization state.
    tests:
      - repo-local PostgreSQL preflight with bundled binaries plus `.env`-backed `DATABASE_URL` / `TEST_DATABASE_URL` connectivity probes: passed
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' make sqlx-test: passed
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api auth_api_key_ -- --nocapture --test-threads=1: passed (10 passed)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api auth_oauth_clients_ -- --nocapture --test-threads=1: passed (9 passed)
      - cargo test -p sourcebot-api --lib migration_inventory -- --nocapture: passed (1 passed)
      - git diff --check: passed
      - git diff --cached --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (3 test-fixture false positives only: `pg-unscoped-secret`, `pg-secret`, `pg-rollback-secret`; no production credential findings)
    review:
      spec: passed one independent post-implementation review; reviewer confirmed the live Task 87c carry-over matches the bounded plan, keeps docs truthful, and treats the `lib.rs` storage export plus repo-permission hydration as directly supportive rather than unrelated drift
      code: approved by one independent post-implementation review; reviewer found no blocking security or logic defects in the PostgreSQL API-key/OAuth-client durability diff, noted the simple secret-regex hits were test fixtures only, and raised only a non-blocking caution that the new `lib.rs` export slightly widens the crate's internal library surface
    commit: d9a0c5bdeb461fc4ae414c217559811cffe0179b
    next_task: task87c2
  - id: task87b2
    status: done
    summary: >-
      Closed one meaningful Task 87b2 auth/org slice instead of splitting PostgreSQL metadata
      helpers, router wiring, focused SQLx regressions, and truthful docs into separate
      paperwork runs. `crates/api/src/auth.rs` now provides a PostgreSQL-backed organization-auth
      metadata store for local-account lookup, admin/user membership visibility, and invite
      redemption with fail-closed duplicate/expired invite handling; `crates/api/src/main.rs`
      routes `/api/v1/auth/login`, `/api/v1/auth/me`, `/api/v1/auth/members`,
      `/api/v1/auth/linked-accounts`, and `/api/v1/auth/invite-redeem` through that durable
      metadata path whenever `DATABASE_URL` is configured while preserving the file-backed
      fallback without `DATABASE_URL`; and the `make sqlx-test` wrapper plus README/auth/parity
      wording now ground that narrower shipped baseline without overclaiming API-key,
      OAuth-client, catalog, connection, analytics, audit-event, or broader organization-aggregate
      durability.
    tests:
      - repo-local PostgreSQL preflight with bundled binaries plus `.env`-backed `DATABASE_URL` / `TEST_DATABASE_URL` connectivity probes: passed
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' make sqlx-test: passed
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api auth_me_ -- --nocapture --test-threads=1: passed (9 passed)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api auth_login_ -- --nocapture --test-threads=1: passed (1 passed)
      - sh scripts/check_local_metadata_env_contract.sh .: passed
      - bash scripts/check_metadata_bootstrap_docs.sh: passed
      - bash scripts/check_sqlx_test_workflow.sh: passed
      - git diff --check: passed
      - git diff --cached --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      spec: passed one independent post-implementation review; reviewer confirmed the live Task 87b2 carry-over stays within the bounded plan by adding the PostgreSQL local-account/membership/invite metadata helper, routing only the targeted auth/org endpoints through it when `DATABASE_URL` is configured, and updating the verification/docs wording without widening into API-key, OAuth-client, catalog, or broader organization-aggregate work
      code: approved by one independent post-implementation review; reviewer found no blocking security or logic defects in the live Task 87b2 diff after the focused SQLx/auth/doc-contract verification and only noted a non-blocking suggestion to document the `TEST_DATABASE_URL` requirement a bit more explicitly for ad hoc targeted runs
    commit: 7a879c2a1626b6a35ba6fd871f3497196c9cfa93
    next_task: task87c
  - id: task87b1
    status: done
    summary: >-
      Closed one meaningful Task 87b bootstrap slice instead of splitting SQL migration,
      bootstrap-store wiring, legacy upgrade handling, router regressions, and truthful docs
      into separate paperwork runs. `crates/api/src/auth.rs` now provides a PostgreSQL-backed
      bootstrap store keyed by `LOCAL_BOOTSTRAP_ADMIN_USER_ID`; `crates/api/src/main.rs`
      routes bootstrap status/create/login through PostgreSQL whenever `DATABASE_URL` is
      configured; legacy `local_accounts` bootstrap rows without `password_hash` stay
      fail-closed as still requiring bootstrap and can be upgraded in place; and the focused
      SQLx/bootstrap/auth coverage plus README/auth/parity updates ground that narrower
      shipped baseline without overclaiming broader durable bootstrap/account/org parity.
    tests:
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' make sqlx-test: passed
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api bootstrap_ -- --nocapture --test-threads=1: passed (27 passed)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api auth_ -- --nocapture --test-threads=1: passed (100 passed)
      - python3 raw-content doc truth checks for README/auth acceptance/parity report: passed
      - bash scripts/check_local_metadata_env_contract.sh .: passed
      - bash scripts/check_metadata_bootstrap_docs.sh: passed
      - git diff --check: passed
      - git diff --cached --check: passed
      - python3 added-lines security scan over `git diff --cached --unified=0`: passed (0 findings)
    review:
      spec: passed one independent post-implementation review; reviewer confirmed the staged slice matches the bounded Task 87b1 plan by adding the bootstrap password-hash migration, PostgreSQL-backed bootstrap store/wiring, legacy bootstrap-row upgrade handling, focused SQLx/router coverage, and truthful docs without widening into broader durable organization/auth aggregates
      code: approved by one independent post-implementation review; reviewer found no blocking security or logic defects in the staged PostgreSQL bootstrap-admin diff and only noted non-blocking follow-ups around propagating invalid DATABASE_URL as a startup error and possibly fail-closing malformed non-null bootstrap password hashes more explicitly
    commit: 64cdbd9076a6b1f2d7d33a58972dbbfcc6e93b99
    next_task: task87b2
  - id: task87a
    status: done
    summary: >-
      Closed one meaningful Task 87 durable-auth slice instead of splitting SQL session
      storage, router wiring, invited-user regressions, and truthful docs into separate
      paperwork runs. `crates/api/src/auth.rs` now provides a PostgreSQL-backed local-session
      store behind `DATABASE_URL`; `crates/api/src/main.rs` persists the corresponding local
      account before durable bootstrap-login and invite-redemption session writes so a fresh
      API process can still restore `/api/v1/auth/me`; and the focused SQLx/auth coverage plus
      README/auth/parity-report updates ground that narrower shipped baseline without
      overclaiming broader durable bootstrap/account/org/catalog parity.
    tests:
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' make sqlx-test: passed
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api auth_ -- --nocapture: passed (100 passed)
      - TEST_DATABASE_URL='postgres://sourcebot:***@127.0.0.1:5432/sourcebot_test' cargo test -p sourcebot-api --bin sourcebot-api tests::invite_redeem_issues_postgres_backed_local_session_for_invited_account_and_auth_me -- --exact --nocapture --test-threads=1: passed
      - cargo test -p sourcebot-api --bin sourcebot-api tests::invite_redeem_reuses_pending_invite_account_adds_membership_and_returns_session -- --exact --nocapture: passed
      - python3 raw-content doc truth checks for README/auth acceptance/parity report: passed
      - git diff --check: passed
      - git diff --cached --check: passed
      - python3 added-lines security scan over `git show --unified=0 HEAD`: passed (0 findings)
    review:
      spec: passed one independent post-implementation review; reviewer confirmed the staged slice stays within one bounded Task 87a closure, includes the invited-user PostgreSQL regression, and keeps broader durable bootstrap/account/org/catalog parity explicitly deferred
      code: approved by one independent post-implementation review; reviewer found no blocking security or logic defects in the final durable local-session diff after the invite-redemption fix and only noted a non-blocking future cleanup around possible orphaned cross-store local-account rows if a later organization-state write fails
    commit: 25fb9e58791238cc39f011525c6ac19b0da51649
    next_task: task87b
  - id: task86
    status: done
    summary: >-
      Closed one meaningful Task 86 operator-visible harness slice instead of splitting
      Makefile wiring, local bootstrap orchestration, focused contract/smoke coverage, and
      truthful docs into separate paperwork runs. `Makefile` now exposes
      `make metadata-dev-bootstrap`; `scripts/bootstrap_local_metadata_dev.sh` validates the
      local Postgres URL contract, waits for readiness, ensures the dedicated
      `sourcebot_test` database exists, runs `make sqlx-migrate`, and then runs
      `make sqlx-test`; the paired shell checks plus README/operator-maintenance/parity-report
      updates ground that workflow truthfully as a local bootstrap-and-compatibility closure
      rather than a shipped durable runtime baseline.
    tests:
      - bash scripts/check_local_metadata_dev_bootstrap_contract.sh: passed
      - bash scripts/check_local_metadata_dev_bootstrap_smoke.sh: passed
      - bash scripts/check_make_sqlx_migrate.sh: passed
      - bash scripts/check_sqlx_test_workflow.sh: passed
      - bash scripts/check_metadata_bootstrap_docs.sh: passed
      - bash scripts/check_local_metadata_env_contract.sh .: passed
      - cargo test -p sourcebot-config: passed (12 passed)
      - cargo test -p sourcebot-api --bin sourcebot-api storage::tests -- --nocapture: passed (4 passed)
      - git diff --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      spec: passed one independent post-implementation review; reviewer confirmed the live worktree stayed within the intended Task 86 file set, removed the out-of-scope plan artifact, and truthfully closed one bounded metadata-bootstrap harness slice instead of another audit-only fragment
      code: approved by one independent post-implementation review; reviewer found no blocking security or logic defects in the new `make metadata-dev-bootstrap` flow, its contract/smoke checks, or the doc wording after aligning the `DATABASE_URL` behavior with the unfinished lazy `PgCatalogStore` path in `crates/api/src/storage.rs`
    commit: 73ce0cfc1caf8a46c48ab4917c331b8bfa9aa183
    next_task: task87
  - id: roadmap-bootstrap-2026-04-22
    status: done
    summary: >-
      Closed one meaningful post-audit roadmap bootstrap instead of reopening the
      finished 85-task plan or leaving the repo blocked behind a vague "remaining
      blockers" note. `docs/plans/2026-04-22-sourcebot-follow-on-parity-roadmap.md`
      now turns the final parity audit blockers into a focused Task 86-100 follow-on
      roadmap, and `docs/reports/2026-04-22-final-parity-audit-and-release-checklist.md`
      now links that roadmap so future runs can resume from a truthful next
      implementation queue rather than from another paperwork-only audit fragment.
    tests:
      - python3 raw-content checks for follow-on roadmap title/tasks 86-100 and audit link: passed
      - git diff --check: passed
    review:
      spec: passed one independent post-implementation review; reviewer confirmed the follow-on roadmap bootstrap is a meaningful post-audit closure, derives Task 86-100 directly from the Task 85 blockers, and truthfully pivots the repo to the superseding roadmap without reopening the finished 85-task plan
      code: approved by one independent post-implementation review; reviewer found no blocking logic, truthfulness, or security issues after the commit-hash/rule/blocker-note fixes and confirmed the state file now anchors to the real substantive docs commit with Task 86 as the next live slice
    commit: faddb33b52a046987b3fbe8ad1facaa905d70f7a
    next_task: task86
  - id: task85
    status: done
    summary: >-
      Closed one meaningful Phase 11 audit closure instead of leaving Task 85 as an
      unfinished paperwork placeholder. `docs/reports/2026-04-22-final-parity-audit-and-release-checklist.md`
      now maps every `specs/FEATURE_PARITY.md` row to shipped code/tests/docs evidence
      and records the release blockers that still prevent an honest full-parity claim;
      `specs/FEATURE_PARITY.md` no longer contains `Needs audit` / `_TBD_`
      placeholders; and `docs/plans/2026-04-22-task85-final-parity-audit-plan.md`
      records the bounded closure plan. The audit stays truthful that the rewrite is
      not yet ready to claim full Sourcebot-replacement parity, so the roadmap task is
      complete as an audit and checklist closure rather than as a release-certification
      claim.
    tests:
      - python3 raw-content checks for Task 85 plan/audit/matrix placeholders: passed
      - bash scripts/check_end_to_end_smoke_matrix_contract.sh /opt/data/projects/sourcebot-rewrite: passed
      - bash scripts/check_end_to_end_smoke_matrix.sh /opt/data/projects/sourcebot-rewrite: passed
      - git diff --check: passed
      - python3 added-lines security scan over `git diff --unified=0`: passed (0 findings)
    review:
      spec: passed one independent post-implementation review; reviewer confirmed the Task 85 docs stay in scope, remove active matrix placeholders, and explicitly state that full-parity release is not yet ready instead of overclaiming completion
      code: approved by one independent post-implementation review; reviewer found no blocking truthfulness, logic, or security issues in the docs-only audit closure and only noted that the remaining `Needs audit` / `_TBD_` phrases appear as explanatory text inside the new checklist rather than live matrix placeholders
    commit: 888f981621cdca921f5775fbb18ce3da7da79a29
    next_task: null
  - id: task84
    status: done
    summary: >-
      Closed one meaningful Phase 11 operator-visible smoke slice instead of splitting
      the harness, focused contract coverage, doc grounding, and roadmap truth into
      separate paperwork runs. `scripts/check_end_to_end_smoke_matrix.sh` now starts
      the real API against an isolated temp runtime, bootstraps and logs in a local
      admin, exercises authenticated connections/search/ask plus public review-webhook
      intake, runs the real one-shot worker with a stubbed completed outcome, and
      verifies the queued review-agent run reaches `completed`; the paired contract
      script plus focused API/worker coverage keep that local smoke baseline honest;
      and `README.md`, `specs/acceptance/index.md`, `specs/acceptance/journeys.md`,
      `docs/reports/2026-04-18-parity-gap-report.md`, and
      `docs/plans/2026-04-22-task84-end-to-end-smoke-matrix-plan.md` now ground it
      truthfully as a bounded stub-backed operator check rather than full release
      certification.
    tests:
      - bash scripts/check_end_to_end_smoke_matrix_contract.sh /opt/data/projects/sourcebot-rewrite: passed
      - bash scripts/check_end_to_end_smoke_matrix.sh /opt/data/projects/sourcebot-rewrite: passed
      - cargo test -p sourcebot-api review_webhook -- --nocapture: passed (37 passed)
      - cargo test -p sourcebot-api ask_ -- --nocapture: passed (25 passed)
      - cargo test -p sourcebot-worker --test explicit_completed_stub_outcome_smoke -- --nocapture: passed (5 passed)
      - bash -n scripts/check_end_to_end_smoke_matrix.sh: passed
      - bash -n scripts/check_end_to_end_smoke_matrix_contract.sh: passed
      - git diff --check: passed
      - git diff --cached --check: passed
      - python3 raw-content doc truth checks: passed
      - python3 added-lines security scan over `git diff --cached --unified=0`: passed (0 findings)
    review:
      spec: passed one independent post-implementation review; reviewer confirmed the live slice matches the bounded Task 84 plan by shipping one repo-local smoke command across auth, integrations, search, ask, and review-agent plus the required truthful docs without overclaiming full release certification
      code: approved by one independent post-implementation review; reviewer found no blocking security or logic defects after the credential-redaction and non-eval assertion fixes, and only noted optional future regression coverage for the authenticated helper path plus a possible build reuse optimization
    commit: e6244d8671590d7704f189ea3f8da37c14365816
    next_task: task85
  - id: task83
    status: done
    summary: >-
      Closed one meaningful Phase 11 implementation-backed security slice instead of
      splitting audit notes, focused regressions, acceptance wording, and roadmap truth
      into separate paperwork runs. `crates/api/src/main.rs` now routes local-session,
      API-key, and public review-webhook credential checks through one shared
      fail-closed Argon2 burn helper so missing or malformed persisted secret hashes do
      not shortcut verification work, focused regressions plus broad auth/webhook and
      full `sourcebot-api` coverage keep the narrower baseline green, and
      `specs/acceptance/auth.md`, `specs/acceptance/integrations.md`,
      `docs/reports/2026-04-18-parity-gap-report.md`, and
      `docs/plans/2026-04-22-task83-credential-verification-burn-plan.md` now ground
      that bounded credential-hardening baseline truthfully without claiming broader
      auth/session or token-storage parity.
    tests:
      - cargo test -p sourcebot-api revoke_explicitly_targets_requested_local_session_and_fails_closed -- --nocapture: passed
      - cargo test -p sourcebot-api auth_me_returns_401_for_missing_session_record -- --nocapture: passed
      - cargo test -p sourcebot-api auth_me_returns_401_for_invalid_persisted_session_hash -- --nocapture: passed
      - cargo test -p sourcebot-api auth_api_key_helper_fails_closed_for_unknown_api_key -- --nocapture: passed
      - cargo test -p sourcebot-api auth_api_key_helper_fails_closed_for_invalid_persisted_hash -- --nocapture: passed
      - cargo test -p sourcebot-api review_webhook_event_intake_returns_401_for_invalid_persisted_secret_hash -- --nocapture: passed
      - cargo test -p sourcebot-api auth_ -- --nocapture: passed
      - cargo test -p sourcebot-api review_webhook -- --nocapture: passed
      - cargo test -p sourcebot-api: passed (298 passed)
      - git diff --check: passed
      - git diff --cached --check: passed
      - python3 raw-content doc truth checks: passed
      - python3 added-lines security scan over `git diff --cached --unified=0`: passed with 1 reviewed test-fixture false positive (`let password = "correct horse battery staple";`)
    review:
      spec: passed one independent post-implementation review; reviewer confirmed the live slice matches the bounded Task 83 credential-burn plan, keeps the changed files in scope, closes the required tests/docs in one run, and avoids broader auth redesign
      code: approved by one independent post-implementation review; reviewer found no blocking security or logic defects in the staged diff and treated the lone password-string regex hit as a test-fixture false positive rather than a production secret leak
    commit: e85a26ae08d789f82632984183cd1001116b8c45
    next_task: task84
  - id: task82
    status: done
    summary: >-
      Closed one meaningful Phase 11 implementation-backed slice instead of another
      audit-only or over-broad search-parity pass. `crates/api/src/browse.rs` now uses
      one shared ignored-directory policy so recursive local browse `glob` and `grep`
      skip `.git`, `target`, `node_modules`, and `dist`; focused regressions plus a
      representative multi-repo adapter smoke prove visible source hits still surface
      while obvious build-artifact directories do not; and
      `specs/acceptance/integrations.md`,
      `specs/repo-git-search-fixture-layout.md`,
      `docs/reports/2026-04-18-parity-gap-report.md`, and
      `docs/plans/2026-04-22-task82-browse-retrieval-scale-sanity-plan.md` now ground
      that practical browse/MCP retrieval scale guardrail truthfully without claiming
      broader indexing or performance parity.
    tests:
      - cargo test -p sourcebot-api glob_paths_skips_common_build_artifact_directories -- --nocapture: passed
      - cargo test -p sourcebot-api grep_skips_common_build_artifact_directories -- --nocapture: passed
      - cargo test -p sourcebot-api browse_glob_and_grep_scale_sanity_skip_ignored_dirs_across_multiple_repositories -- --nocapture: passed
      - cargo test -p sourcebot-api browse:: -- --nocapture: passed (19 passed)
      - cargo test -p sourcebot-api: passed (320 passed)
      - git diff --check: passed
      - git diff --cached --check: passed
      - python3 raw-content doc truth checks: passed
      - python3 added-lines security scan over `git diff --cached --unified=0`: passed (0 findings)
    review:
      spec: passed one independent post-implementation review; reviewer confirmed the slice stays narrowly scoped to local browse retrieval scale sanity, closes implementation plus focused tests plus the representative multi-repo smoke and docs in one run, and keeps broader indexing/performance parity explicitly deferred
      code: approved by one independent post-implementation review; reviewer found no blocking security or logic defects in the staged diff and only noted the non-blocking tradeoff that explicit `target/` browse `glob`/`grep` queries now fail closed because recursive traversal is intentionally pruned at ignored directories
    commit: e7b68e061a35340926dc2a82b62805a6e4986c90
    next_task: task83
  - id: task81b
    status: done
    summary: >-
      Closed the next meaningful Task 81 operator-visible slice instead of splitting
      helper scripts, focused smoke coverage, runbook truthfulness, and acceptance/report
      updates into separate paperwork runs. `scripts/backup_local_metadata_db.sh` and
      `scripts/restore_local_metadata_db.sh` now add local-only Postgres metadata
      dump/restore helpers guarded by scheme, localhost, and manifest-match checks;
      `Makefile` exposes `metadata-backup` and `metadata-restore`; and
      `scripts/check_local_metadata_backup_contract.sh`,
      `scripts/check_local_metadata_backup_restore_smoke.sh`,
      `scripts/check_operator_maintenance_contract.sh`, `README.md`,
      `specs/acceptance/operator-maintenance.md`, `specs/acceptance/index.md`,
      `specs/acceptance/journeys.md`,
      `docs/reports/2026-04-18-parity-gap-report.md`, and
      `docs/plans/2026-04-22-task81b-local-metadata-backup-plan.md` now ground that
      widened maintenance baseline truthfully while still explicitly deferring broader
      durable-surface, readiness, and production deployment guarantees.
    tests:
      - bash scripts/check_local_metadata_backup_contract.sh /opt/data/projects/sourcebot-rewrite: passed
      - bash scripts/check_local_metadata_backup_restore_smoke.sh /opt/data/projects/sourcebot-rewrite: passed
      - bash scripts/check_operator_maintenance_contract.sh /opt/data/projects/sourcebot-rewrite: passed
      - bash scripts/check_local_metadata_env_contract.sh /opt/data/projects/sourcebot-rewrite: passed
      - git diff --check: passed
      - git diff --cached --check: passed
      - python3 added-lines security scan over `git diff --cached --unified=0`: passed (0 findings)
    review:
      spec: passed one independent post-implementation review; reviewer confirmed the live slice adds the intended local-only metadata backup/restore capability, keeps `make dev-up` ordered before metadata backup in the runbook, and closes scripts/tests/docs/reporting together in one honest Task 81 closure instead of another docs-only or audit-only fragment
      code: approved by one independent post-implementation review; reviewer found no blocking security or logic defects in the staged diff and only suggested an optional future manifest schema/version field for clearer forward-compatibility failures
    commit: d31eb6127bb1de73d8c8dc783d02e8b4b7934360
    next_task: task82
  - id: task81a
    status: done
    summary: >-
      Pivoted Task 81 away from an over-broad backup/restore/migration parity ask and
      closed one meaningful operator-visible slice instead of splitting contract,
      docs, backup tooling, restore tooling, and focused verification into separate
      paperwork runs. `scripts/backup_local_runtime_state.sh` and
      `scripts/restore_local_runtime_state.sh` now let operators capture and restore
      the current file-backed runtime state, `Makefile` exposes `runtime-backup` and
      `runtime-restore`, `scripts/check_operator_maintenance_contract.sh` plus
      `scripts/check_runtime_backup_uniqueness.sh` prove the contract and collision-
      safe backup path, and `README.md`, `specs/acceptance/operator-maintenance.md`,
      `specs/acceptance/index.md`, `specs/acceptance/journeys.md`,
      `docs/reports/2026-04-18-parity-gap-report.md`, and
      `docs/plans/2026-04-22-task81a-operator-maintenance-baseline-plan.md` now
      ground the shipped local maintenance baseline truthfully while explicitly
      deferring durable metadata backup/restore parity, readiness checks, and
      production-grad

... [OUTPUT TRUNCATED - 420957 chars omitted out of 470957 total] ...

b1
    status: done
    summary: >-
      Split task25c2b2b and completed the first follow-up local-session slice by
      adding POST /api/v1/auth/logout, extending the local-session store with
      minimal deletion support, revoking only the currently authenticated Bearer
      <session_id>:<session_secret> session, returning 204 No Content on success,
      and leaving revoked, missing, malformed, or otherwise invalid tokens
      fail-closed without adding session listing or arbitrary revocation yet.
    tests:
      - cargo test -p sourcebot-api file_local_session_store_deletes_only_the_requested_session -- --nocapture: passed
      - cargo test -p sourcebot-api logout_ -- --nocapture: passed (2 tests)
      - cargo test -p sourcebot-api auth_ -- --nocapture: passed (5 tests)
      - cargo test -p sourcebot-api login_ -- --nocapture: passed (4 tests)
      - cargo fmt --all --check: passed
      - cargo check: passed
    review:
      code: approved by one independent post-implementation review; reviewer confirmed the logout slice is spec-compliant for the planned scope, found no blocking security or logic issues, and noted only a non-blocking follow-up to lock in second-logout behavior after revocation
    commit: 0d70cbc1561fe9165e1e807b4296610d6eeb43d8
    next_task: task25c2b2b2
  - id: task25c2b2a
    status: done
    summary: >-
      Split task25c2b2 and completed the first follow-up auth slice by adding
      GET /api/v1/auth/me plus minimal local-session request handling that reads
      Bearer <session_id>:<session_secret> credentials from Authorization,
      verifies the persisted Argon2-hashed session secret, fails closed for
      missing, malformed, invalid, or bootstrap-missing auth state, and returns
      the persisted bootstrap-admin identity without adding logout or revocation
      yet.
    tests:
      - cargo test -p sourcebot-api auth_me -- --nocapture: passed (5 tests)
      - cargo test -p sourcebot-api login_ -- --nocapture: passed (4 tests)
      - cargo test -p sourcebot-api bootstrap -- --nocapture: passed (16 tests)
      - cargo fmt --all --check: passed
      - cargo check: passed
    review:
      code: approved by one independent post-implementation review; reviewer confirmed the slice is spec-compliant for this scope, found no blocking security or logic issues, and noted only a non-blocking missing-session-id path test follow-up
    commit: 8d473705c81ede770828deeacf6b10d9671ef9c4
    next_task: task25c2b2b
  - id: task25c2b1
    status: done
    summary: >-
      Split task25c2b and completed the first login slice by adding
      POST /api/v1/auth/login, reading the persisted bootstrap-admin state,
      returning 409 Conflict while bootstrap is still required, verifying the
      stored Argon2 password hash without trimming password bytes, issuing a
      durable local session through the existing file-backed local session
      store, and persisting only an Argon2 hash of the returned session secret
      with a non-counter-based session id.
    tests:
      - cargo test -p sourcebot-api login_ -- --nocapture: passed (4 tests)
      - cargo test -p sourcebot-api: passed (109 tests)
      - cargo fmt --check: passed
      - cargo check: passed
    review:
      code: approved by one independent confirming review after one fix; reviewer confirmed the slice uses persisted bootstrap state, keeps scope to the single login endpoint, persists only hashed session secrets, preserves password whitespace semantics, and avoids resettable counter-based session ids; noted only non-blocking email-enumeration and extra invalid-payload coverage follow-ups
    commit: ace08961e6ce5c536612e63f8fee7c695ec73eeb
    next_task: task25c2b2
  - id: task25c2a
    status: done
    summary: >-
      Split task25c2 and completed the first slice by adding shared
      local-session models plus a LocalSessionStore contract, wiring a
      dedicated file-backed local session store through config and app state,
      and persisting session records with owner-only files,
      overwrite-by-session-id behavior, concurrent-write locking,
      invalid-JSON fail-closed semantics, and stale-lock timeout handling
      without adding any login or session HTTP endpoints yet.
    tests:
      - cargo test -p sourcebot-api file_local_session_store_times_out_when_lock_file_is_stale -- --nocapture: passed
      - cargo test -p sourcebot-api local_session -- --nocapture: passed (6 tests)
      - cargo test -p sourcebot-api bootstrap -- --nocapture: passed (11 tests)
      - cargo test -p sourcebot-config -- --nocapture: passed (2 tests)
      - cargo fmt --all --check: passed
      - cargo check: passed
    review:
      code: approved by one independent combined spec-and-quality review; reviewer confirmed the slice adds only local-session persistence scaffolding, keeps login/session HTTP surfaces absent, preserves fail-closed behavior for invalid JSON and stale locks, and noted only non-blocking cross-platform rename and future contention considerations
    commit: a3d7dc1c6abce87a0c22b8463e5dfd4d39999fdb
    next_task: task25c2b
  - id: task25c1
    status: done
    summary: >-
      Split task25c and completed the first local-login prerequisite slice by
      extending the bootstrap store contract with a read-only bootstrap_state API,
      reusing shared persisted-state parsing so missing or invalid bootstrap files
      still keep onboarding open, and exposing valid persisted bootstrap admin
      state for later password verification without adding any login or session
      endpoints yet.
    tests:
      - cargo test -p sourcebot-api file_bootstrap_store_reads_bootstrap_state_only_when_persisted_state_is_valid -- --nocapture: passed
      - cargo test -p sourcebot-api bootstrap -- --nocapture: passed (11 tests)
      - cargo fmt --all --check: passed
      - cargo check: passed
    review:
      code: approved by one independent combined spec-and-quality review; reviewer confirmed the new bootstrap_state API is minimal, returns None for missing or invalid state, preserves existing bootstrap status semantics through shared parsing, and adds no login/session surface yet
    commit: d5da1a02ffc0a120ca174a74c6cdd7459dcf1003
    next_task: task25c2
  - id: task25b
    status: done
    summary: >-
      Completed the second local-auth slice by adding POST /api/v1/auth/bootstrap,
      hashing the first admin password with Argon2, persisting structured bootstrap
      state to the configured file path, closing bootstrap after successful
      initialization, rejecting repeat initialization with 409 Conflict, and
      hardening the file store with invalid-state detection plus crash-safe
      owner-only persistence.
    tests:
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api file_bootstrap_store_requires_bootstrap_when_state_file_is_invalid_json -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api file_bootstrap_store_persists_initial_admin_state -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api bootstrap_create -- --nocapture: passed (3 tests)
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api bootstrap -- --nocapture: passed (10 tests)
      - /opt/data/home/.cargo/bin/cargo fmt --all --check: passed
      - /opt/data/home/.cargo/bin/cargo check: passed
    review:
      code: approved by a single independent confirming review after one fix; reviewer confirmed the bootstrap store now treats invalid JSON as still-open bootstrap, persists via a crash-safe temp-file plus hard-link path, enforces owner-only 0600 permissions on Unix, and keeps the AlreadyExists race test meaningful
    commit: 1c58ea1b0accae4ae2e1a3000200d0950ab3bf64
    next_task: task25c
  - id: task25a
    status: done
    summary: >-
      Split task25 and completed the first auth slice by adding a file-backed
      bootstrap status contract, config plumbing, and GET /api/v1/auth/bootstrap
      so self-hosted first-run onboarding can detect whether bootstrap is still
      required without implementing login or account creation yet.
    tests:
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api file_bootstrap_store_requires_bootstrap_when_path_is_a_directory -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api bootstrap_status_returns_required_when_state_file_is_missing -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api bootstrap_status_returns_not_required_after_initialization_state_exists -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api -- --nocapture: passed (93 tests)
      - /opt/data/home/.cargo/bin/cargo fmt --all --check: passed
      - /opt/data/home/.cargo/bin/cargo check: passed
    review:
      code: approved by a single independent confirming review after one fix; reviewer confirmed the store now requires a regular file via is_file(), so directories at the configured bootstrap path do not falsely disable bootstrap, and noted only a non-blocking route-level directory-edge-case test gap
    commit: a4d85706888115c1ef683eca7882fefa3ed8950c
    next_task: task25b
  - id: task24b
    status: done
    summary: >-
      Completed the second MCP slice by adding a transport-agnostic tool-call
      execution API in sourcebot-mcp with typed request/response structs, JSON
      argument parsing, dispatch for list_repos/list_tree/read_file/glob/grep,
      and structured success/error results that preserve repository scope by
      delegating execution to the existing sourcebot-core retrieval tools.
    tests:
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-mcp execute_tool_call -- --nocapture: passed (5 tests)
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-mcp -- --nocapture: passed (8 tests)
      - /opt/data/home/.cargo/bin/cargo fmt --all --check: passed
      - /opt/data/home/.cargo/bin/cargo check: passed
    review:
      code: approved by a single independent combined post-implementation review with no blocking issues; reviewer noted the schema/executor strictness mismatch around additionalProperties false and suggested future drift-avoidance around hard-coded dispatch plus broader direct tests for read_file/glob/grep and execution-failure mapping
    commit: b4608191484d3ee838ff0e255ce596f4b39b4d9d
    next_task: task25
  - id: task24a
    status: done
    summary: >-
      Split task24 and completed the first MCP slice by adding a new sourcebot-mcp
      crate that exposes serializable MCP server metadata plus repository-aware
      tool definitions for list_repos, list_tree, read_file, glob, and grep with
      minimal JSON-schema input contracts, without introducing transport/runtime
      handling yet.
    tests:
      - cargo test -p sourcebot-mcp -- --nocapture: passed (3 tests)
      - cargo fmt --all --check: passed
      - cargo check: passed
    review:
      code: approved by a single independent combined spec-and-quality review with no blocking issues; reviewer noted that a later transport slice should confirm MCP wire-format field naming and may optionally tighten schemas with additionalProperties constraints
    commit: b97d61fc99cbdd476507f9dcf6647f61098edca1
    next_task: task24b
  - id: task23b2
    status: done
    summary: >-
      Completed the second task23b slice by wiring POST /api/v1/ask/completions to
      append repo-scoped prompts and assistant replies into an existing private ask
      thread when thread_id is supplied, preserving prior message history, keeping
      the same thread id, copying assistant citations, and advancing updated_at.
    tests:
      - cargo test -p sourcebot-api ask_completions_appends_to_existing_repo_scoped_thread_when_thread_id_is_supplied -- --nocapture: passed
      - cargo test -p sourcebot-api ask_completions -- --nocapture: passed (6 tests)
      - cargo fmt --all --check: passed
      - cargo check: passed
    review:
      code: approved by a single independent diff review covering spec compliance and code quality with no blocking issues; reviewer noted the two-write append path could later be made transactional and suggested an eventual regression test for missing thread_id targets
    commit: cea0984f99d90012e64aaa10459cf92916bf1569
    next_task: task24
  - id: task23b1
    status: done
    summary: >-
      Split task23b and completed the first persistence slice by wiring the ask
      thread store into POST /api/v1/ask/completions so repo-scoped completions
      without a thread_id now create a private thread with repo_scope, a
      prompt-derived title, RFC3339 timestamps, and both the user prompt and
      assistant response messages, while leaving existing-thread appends for the
      next slice.
    tests:
      - cargo test -p sourcebot-api ask_completions_persists_new_repo_scoped_threads -- --nocapture: passed
      - cargo test -p sourcebot-api ask_completions_does_not_create_new_thread_when_thread_id_is_supplied -- --nocapture: passed
      - cargo test -p sourcebot-api ask_completions -- --nocapture: passed (6 tests)
      - cargo test -p sourcebot-api: passed (88 tests)
      - cargo fmt --all --check: passed
      - cargo check: passed
    review:
      code: approved by a single independent diff review covering spec compliance and code quality with no blocking issues; reviewer noted the temporary local_user/id strategy and suggested a future assertion that existing-thread messages remain unchanged on the deferred thread_id path
    commit: 81be9fe73029cd95e56322d5ab5671a89cf06888
    next_task: task23b2
  - id: task23a
    status: done
    summary: >-
      Split task23 and completed the first repo-scoped ask slice by adding
      POST /api/v1/ask/completions, validating trimmed prompt and repo_scope
      input against the catalog, dispatching through the configured LLM provider,
      and returning the existing AskCompletionResponse surface without persisting
      ask threads yet.
    tests:
      - cargo test -p sourcebot-api ask_completions -- --nocapture: passed (4 tests)
      - cargo test -p sourcebot-api: passed (86 tests)
      - cargo fmt --all --check: passed
      - cargo check: passed
    review:
      code: approved by a single independent diff review with no blocking spec gaps, security concerns, or logic errors; reviewer suggested an eventual HashSet optimization for repo-id validation and fuller response-shape assertions
    commit: e2afa2eafb0bbb37822e72eed0878c9e7a12bfc3
    next_task: task23b
  - id: task22b2b2
    status: done
    summary: >-
      Split task22b2b and completed the second ask API slice by adding an
      API-layer AskCompletionResponse that maps sourcebot_core::AskResponse,
      preserves raw citations, and exposes derived rendered_citations for the
      top-level ask completion surface without changing routes or storage models.
    tests:
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api ask::tests::ask_completion_response_includes_rendered_citations_alongside_raw_citations -- --exact: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api ask::tests -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo fmt --all --check: passed
      - /opt/data/home/.cargo/bin/cargo check: passed
    review:
      code: approved by a single independent diff review with no blocking spec gaps, security concerns, or logic errors; reviewer noted the DTO is a preparatory surface and accepted that narrow scope
    commit: cc3e51c32fbc5189f44635d512af3dd7a93ae763
    next_task: task23
  - id: task22b2b1
    status: done
    summary: >-
      Split task22b2b and completed the first ask API slice by adding API-layer
      AskMessageResponse and AskThreadResponse converters that preserve raw
      citations while exposing derived rendered_citations for persisted ask thread
      messages without changing storage models or adding routes.
    tests:
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api ask_message_response_includes_rendered_citations_alongside_raw_citations -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api ask_thread_response_maps_message_rendered_citations -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api ask -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo fmt --all --check: passed
      - /opt/data/home/.cargo/bin/cargo check: passed
    commit: 73d3d5c80fe5bc2de64e8b946a507b17a9ceb352
    next_task: task22b2b2
  - id: task22b2a
    status: done
    summary: >-
      Split task22b2 and completed the first response-shaping slice by adding a
      shared AskRenderedCitation payload plus AskCitation::rendered() so later ask
      API and UI surfaces can reuse canonical machine-readable and human-facing
      citation metadata without recomputing labels or pinned locations.
    tests:
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-models ask_citation_rendered_payload_includes_machine_and_human_facing_fields -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-models -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo fmt --all --check: passed
      - /opt/data/home/.cargo/bin/cargo check: passed
    commit: f9f136a3494a5d1b3df441a7b0b03f88f71d4520
    next_task: task22b2b
  - id: task22b1
    status: done
    summary: >-
      Split task22b and completed the first renderer slice by adding shared
      AskCitation helpers for display labels, line fragments, and revision-pinned
      location strings so later ask API/UI work can reuse canonical citation text.
    tests:
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-models ask_citation_renders_display_labels_for_single_lines_and_ranges -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-models ask_citation_renders_pinned_locations_with_revision_and_line_fragments -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo fmt --all --check: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-models -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo check: passed
    commit: 68dccd538026e36e4801bc26e9e5cac92201c287
    next_task: task22b2
  - id: task22a
    status: done
    summary: >-
      Split task22 and completed the first slice by adding a shared ask citation
      model in sourcebot-models, extending persisted ask messages to carry
      citations, and reusing that shared type from sourcebot-core so reopened
      thread history can preserve machine-readable citation metadata.
    tests:
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-models ask_message_serialization_includes_machine_readable_citations -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-api in_memory_store_preserves_message_citations_across_reload_and_replace -- --nocapture: passed
      - /opt/data/home/.cargo/bin/cargo fmt --all --check: passed
      - /opt/data/home/.cargo/bin/cargo test -p sourcebot-models -p sourcebot-core -p sourcebot-api: passed
      - /opt/data/home/.cargo/bin/cargo check: passed
    commit: 8202cba73cf9a352a0650f5116b1d12e86136658
    next_task: task22b
  - id: task21c2b2b
    status: done
    summary: >-
      Completed the replacement follow-up slice by adding owner-scoped full ask
      thread message replacement to the async thread-store contract and in-memory
      implementation, preserving message order while refreshing thread updated_at
      metadata.
    tests:
      - cargo test -p sourcebot-api replace_message -- --nocapture: passed (2 tests)
      - cargo test -p sourcebot-api ask -- --nocapture: passed (17 tests)
      - cargo test -p sourcebot-core -- --nocapture: passed (21 tests)
      - cargo fmt --check: passed
      - cargo check: passed
    commit: 321089ff9ab60c12f88fffd3ce3ff07cf203994a
    next_task: task22
  - id: task21c2b2a
    status: done
    summary: >-
      Split task21c2b2 and completed the deletion-first slice by adding
      owner-scoped ask thread message deletion to the async thread-store contract
      and in-memory implementation, preserving remaining message order while
      refreshing thread updated_at metadata.
    tests:
      - cargo test -p sourcebot-api delete_message -- --nocapture: passed (1 test)
      - cargo test -p sourcebot-api ask -- --nocapture: passed (15 tests)
      - cargo test -p sourcebot-core -- --nocapture: passed (21 tests)
      - cargo fmt --check: passed
      - cargo check: passed
    commit: 48df3328da434c69648f362d037c8c4118e6856f
    next_task: task21c2b2b
  - id: task21c2b1
    status: done
    summary: >-
      Split task21c2b and completed the first mutation slice by adding owner-scoped
      ask thread message content updates to the async thread-store contract and
      in-memory implementation, preserving message order while refreshing thread
      updated_at metadata.
    tests:
      - cargo test -p sourcebot-api in_memory_store_updates_message_content_for_owner -- --nocapture: passed (1 test)
      - cargo test -p sourcebot-api in_memory_store_does_not_update_message_for_non_owner_or_missing_message -- --nocapture: passed (1 test)
      - cargo test -p sourcebot-api ask -- --nocapture: passed (13 tests)
      - cargo test -p sourcebot-core -- --nocapture: passed (21 tests)
      - cargo fmt --check: passed
      - cargo check: passed
    commit: dec5fcdd61f545b997ec1c9a76d415bfcab336d7
    next_task: task21c2b2
  - id: task19
    status: done
    summary: >-
      Added async LLM provider abstraction and safe public config metadata.
    tests:
      - cargo test: passed
      - cargo check: passed
    commit: 842f68f
    next_task: task20a
  - id: task20a
    status: done
    summary: >-
      Added reusable async retrieval-tool contracts and the first scoped
      list_repos tool slice with API/catalog async alignment.
    tests:
      - cargo test -p sourcebot-core: passed (5 tests)
      - cargo test -p sourcebot-api repos: passed (4 tests)
      - cargo test -p sourcebot-api storage: passed (3 tests)
      - cargo check: passed
    commit: 81d7ad3
    next_task: task20b
  - id: task20b
    status: done
    summary: >-
      Added reusable async tree retrieval contracts, a machine-readable
      list_tree tool result, active-scope validation, and a browse adapter for
      future retrieval orchestration.
    tests:
      - cargo test -p sourcebot-core list_tree_tool -- --nocapture: passed (4 tests)
      - cargo test -p sourcebot-core: passed (9 tests)
      - cargo test -p sourcebot-api browse: passed (9 tests)
      - cargo check: passed
    commit: 25140c207795503afb28c22d7c3dfc02cc7e36b0
    next_task: task20c
  - id: task20c
    status: done
    summary: >-
      Added reusable async blob retrieval contracts, a machine-readable
      read_file tool result, active-scope and path validation, and a browse
      blob adapter for retrieval reuse.
    tests:
      - cargo fmt --check: passed
      - cargo test -p sourcebot-core read_file_tool -- --nocapture: passed (4 tests)
      - cargo test -p sourcebot-api browse_blob_store_adapter_converts_browse_blob_for_core_retrieval -- --nocapture: passed (1 test)
      - cargo test -p sourcebot-core -- --nocapture: passed (13 tests)
      - cargo test -p sourcebot-api browse::tests -- --nocapture: passed (5 tests)
      - cargo check: passed
    commit: e4d9ae533ee95a54d73e52789ea70a990c863505
    next_task: task20d
  - id: task20d1
    status: done
    summary: >-
      Added reusable async glob retrieval contracts, a machine-readable glob
      tool result, active-scope and returned-metadata validation, and a browse
      glob adapter with regression coverage for browse-visible target and
      symlink paths.
    tests:
      - cargo test -p sourcebot-core glob_tool -- --nocapture: passed (4 tests)
      - cargo test -p sourcebot-api glob -- --nocapture: passed (4 tests)
      - cargo fmt --check: passed
      - cargo test -p sourcebot-core -- --nocapture: passed (17 tests)
      - cargo test -p sourcebot-api browse::tests -- --nocapture: passed (9 tests)
      - cargo check: passed
    commit: d0cbda4376c2956ef0964f6b178c9d188d4a5a65
    next_task: task20d2
  - id: task20d2
    status: done
    summary: >-
      Added reusable async grep retrieval contracts, a machine-readable grep
      tool result, active-scope and returned-metadata validation, and a browse
      grep adapter with regression coverage that blocks symlinked files outside
      the active repository root.
    tests:
      - cargo test -p sourcebot-core grep_tool -- --nocapture: passed (4 tests)
      - cargo test -p sourcebot-api grep -- --nocapture: passed (3 tests)
      - cargo test -p sourcebot-core: passed (21 tests)
      - cargo test -p sourcebot-api: passed (61 tests)
      - cargo fmt --check: passed
      - cargo check: passed
    commit: 285309d61e3b91b4fca0d7c0d06fea2c31c70890
    next_task: task21
  - id: task21a
    status: done
    summary: >-
      Split task21 and completed the first subtask by adding ask-thread domain
      models, async thread-store contracts, and an in-memory persistence slice
      for create/list/get thread history with owner scoping.
    tests:
      - cargo test -p sourcebot-api ask -- --nocapture: passed (4 tests)
      - cargo test -p sourcebot-models -- --nocapture: passed (1 test)
      - cargo test -p sourcebot-core -- --nocapture: passed (21 tests)
      - cargo fmt --check: passed
      - cargo check: passed
    commit: c471f093ca19318ceb7dc3e31d55279560be6a90
    next_task: task21b
  - id: task21b1
    status: done
    summary: >-
      Split task21b and completed the first subtask by adding owner-scoped ask
      thread metadata mutation persistence for rename and visibility updates in
      the async thread-store contract and in-memory store.
    tests:
      - cargo test -p sourcebot-api in_memory_store_updates_thread_title_and_visibility_for_owner -- --nocapture: passed (1 test)
      - cargo test -p sourcebot-api in_memory_store_does_not_update_thread_metadata_for_non_owner -- --nocapture: passed (1 test)
      - cargo test -p sourcebot-api ask -- --nocapture: passed (6 tests)
      - cargo test -p sourcebot-core -- --nocapture: passed (21 tests)
      - cargo fmt --check: passed
      - cargo check: passed
    commit: 5cb73e5bab9883de8dc29c32dfdf98fa6b8a4f0c
    next_task: task21b2
  - id: task21b2
    status: done
    summary: >-
      Completed the second task21b slice by adding owner-scoped ask thread lookup
      by session_id in the async thread-store contract and in-memory
      implementation, with regression coverage for matching and missing sessions.
    tests:
      - cargo test -p sourcebot-api in_memory_store_returns_thread_for_matching_user_session -- --nocapture: passed (1 test)
      - cargo test -p sourcebot-api ask -- --nocapture: passed (7 tests)
      - cargo test -p sourcebot-core -- --nocapture: passed (21 tests)
      - cargo fmt --check: passed
      - cargo check: passed
    commit: a3209e6ba2ccf1db43e40249b59f8340d5c1c9e5
    next_task: task21c
  - id: task21c1
    status: done
    summary: >-
      Split task21c and completed the first slice by adding owner-scoped ask
      thread message append persistence to the async thread-store contract and
      in-memory implementation, including updated thread timestamps and summary
      message counts.
    tests:
      - cargo test -p sourcebot-api in_memory_store_appends_message_for_owner_and_updates_summary -- --nocapture: passed (1 test)
      - cargo test -p sourcebot-api ask -- --nocapture: passed (9 tests)
      - cargo test -p sourcebot-core -- --nocapture: passed (21 tests)
      - cargo fmt --check: passed
      - cargo check: passed
    commit: 606d9c0f5d8cae3151d1cb7c3d0adcd030d8441e
    next_task: task21c2
  - id: task21c2a
    status: done
    summary: >-
      Split task21c2 and completed the first slice by adding owner-scoped ask
      thread message retrieval to the async thread-store contract and in-memory
      implementation, preserving append order and hiding persisted messages from
      non-owners and missing threads.
    tests:
      - cargo test -p sourcebot-api in_memory_store_returns_thread_messages_for_owner_in_append_order -- --nocapture: passed (1 test)
      - cargo test -p sourcebot-api in_memory_store_hides_thread_messages_from_non_owner_and_missing_threads -- --nocapture: passed (1 test)
      - cargo test -p sourcebot-api ask -- --nocapture: passed (11 tests)
      - cargo test -p sourcebot-core -- --nocapture: passed (21 tests)
      - cargo fmt --check: passed
      - cargo check: passed
    commit: 1ea2d0bee86367c951fe1359b61f376c4e07aec7
    next_task: task21c2b
notes:
  execution_rules:
    - docs/status/roadmap-state.yaml is the single source of project status.
    - Execute exactly one smallest meaningful closure per execution unit.
    - A task is complete only after implementation, focused verification, docs/review as needed, commit, and push.
    - After each task, update this file with status, summary, tests, commit, and next task.
    - If a task is too large, split it toward a user-visible, operator-visible, or real implementation-backed closure rather than a paperwork-only fragment.
    - If blocked, write blocked reason and suggested workaround, then switch to the next unblocked parity slice in the following unit.
    - Use docs/plans/2026-04-28-sourcebot-practical-replacement-blockers-roadmap.md as the governing practical-replacement blocker roadmap until it is explicitly superseded.
  blockers:
    - id: follow-on-roadmap-bootstrap
      title: Prior per-task blockers archived by the follow-on roadmap bootstrap
      status: resolved
      evidence: >-
        The previous roadmap-specific blocker notes belonged to superseded pre-audit and
        pre-Task-100 task splits. Task 100 completed the follow-on release-candidate
        audit and failed closed; the governing roadmap is now
        `docs/plans/2026-04-28-sourcebot-practical-replacement-blockers-roadmap.md`.
        The next live work item is the `current_task` recorded above, not any archived
        Task 87b2, Task 86-100, or pre-follow-on blocker thread.
